# MTA5_l 工作区目录整理方案（2026-09-08）

> 状态：**D1~D5 已于 2026-09-08 拍板（见 §七），Phase 0~4 已全部执行完毕**；执行记录见 `00_README.md` §7 的 09-08 21:30 / 23:15 两条。
> 阅读提示：§一 现状画像与 §二 问题清单是**执行前快照**，不代表当前盘面；本文件保留为方案依据与决策存档。
> 依据：两轮全量只读扫描（一级画像 + 黄金/验证目录/备份/git 下钻）+ 代码硬引用核查。
> 口径：扫描跳过 .git / __pycache__ / node_modules / .obsidian；backup_20260907 单独统计。

---

## 一、现状画像（数字先行）

**总量**：约 2.4 GB / 6,400+ 文件。大头分布：

| 一级目录 | 文件数 | 大小 | 性质 |
|---|---:|---:|---|
| 黄金 | 4,707 | 1.6 GB | 策略主战场（6 个策略，其中 30m2H策略 独占 1.02 GB） |
| V型反转策略 | 171 | 253 MB | 策略（已定论降级，数据未清） |
| 原油黄金数据行情策略 | 115 | 195 MB | 策略 |
| 原油 | 265 | 121 MB | 策略族（8 个目录，含实验与门变体） |
| AI数据中心资本开支研报 | 190 | 96 MB | **与交易系统无关的研报抓取** |
| 大周期拐点 | 212 | 21 MB | 策略 + 知识库混排 |
| 宏观日历研究 | 90 | 18 MB | 共享研究模块 |
| backup_20260907 | 558 | 8 MB | 一次性归档（模式正确） |
| observation_dashboard | 94 | 7 MB | **监控运行时产物（勿动）** |
| scripts | 66 | 0.4 MB | 跨策略工具脚本 |
| ea_alignment_logs | 98 | 0.2 MB | Tester 对齐工作区（**验收进行中，暂缓**） |
| 项目文档 / 插件调研 / .workbuddy | 20 | 3 MB | 文档杂项 |

**根目录散文件 24 个**：6 个一次性 py（_check/_tmp/_mk 前缀）、3 个 DSH 启动器 + 2 个图标、2 个 0 字节 node 日志、1 个 1.5 MB 交易标记图 HTML + PNG、10 个跨策略 md 文档。

**git 仓库已死**：`.git` 仅 301 字节、`git log` 报 "not a git repository"——**这个 2.4 GB 的工程当前没有任何版本控制**（.gitignore 是写了规范但无从生效）。

---

## 二、问题清单（按严重度排序，均有扫描证据）

| # | 问题 | 证据 | 影响 |
|---|---|---|---|
| P1 | **git 失效，无版本控制** | .git 301B，非仓库 | EA 源码/文档历史裸奔，误删无法回滚 |
| P2 | **一次性验证目录爆炸** | 黄金\30m2H策略\data\validation 下 130+ 个日期化目录（dynamic_risk_alignment_*、mapped_trade_alignment_*、mt5_log_session_*、stage_state_*…） | data/validation 不可读，当前有效的 expected ledger 被埋没 |
| P3 | **派生大文件重复** | 13.7 MB×3 的 full ledger 三代（deal_history_20260713_v1 / ledger_magic0fix_20260713_v1 / trade_ledger_20260712_v2）；2H 策略 signals 副本 36.3 MB；参考实现工程 base_data 备份 57.7 MB；214.9 MB tester_agent 日志 | 黄金目录虚胖 ~500 MB |
| P4 | **策略目录骨架不统一** | 乖离反转：回测脚本散落策略根目录（backtest_v2..v9、diag_cost、_dbg_v8 + __pycache__）；H1_M30_H4：10 个结果 md 散在策略根；数据行情策略：参考/、说明文档/ 两个空目录；2H_1H_6H 与 30m2H 有"复用模板"仅 30m2H 有 | 新策略无范式可循，找文件靠猜 |
| P5 | **临时文件 203 个散布全树** | 根目录 6 个、scripts 14 个（_fix/_inspect/_exp/_tmp）、宏观日历研究 13 个、ea_alignment_logs\_scratch 100 个；各策略 .bak 25 个 | 噪声大；部分一次性脚本与 backup_20260907\scripts_old 内容重叠 |
| P6 | **与交易系统无关的内容混在工程根** | AI数据中心资本开支研报（96 MB txt/htm 抓取件）、插件调研（3 MB GitHub 清单）、空壳 V型反转策 | 工程边界模糊 |
| P7 | **空目录 22 个 / 0 字节残留** | .codex、V型反转策、scripts\{prepare,signals,validate}、数据行情策略\{参考,说明文档,data\signals}、2H_1H_6H 五个空骨架、node_out/err.txt 等 | 误导"该目录在工作"的错觉（2H_1H_6H 的空 scripts 骨架即例证） |
| P8 | **文档命名与版本链靠人肉** | 策略审查状态_20260906 与 _20260907 并存（靠文内 supersedes 声明）；问题记录尾部两个"§二十" | 引用歧义，已有前车之鉴（09-07 快照专门记了纠正误读） |
| P9 | **复制型公共文档已分叉** | 1H_M30_4H / 2H_M30_6H / 2H_1H_6H 三策略各有一份 `说明文档\00_策略生成规则\原始数据独立规则.md`——1H 与 2H 同为 918B 但 MD5 不同，2H_1H_6H 只有 808B；`MT5直接数据接入方案` 全量版在项目文档、各策略摘要版 3 份、hash 全异；**骨架权威母本《其他策略复用流程》埋在 黄金\30m2H策略\复用模板\**（乖离反转/H1_M30_H4/数据行情等新策略均未持有任何一份） | 规范三处三个版本，新策略靠人肉找母本；母本修订无法传导到实例 |

---

## 三、目标结构设计

原则：**只动"没有硬引用的"，运行时目录全部原地钉死**（本工程 700+ 处代码引用为绝对路径，结构迁移=改代码，收益低、风险高。因此目标不是物理重排，而是"根收口 + 策略骨架统一 + 时间轴归档"三件事）。

```
F:\use_code\MTA5_l\
├── 00_README.md                  # ★新建：项目总索引（见 §五b，入口文档，任何人/新会话 5 分钟看懂全工程）
├── 00_项目规则.md                # ★新建：更新与留档规则（见 §五c，"策略变更/新文件/公共资产"如何登记归档）
├── 00_文档中心\                  # ★新建：跨策略文档唯一入口
│   ├── 策略审查状态.md            #   滚动单文件（旧日期版进 归档\）——跨策略进度主索引
│   ├── 问题记录.md                #   留在项目级：§六起覆盖"所有策略"的审查流水，非单策略文件（§编号冲突修复后滚动）
│   ├── 数据测试对比报告\          #   跨策略对比性质，按日期归档
│   ├── 公共方法论\                # ★跨策略经验/规范文档（判据与清单见 §五）
│   │   ├── MT5_Tester自动调用经验总结_20260908.md
│   │   ├── MT5_Tester同名set覆盖参数坑_20260906.md
│   │   ├── 其他策略复用流程.md      # 自 黄金\30m2H策略\复用模板\ 提级 = 策略目录骨架权威母本
│   │   └── 策略生成规则 / 策略验证总方案 / 策略验证重建执行记录 / MT5数据接入方案(全量版) / 数据接入执行记录 / EA逐笔对齐执行记录 / 策略收益与部署评估 / 原始K线止损回放计算记录（8 份，自 项目文档\ 并入）
│   ├── 归档\                     # 项目文档\ 中的 4 份过程记录（优先级总结/归类清单/深度整理报告/完成度核实）
│   ├── 经验备忘\                  #   其余单点坑/小结按日期入此
│   └── 监控运维\                  #   QQ监控推送使用说明.md（含驱动方=DSH harness 的更正）
├── QQ_AGENT_INSTRUCTIONS.md      # ⚓钉死根目录：网关 directPrompt 绝对路径引用
├── 启动DSH-Web.cmd               # ⚓保留根目录（桌面快捷方式硬指向；如需挪必须同步重建 .lnk）
├── scripts\                      # ⚓保留根目录（monitor 脚本 ROOT 硬编码）
│   └── _archive\2026-09\         # ★一次性脚本统一沉底（_fix/_inspect/_exp/_tmp/_check 前缀）
├── observation_dashboard\        # ⚓不动（运行时产物）
├── ea_alignment_logs\            # ⚓不动（Tester 验收进行中；_scratch 收口待验收闭环后做）
├── 黄金\ 原油\ 原油黄金数据行情策略\ 大周期拐点\ V型反转策略\ 宏观日历研究\
│   │                             # ⚓族目录不动；内部按骨架规范逐步归位
│   └── V型反转策略\V反策略执行计划.md  # ★自根目录下沉（纯 V 反单策略计划；其"实盘启动条件"归 00_策略准备手册 管辖）
├── archive\                      # ★新建：统一归档层
│   ├── backup_20260907\          #   挪入（保持日期命名法，后续备份也进这里）
│   ├── AI数据中心资本开支研报\    #   挪入（或移出本工作区，见决策点D3）
│   └── 参考调研\插件调研\         #   挪入
├── .git\                         # 见决策点D1：重建版本控制
└── （工具杂项）tools\dsh_launcher\ # 可选：2个 ps1 + 图标收口（.cmd 内相对路径需同步改）
```

**策略目录标准骨架**（新策略强制、存量策略在 P2 阶段随验证归档顺带补齐）：

```
<策略名>\
├── README.md            # 一页状态：口径版本/EA版本/对齐率/观察期/当前待办
├── 执行计划.md           # 单策略推进计划（V反策略执行计划.md 即入此槽位；无计划则不放）
├── 说明文档\            # 规格书、手册、验证结果（禁止散在策略根）
├── auto_trade\          # EA 源码(.mq5) + .set + 同步脚本；备份只进 归档\
├── scripts\             # 全部 py（含回测），禁止策略根裸放 .py
├── data\
│   ├── raw\             # mt5_history 只留最新 bundle + 上一代，其余进 _archive\
│   ├── processed\       # 派生数据，可再生者不双份保留
│   ├── signals\         # 当前口径
│   └── validation\      # ★活跃目录 ≤ 20 个；90天前的日期化目录进 _archive\<年>\
└── 归档\                # 历史脚本、旧版本快照
```

---

## 四、分阶段执行计划（每阶段独立可停、先备份后动手）

### Phase 0 · 零风险清面（预计 30 分钟，纯删除→回收站）
1. 删 0 字节残留：`node_out.txt`、`node_err.txt`；
2. 删空目录 22 个（清单见 §五附录 A；**注**：2H_1H_6H 的 5 个空 scripts 骨架删前确认研究线立项是否还要用同构占位）；
3. 删 `V型反转策`（0 文件空壳，真身 `V型反转策略` 健在）；
4. 全树 `__pycache__` 清理（可再生）；
5. 删除本次审计自产脚本 `scripts\_tmp_dir_structure_audit.py`、`_tmp_dir_drill2.py`、`_tmp_ea_chart_audit.py`（如你确认数字已入档）。
- 验证：目录枚举复查；监控 :24/:54 报告正常产出一次。
- 回滚：回收站还原。

### Phase 1 · 根目录收口（1 小时，只挪散文件）
1. 新建 `00_文档中心\`，根级 md 按 §三 迁入：`策略审查状态_20260906.md`、`数据测试对比报告_20260904.md` 等旧日期版进 `00_文档中心\归档\`，滚动主文件留区根；`问题记录_20260904.md` → `00_文档中心\问题记录.md`（跨策略审查流水，属项目级，**不下沉**——§六起标题自证覆盖所有策略）；`V反策略执行计划.md` → `V型反转策略\`（单策略计划下沉本体）；
2. 新建两份顶层文档（内容规范见 §五b/§五c）：`00_README.md`（项目总索引）、`00_项目规则.md`（更新与留档规则）；
2. `1H_M30_4H_五笔空单_交易标记图_20260818.*` → `黄金\1H_M30_4H策略\data\validation\reports_20260818\`（该文件是 08-18 交易标记复盘图，属策略产物）；
3. 根目录 6 个一次性 py 先 grep 确认无引用 → `scripts\_archive\2026-09\`；
4. `QQ_AGENT_INSTRUCTIONS.md`、`启动DSH-Web.cmd` 原地不动（硬引用）；DSH ps1/图标移 `tools\dsh_launcher\` 为可选项，若做：改 .cmd 内 ps1 路径 + 重建桌面 .lnk。
- 验证：grep 全树对被挪文件的路径引用 = 0；监控跑一轮。
- 回滚：逐条反向 mv（每步登记 清单 CSV）。

### Phase 2 · 一次性脚本与验证目录沉底（核心，1~2 小时，规则驱动 + dry-run 清单确认）
1. **scripts\ 与 宏观日历研究\scripts\** 内 `_fix_/_inspect_/_exp_/_tmp_/_check_/_dbg_/_watch_/_tokyo_` 前缀共 ~30 个 → `_archive\2026-09\`（backup_20260907\scripts_old 已含 22 个同名者的，去重只挪增量）；
2. **validation 归档规则**（黄金\30m2H策略 为主，130+ → 活跃 ≤20）：
   - 判定"活跃"：被当前任何 .py/.md/.set 引用的目录，或 mtime 在 14 天内，或名称含当前主线（mainline_v337_tester_20260907、final_ea_alignment_20260811、ea_python_full_history_alignment 等）；
   - 其余按 `data\validation\_archive\2026-07\` 等年月沉底；执行前先跑自动引用扫描生成**逐目录 dry-run 清单**，你过目确认后才动；
3. 乖离反转策略根目录的 backtest_*.py/diag_cost.py → 本策略 `scripts\`（这些脚本用绝对路径 sys.path，移动安全，仍逐个 py_compile + 干跑 import 验证）；H1_M30_H4 根级 10 个结果 md → `说明文档\03_验证结果\`；数据行情策略 `参考\`、`说明文档\` 空目录：填充或删除（按内容归属）；
4. `.bak`/`.bak_日期` 文件 25 个（黄金系）→ 各策略 `归档\`（同名 .set/.ex5 的旧版直接留最新+上一版即可，其余进归档）。
- 冻结区：`黄金\30m2H策略\auto_trade\`、`ea_alignment_logs\` —— 30m2H 主线 Tester 验收闭环前不动（见既有待办）。
- 验证：monitor_cycle + Tester 驱动脚本干跑一次；对齐链冒烟（原油 2H 53/53 脚本若被挪，先修路径再验）。
- 回滚：dry-run 清单反向执行。

### Phase 3 · 大对象瘦身（1 小时，黄金 1.6 GB → 约 1.0 GB）
| 动作 | 对象 | 预计回收 |
|---|---|---:|
| 压缩 7z（保留原件删除需你逐项点头） | 30m2H mt5_stage_state_full 2018 的 tester_agent 日志 214.9 MB | ~210 MB |
| 三代 full ledger 去重留最新 | mt5_deal_history_full_20260713_v1 等 13.7 MB×3 | ~27 MB |
| 压缩 | 参考实现工程 base_data_backup_20260811 57.7 MB | ~50 MB |
| 压缩或删（可由信号管线再生，删前核管线） | 2H_M30_6H data\signals\_backup_20260811 36.3 MB | ~36 MB |
| mt5_history bundle 每策略只留最新+上一代 | 各策略 4~24 MB×若干 | ~80 MB |
| V型反转策略 processed 特征 80 MB 压缩（策略已降级观察） | XAU/USOIL M15/M30 features | ~60 MB |
| **合计** | | **≈ 460 MB** |
- 另：AI数据中心资本开支研报 96 MB 见决策点 D3。

### Phase 4 · 版本控制重建（决策点 D1 通过后执行）
1. `git init` + 现 .gitignore 扩充：`observation_dashboard/`、`ea_alignment_logs/`、`data/`（保留白名单 *.md/manifest）、`archive/`、`*.7z`、`*.bak*`；
2. 首次提交 = 全部源码 + 文档（约 2~3 MB 量级，1.6 GB 数据不进库）；
3. 约定：EA .mq5 每次改动即提交，备份目录模式退役为 `git tag`。

---

## 五、共享文件层（所有/多数策略共同依赖，整理全程视为一个整体）

这些不是某个策略的私产，挪走任何一个会同时断掉多条策略链：

| 共享文件 | 谁依赖它 | 结论 |
|---|---|---|
| `scripts\monitor_all_strategies.py` / `monitor_cycle.py` / `qq_snapshot.py` / `watch_signal_alerts.py` / `qq_digest_push.py` | 全部 9 条策略的监控与推送链路 | 永久钉死 `scripts\`，禁入 `_archive` |
| `scripts\strategy_research_common.py`（SMMA/周期映射公共库） | 乖离反转 backtest 系、各策略研究脚本（经 root scripts 进 sys.path）；**其内部又引用** 黄金\30m2H策略\参考实现工程 | 钉死 + 禁止归档 |
| `scripts\replay_raw_signals_with_stops.py` | 乖离反转 v4/v5/v6/v9 等回测脚本 import | 钉死 + 禁止归档 |
| `大周期拐点\策略开发\阶段4_EA对齐\scripts\monitor_mct_adapter.py`、`原油黄金数据行情策略\scripts\monitor_data_event_adapter.py` | monitor_all_strategies 按路径 import（MCT/DataEvent 实时口径） | 钉死原策略目录 |
| `宏观日历研究\data\calendar_export.csv` | monitor_cycle 事件门、V反 sim 系、filter_sim、数据行情策略 | 钉死 |
| **`黄金\30m2H策略\参考实现工程\` 整目录** | ① `multi_tf_matrix` = 事实上的跨策略验证引擎（H1_M30_H4 的 h1_m30_h4_common.py 按 sys.path 引用其 shadow_tests\multi_tf_matrix）；② `base_data\XAUUSDm30.csv` 等被 Tester 验收 scratch 脚本硬读；③ `data\results\position_sizing_strict_certify_20260627` = 主线 102 笔 PF11.54 认证基准，Tester 对齐验收的对照组；④ strategy_research_common 的 REF_ROOT 指向这里 | **整个目录（含 base_data、results）Phase 2/3 均冻结**；仅 `base_data_backup_20260811`、`scripts\_archive_20260626`、`auto_trade\backup_v336_20260812` 等**名字带 backup/_archive 的子目录**可按 Phase 3 压缩 |
| `1H_M30_4H 与 2H_M30_6H 的 ABC_EA` 精确 stack 代码 | 30m2H 主线 v3.37 的 P0-1 修复即从 ABC 移植——EA 源码间存在"口径血缘" | 整理不动 EA，但归位 .bak 时注意保留血缘可追溯（提交信息/归档说明注明版本） |
| **跨策略方法论文档（公共文档层）** | `MT5_Tester自动调用经验总结_20260908.md`（文内自述"供后续任何策略的 MT5 自动回测复用"）、`MT5_Tester同名set覆盖参数坑_20260906.md`（姊妹篇）、`项目文档\` 下策略生成规则/验证总方案/验证重建执行记录 | **不属任何单一策略**，集中到 `00_文档中心\公共方法论\`；此类文档是工程复利所在，禁止散落或混进策略归档 |
| **Tester 自动化 deploy 脚本群**（模式共享、文件各自独立） | 各策略下 `run_tester_30m2h.py`、`乖离反转\scripts\deploy\run_tester_bias_replay.py`、`MCT\auto_trade\auto_run_tester_mct.py`、`数据行情 auto_trade\auto_run_tester*.py`——同一 BM_CLICK 手法的 N 处副本 | 文件留各策略（各自硬编码路径），但在公共方法论区建**索引表**登记"同款手法、副本清单"，防止各改各的分叉 |

### 什么算"公共文档"（下沉判据，满足任一条即进 00_文档中心，而非留在策略目录）

1. 文中明说"供其他/后续策略复用"，或举例跨两个以上策略；
2. 描述的是**工具与环境层**知识（Tester 调用、编译部署、同名 .set 覆盖、MT5 缓存限制、终端目录结构），而非某策略的信号/参数口径；
3. 被多个策略文档相互引用（如"同名 set 坑"被审查状态表与 Tester 经验总结双向引用）。

反向判据——**留在策略目录**：该策略特有的规格书、认证参数表、验证/对齐报告、待调整记录（如 `黄金\30m2H策略\auto_trade\Tester回测操作清单_20260907.md` 含 30m2H 认证参数，属策略私有，但其**方法论部分**已在 09-08 经验总结中被抽象为公共层，两份并存是"总-分"关系）。

**落地动作（并入 Phase 1 执行）**：
1. 两份 MT5_Tester 文档（根目录）→ `00_文档中心\公共方法论\`；
2. `项目文档\` 拆分：8 份方法论/记录类（策略生成规则、验证总方案、验证重建执行记录、MT5 数据接入方案全量版、数据接入执行记录、EA 逐笔对齐执行记录、策略收益与部署评估、原始 K 线止损回放计算记录）→ 公共方法论；4 份过程记录（优先级 1-3 总结 20260730、文件归类清单 20260815、深度整理报告 20260815、策略整理与完成度核实 20260726）→ `00_文档中心\归档\`；`项目文档\` 目录退役；
3. **骨架母本提级**：`黄金\30m2H策略\复用模板\其他策略复用流程.md`（文内自述"规定如何把这套方法复制成另一套策略"，即 P4 骨架规范的权威母本）→ `公共方法论\` 成为唯一权威版；§三 的标准骨架即以它为基底；原路径留两行指针文件防 30m2H README 等旧引用死链；
4. **复制型实例防分叉（对应 P9）**：三策略各自的 `原始数据独立规则.md`（1H/2H 同 918B 但 MD5 不同、2H_1H_6H 仅 808B）与 `MT5直接数据接入方案.md` 摘要版 ×3（hash 全异）——先出三版 diff 差异表给你裁决权威版，**裁决前不合并、不删任何实例**；裁决后权威版入公共方法论，实例头部改为"引用公共版 + 本策略特例注记"；
5. 建 `00_文档中心\公共方法论\README.md` 索引：文档 → 适用场景 → 关联 deploy 脚本副本清单（Tester BM_CLICK 手法在 4+ 策略各有一份脚本）→ 权威-实例映射表；
6. **引用同步**：全树 grep 被挪文档的旧文件名/旧路径（策略审查状态表、QQ 说明、30m2H\README、脚本与数据接口说明等均有引用），逐一改链，防止文档死链。

> 长期建议（不在本次执行范围）：待 30m2H Tester 验收闭环后，可评估把"参考实现工程"升格为显式的 `shared_engine\` 顶层目录——但那需要一次性改完所有引用路径并全链回归，属于独立工程，不要和本次整理混做。

## 五b · 顶层双文档内容规范（Phase 1 新建，本次整理的"防再乱"机制）

> 本节 = 双文档之一 `00_README.md`；之二 `00_项目规则.md` 见 §五c。

### `00_README.md` —— 项目总索引（回答"整个项目现在什么情况"）

固定七段，每段都指向权威文件而非复述内容（防止索引与实体漂移）：

1. **一句话工程定义**：黄金/原油多策略量化研究工程，Python 研究 + MQL5 EA + DSH harness 监控，DEMO 账户只读监控不下单。
2. **策略状态总表**（从 策略审查状态 滚动生成，标注"数据截至"日期）：策略 | 正式口径版本 | 审查 | 对齐率 | 监控中? | 状态（实盘DEMO/模拟观察/待调整/降级）。9+ 条策略一行一条。
3. **目录地图**：一级目录逐个一句话（是什么、能不能动），标注 §六 冻结区与 ⚓/★ 符号。
4. **共享层清单**：§五 表原样收录（公共脚本、参考实现工程、日历 CSV、适配器、公共方法论）。
5. **运行链路**：MT5 终端(谁)→ EA 导出(哪些文件)→ monitor_cycle(DSH tool-jobs 30min)→ 监测报告/dashboard → QQ 网关；标注"关 DSH Web=监控停"这一单点。
6. **当前开放事项**：TODO-8、Tester 验收余波、待调整三原油、MCT 观察期等，链到登记文件。
7. **最近变更日志**（滚动 10 条）：滚动刷新义务见 `00_项目规则.md` §三 收尾义务第 1 条，滚动规则见其 R7。

## 五c · `00_项目规则.md` 内容规范 —— 更新与留档规则（回答"新东西怎么进、旧东西怎么记"）

> **本节原为 R1~R9 规则表的撰写草案，条文已停用。** 规则的**唯一权威版**见 `00_项目规则.md` §一（配套：§二 公共文档三判据、§三 收尾义务、§四 禁动清单速查）。
> 为何收敛为单源：本方案原 R3 写"登记进 00_README §5"，而权威版与 README 实际编号为 §4 —— 复制型规则表必然漂移（即本方案 §二 P9 所述同类问题），2026-09-09 已修正并撤表。原草案表全文可从 git 历史取回。
> **唯一保留于此、权威版未收录的原则**：规则执行主体 = **谁改谁记**（人或 AI 会话）；每次 AI 大轮收口时（如本次整理），把 00_README §2/§6/§7 刷新一遍作为固定收尾动作。

## 五d · agent 自动执行机制（规则如何对"每个进场的 agent"生效）

本工程的实际作业方有四类 agent（我/千问、DSH harness 会话、Codex 痕迹工程、QQ 机器人网关）。靠"人记得提醒 AI"必然失效，机制分三层：

**第 1 层 · 入口自动加载（零成本，收益最大）**
各主流 agent 都有"进目录自动读约定文件"的机制：`AGENTS.md`（Codex CLI、DSH、opencode、Cursor 等均识别）与 `CLAUDE.md` 同族。**在工程根新建 `AGENTS.md`，内容分四段：A 行为纪律 → B 工程指针 → C 禁止 → D 收尾义务**：
```
# AGENTS.md（工程根 · 全部 agent 进场必读）

## A. 工作纪律（用户定的 4+8 规则，全部 agent 适用）

### A1 基础黄金规则
1. **先思考再编码**：开工前输出需求清单（我的理解 · 关键假设 · 待确认项 · 计划与验证/回滚法），
   确认后才写代码；纯读取/查询类操作除外。
2. **简单优先**：用能达成目标的最少代码实现；主动删冗余内容，不堆"以防万一"的分支与参数。
3. **只改任务相关**：不触碰无关代码、文件与格式；不顺手重构、不顺手美化。
4. **只对最终验收标准负责**：人给目标与验收标准，实现路径 AI 自主决定；
   但遇阻、需绕路或可能要改动目标本身时——停，给出 ①根因分析 ②备选方案与利弊 ③建议方向，
   由人拍板。**边界：路径自主 ≠ 遇阻自决。**

### A2 进阶规则
5. **只做判断，不写死逻辑**：规则/阈值/特例表达为可核验的判断，不散埋常量与补丁式 if-else。
6. **严格控制 token 消耗**：不重复执行同因失败的命令；大扫描/多轮重试先估成本；产出够用即止。
7. **代码冲突先读源码、不妥协**：冲突与疑似 bug 必须读通相关源码再动手，
   禁止 workaround 糊住，不为"跑通"向错误设计让步。
8. **拒绝假测试**：验证走真实下游路径；exit 0 / 脚本自印"成功"字样不算证据；
   结论必须标注证据档位（执行级 / 同层读回 / 端到端）。
9. **分阶段设检查点**：长任务逐阶段落检查点，任一检查点不过即停下汇报，不带病推进。
10. **遵守原有代码风格**：命名、注释语言、目录约定与所在文件/策略现状一致，不自造新范式。
11. **有问题不隐瞒**：报错、降级、跳过、未验证项，如实写进汇报与问题记录。
12. **遇不合理结构主动说明**：发现职责外的结构问题只登记/报告，不擅自修改（走 R 规则流程）。

附 · 问与不问：能自查的（读文件/跑查询/查配置）不问人；答案只在人脑中且猜错返工代价高才问。

## B. 工程指针（细则所在）
1. 00_README.md      —— 项目现状与目录地图
2. 00_项目规则.md    —— R1~R9 变更/新文件/归档留档规则
3. 整理方案 §六      —— 硬引用冻结区清单（违反即破坏运行链路）

## C. 禁止
- 在冻结区移动/删除文件；在 data\ 内建 _备份\ 副本；
- 新建不带 _tmp_ 前缀的一次性脚本；改动 scripts\ 与 observation_dashboard\ 布局。

## D. 收尾义务
- 更新所动策略的 README / 00_README §2·§6·§7 / 问题记录追加节；
- 归档动作留 dry-run 三列清单（源→目标→原因）。
```
> 分层原则：**AGENTS.md 控制在一页内**（太长会稀释遵循度），行为纪律是"每个任务都适用"的最小集，工程细节一律指路 B 段而非复述——复述即漂移。
> 诚实说明约束性质：AGENTS.md 是**软约束**（agent 大概率遵守但无强制保证），"遇阻不自决""收尾刷索引"这类纪律要变硬，依赖第 3 层 check_project_rules.py 事后稽核 + git pre-commit + 权限审批流兜底。
放根目录的原因：这是唯一"每个 agent 都会撞见"的位置；与 §六 冻结区不冲突（新文件，无人引用它的路径）。

**第 2 层 · 网关与会话配置内嵌（针对两个常驻执行体）**
- **QQ 机器人**：其 directPrompt 已硬编码"先阅读 QQ_AGENT_INSTRUCTIONS.md"，在该文件头部追加一段"监控解读与回答须引用 00_README 状态表，不得引用已归档文件"即可生效（Phase 1 一并做，属文档内容修订、不动网关配置）；
- **DSH harness**：它的 tool-jobs 只跑 monitor_cycle（无 LLM 判断），不需要规则注入；但**人工在 DSH 里开的会话**会读本工程根——第 1 层 AGENTS.md 对 DSH 同样生效，一份文件覆盖两家。

**第 3 层 · 机械校验兜底（规则从"软约定"变"硬门禁"）**
文档约束会漂移，脚本校验不会。Phase 4 落地 `scripts\check_project_rules.py`（只读扫描、输出违规清单、不改文件），把可判定的规则转成断言：
| 规则 | 可机检断言 |
|---|---|
| R4 | 工程根与 scripts\ 根出现 `_tmp_` 且 mtime>7 天 → 报"待归档" |
| R7 | 00_文档中心\ 下同名前缀+不同日期的现役文件 >1 → 报"违反滚动单文件" |
| R6 | data\validation 活跃目录数 >20，或出现 `_\d{8}$` 结尾的 _备份类目录 → 报 |
| §六 | 冻结区路径清单做成 allowlist，扫描近 24h 被移动痕迹（mtime 异常）→ 提示核对 |
| R8 | 存在未执行的整理清单 CSV（状态列≠已执行）>7 天 → 报 |
| 基础2 | 新增脚本行数/参数相对同类基线膨胀超阈值 → 提示（简单优先稽核） |
| 基础4/进阶6 | 同一命令在任务日志中因相同错误重复执行 ≥3 次 → 提示（token 消耗守卫） |
| 进阶4 | validation 目录有产物但无对应结论 md（裸实验）→ 报（拒绝假测试） |

> 后三行为 2026-09-08 用户同意后补入。
挂载方式：先手动跑（整理期每周一次），git 重建（D1）后转 pre-commit hook；也可注册进 DSH tool-jobs 做成每日巡检（与监控循环同通道，产出追加进监测报告尾部"工程卫生"一节）。

**我这侧（千问）的生效方式**：我会把"本工程有 00_项目规则 + AGENTS.md 约定，进目录先读、收尾必刷索引"写入我的项目记忆，后续会话自动遵守，无需你再提醒。

> 落地顺序：Phase 1 建 AGENTS.md（5 分钟，纯新增）→ Phase 4 建校验脚本与挂载点 → D1 通过后转 pre-commit。规则文档本身放 Phase 1，机制三层放 Phase 1/4。

## 六、硬引用冻结区（整理全程禁止移动，除非同步改代码并验证）

| 路径 | 被谁引用 |
|---|---|
| `QQ_AGENT_INSTRUCTIONS.md`（根） | DSH qqbot 网关 directPrompt 绝对路径 |
| `启动DSH-Web.cmd`（根） | 桌面快捷方式 .lnk |
| `scripts\`（整目录，含共享库）、`observation_dashboard\`、`ea_alignment_logs\` | monitor 系 ROOT 硬编码；Tester 验收进行中 |
| `宏观日历研究\data\calendar_export.csv` | monitor_cycle、V反 sim 系、filter_sim 等 |
| `大周期拐点\...\monitor_mct_adapter.py`、`原油黄金数据行情策略\scripts\monitor_data_event_adapter.py` | monitor_all_strategies 按路径 import |
| `黄金\30m2H策略\参考实现工程\`（除明确带 backup 字样的子目录） | 见 §五——跨策略引擎 + 认证基准 + Tester 对照组 |
| `黄金\1H_M30_4H策略\scripts\signals\` | 乖离反转回测脚本 sys.path |
| 各策略 `auto_trade\` | MT5 终端 Experts 同步操作（人工流程锚点） |
| `黄金\30m2H策略\` 整目录 | Tester 主线验收进行中（Phase 2 对该策略延后至验收闭环） |

## 附录 A · 空目录清单（Phase 0 处置对象）
**例外：参考实现工程内的 2 个空目录（data\processed、strategy\Strategy_30m2H\analysis）不在 Phase 0 删除**——整目录属 §五 冻结区，其内部脚本可能按约定路径重建写入，删除动作延后至该目录解冻时统一处理。其余 20 个：
.codex / V型反转策 / scripts\{prepare,signals,validate} / 原油黄金数据行情策略\data\signals / 原油黄金数据行情策略\scripts\signals / 原油黄金数据行情策略\参考 / 原油黄金数据行情策略\说明文档 / 大周期拐点\...\mct_mt5_xauusdm_usoilm_20260830_133238 / 大周期拐点\...\阶段0\输出 / 黄金\2H_1H_6H策略\{data\live, data\raw\mt5_history, scripts\{bundle,data_source,prepare,signals,validate}} / 黄金\30m2H策略\归档\legacy_nested...\data\signals_mt5 / backup_20260907 内 2 个（归档区保留不动）

---

## 七、需要你拍板的决策点

> **2026-09-08 拍板留档**：用户指示"同意建议、按方案开始工作"。解释为：D1=A（重建 git）；D2=立即执行 Phase 0/1；D3=B 的建议变体——AI研报与插件调研集中入 archive\（与 D5 一并执行，不强行移出盘外）；D4=A（6H/8H 门与金叉实验进策略归档）；D5=按建议（验收闭环后收口 _scratch，暂冻结）。Phase 4 机检已获同意加三条断言（见 §五d 第 3 层表后注）。执行中若遇本解释未覆盖的新决策点，停下询问。

| # | 决策 | 选项 | 建议 |
|---|---|---|---|
| D1 | git 重建？ | A 按 Phase 4 重建（推荐） B 不要版本控制，删死 .git，沿用日期备份目录 | A——EA 源码已经历"30m2H 五份源 md5 全不同、版本号无法区分"之乱，git 是根治 |
| D2 | Phase 0-1 是否立即执行 | 现在做 / 等 30m2H Tester 验收后一起做 | 0-1 全是零风险项，可以现在做 |
| D3 | AI数据中心资本开支研报（96 MB）与插件调研 | A 进 archive\ B 移出 MTA5_l（如 F:\use_code\research\） C 原地不动 | B——与交易系统无关，建议离库 |
| D4 | 原油 6H/8H 门策略（无 EA、仅回测变体、09-08 后无动作） | A 进策略\归档\ B 保留观察 | A——金叉实验_20260815 同批处理 |
| D5 | ea_alignment_logs\_scratch（100 个 Tester UI 驱动脚本） | 待主线验收闭环后：有用的提升为 scripts\tester_ui\，其余归档 | 已列入 Phase 2 冻结区，验收后做 |

> 确认 D1~D5 后我从 Phase 0 开始逐阶段执行，每阶段结束跑一轮监控验证 + 出逐文件移动清单（dry-run 先行，重大批次单独点头）。
