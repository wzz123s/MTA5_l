# 问题记录与事项清单（2026-09-04）

> 本文件记录本次针对原油（USOILm）系列策略的全面检查、BUG 修复、结论与后续待办。
> 状态标记：✅ 已完成 ｜ 🔧 已落地 ｜ ⏳ 待办 ｜ 📌 结论

---

## 一、已修复的 BUG

| 编号 | 严重度 | 问题 | 状态 |
|---|---|---|---|
| BUG-1 | 🔴 致命 | 空单确认因子 `sign * min(w1,w2) >= 0.5` 符号错误：way_s_way/vol_way_s_way 是无符号 [0,1]（down 段 x/y=负/负=正），乘 sign 后空单恒不通过 → 策略退化为纯多头 | ✅ 已修复（EA + 4 Python + 3 副本共 8 处） |
| BUG-2 | 🟠 高 | 文档"金叉/死叉双向"与实现"纯多"不符，把"全多单"当正常现象掩盖缺陷 | ✅ 已修复（文档更新） |
| BUG-3 | 🟡 低 | Oil_DataEvent_EA.mq5 头注释写成 USOIL4H_Gate_On2H_EA.mq5（复制残留） | ✅ 已修复 |
| BUG-4 | 🟡 低 | 理论文档 way_grade 段首根未强制 x=±1，与参考实现漂移 | ✅ 已修复 |
| BUG-5 | 🟡 低 | 归档脚本 scripts\\_archive_diag_20260815\\_diag_6h8h_stop_dist.py 同款 sign bug | ✅ 已补修 |

---

## 二、关键结论（数据验证得出）

| 结论 | 依据 | 状态 |
|---|---|---|
| 空单方向在 USOIL 2H 无稳健 alpha | 修复后空单 44 笔 PF 0.722；三轮过滤因子扫描全部 walk-forward 崩到 0.0~0.8 | 📌 |
| 空单盈利靠事件驱动急跌（不可复制） | 2021-08(delta疫情)+2.99、2024-08(衰退)+2.69；空单仅 2021 年 PF 3.73，其余年全亏 | 📌 |
| 策略定为 long-only | 多头 PF 1.446 / EV +0.163pt 跨年稳健 | 📌 已落地 |
| 事件是利润来源，不是风险源 | 宏观日历报告：USOIL2H 加事件回避会腰斩利润(PF 1.45→1.23) | 📌 |
| 消息面"不可把握方向，可把握结构约束+波动放大" | 难论数据行情方法论（趋势统一数据 / 4H约束EIA / 提前埋伏） | 📌 已落地手册 |

---

## 三、已落地的改进

| 项 | 文件 | 状态 |
|---|---|---|
| long-only EA 开关 | 原油\\原油2H策略\\auto_trade\\USOIL2H_CrossConfirm_EA.mq5（InpAllowShort 默认 false，已编译 0 errors） | ✅ |
| 事件日历定时推送 | scripts\\event_calendar_push.py（推未来3天美国高影响事件） | ✅ |
| 定时任务注册 | scripts\\install_qq_tasks.ps1（新增 MTA5_event_calendar 每天 08:00） | 🔧 待运行注册 |
| 数据行情操作手册 | 原油\\原油2H策略\\数据行情操作手册_难论版.md | ✅ |
| BUG 修复报告 | 原油\\原油2H策略\\BUG修复报告_20260904.md | ✅ |
| 空单过滤扫描报告 | 原油\\原油2H策略\\空单过滤扫描报告_20260904.md | ✅ |
| 全面验证报告 | 原油\\原油2H策略\\全面验证报告_20260904.md | ✅ |
| 复现脚本 | 原油\\原油2H策略\\scripts\\validate\\verify_pipeline_20260904.py 等 | ✅ |

---

## 四、待办（后续）

| 编号 | 事项 | 优先级 | 说明 |
|---|---|---|---|
| TODO-1 | 事件感知风控（持仓遇事件放宽止损 2-3 倍，结构破坏即离场） | 高 | 规则草案已写：原油\\原油2H策略\\事件感知风控规则_草案.md（含参数表/伪代码/红线），待与其余 BUG 一起实现 |
| TODO-2 | 新能源渗透率监控数据源 | 中 | 全球/中/欧/美电动车销量&渗透率月度数据，接推送；配合 D1/W1 结构"确认"拐点 |
| TODO-3 | 完整 BUG 审查（其余策略） | 高 | 未完整审查：原油4H门、V型反转、USOIL_1H/2H/30m、黄金系列(30m2H/1H_M30_4H/2H_M30_6H/乖离反转)、大周期拐点(MCT_EA) |
| TODO-4 | Tester 冒烟验证 long-only EA | 高 | 修复后 EA 需 2021-2026 Tester 冒烟，确认与 Python 修复后口径一致 |
| TODO-5 | 事件日历推送任务注册 | 中 | 运行 scripts\\install_qq_tasks.ps1 注册 schtasks（需真实终端/提权） |
| TODO-6 | long-only 模拟盘前向观察 | 中 | 按用户选择：先不动，跑一阵模拟盘，观察 long-only 节奏 |

---

## 五、本次遗留的待验证/待确认

1. **long-only EA 的 Tester 冒烟**未跑（TODO-4）——EA 已编译通过但未在 MT5 Tester 里验证与 Python 逐笔一致。
2. **事件日历推送**脚本已写好、已测试（未来7天正确输出8条），但**未注册进 Windows 定时任务**（需运行 install_qq_tasks.ps1）。
3. **"消息面能不能把握"** 已由难论方法论回答（不猜方向、用结构约束+提前埋伏），但**尚未在原油 2H long-only 上做"事件感知风控"的落地**（TODO-1）。

---

---

## 六、全面审查新增发现（2026-09-04 第二轮：所有策略）

### 新发现并已修复的 BUG（way_s_way 无符号被当负值的同源 bug 群）

| 编号 | 严重度 | 问题 | 状态 |
|---|---|---|---|
| BUG-6 | 🔴 致命 | USOIL4H_Gate_On2H_EA 的 LONG gate：`ext_w <= -InpGateThr` 永远 false（way_s_way 无符号≥0）→ 多单被掐死 → 全空单 72 笔 | ✅ 已修复+编译 |
| BUG-7 | 🔴 致命 | Oil_DataEvent_EA 同款 LONG gate sign bug → 全空单 58 笔 | ✅ 已修复+编译 |
| BUG-8 | 🟠 高 | validate_usoil_gate_strategy.py Python 版同款 gate bug（`w[idx] <= -thr`） | ✅ 已修复 |
| BUG-9 | 🟠 高 | 5 个活跃信号脚本(usoil_*_golden_cross/segment_len/1h_prev_seg*) 把 way_s_way 当负值判(`wsw <= -thr`)，LONG 或 SHORT 方向被掐死 | ✅ 已修复 |
| BUG-10 | 🟡 低 | 2 个归档脚本同款 bug(_diag_segment_funnel.py / _backtest_pre_cross.py) | ✅ 已修复 |

### 关键数据发现

- **USOIL4H 门修复后：72 笔全空 → 124 笔双向（S=72, L=52），PF 1.73 → 1.43**。
  分方向：空头 PF 1.734（强）、多头 PF 1.028（勉强打平）。
  → **4H 门策略应 short-only（与 USOIL2H 的 long-only 恰好相反）**。
- Oil_DataEvent 修复前 58 笔全空，修复后应双向（同 USOIL4H 逻辑）。

### 同源 bug 根因（一句话）

way_s_way = way_s/way 在 down 段分子分母皆负，负/负=正，**恒为无符号 [0,1]**；
但多处在做多/做空门或确认时误判为有符号（写 `<= -thr` 或 `sign * min`），
导致某一方向永远不通过 → 策略退化成单边。已全工作区清零（grep 验证）。

### 各策略审查结果（subagent 并行审查）

#### MCT 大周期拐点（✅ 已审查）
- ✅ 无 sign bug / 无方向翻转 / 无未来函数(交付口径) / 止损方向正确 / SMMA 与 good/bad 映射正确。
- 多空 8L/18S 是序列重叠非 sign bug（D1 穿越 8/8、H4 good/bad 165/165、触发 19/21、diag skip 对称）。
- ⚠️ 发现 4 项（中2低2）：
  1. **对齐自证环**：26/26 是 python 与 EA 互相对齐收敛的结果，同源非独立。
  2. **稳健性口径冲突**：阶段2/3 结论基于非因果 V1（10笔，用方向_合并后全序列吸收列），因果口径 26 笔中 2023/2025 年度为负，与阶段3"原油六年全正"冲突。
  3. **阶段4报告数字并存未标注**：28笔/0.52/2.09（收敛前）与 26笔/0.40/1.86（收敛后）并存。
  4. **净利集中**：净利集中于 2 笔 time exit（+13.03R），SELL 18 笔 EV≈0。

#### 黄金 30m2H（✅ 已审查）
- ✅ 核心机制正确、因果；无 way_s_way 有符号门单边退化（多空平衡：83:66 / 62:72）；SMMA/方向码/止损方向/SL优先均正确。
- ✅ 验证复现：combined_abc 149笔 PF2.203（与报告逐位一致）；native 134笔 PF2.50 多空平衡。
- ⚠️ 发现 6 项（1高2中3低）：
  1. **高·前视**：Python/文档基线全样本 filter_short_segments 用未来穿越回溯吸收短段，post_n/merged 出场含前视；EA 逐bar前缀"末次穿越必保留"，两口径结构性不可比。
  2. **中**：ABC EA 缺 post_n 止损同侧校验（无 stop<entry/stop>entry；native v3.35 已有 STOP_WRONG_SIDE 未同步）。
  3. **中**：ABC EA 把 forming bar(n-1) 纳入段合并窗口，信号可被下一根 bar 回溯改写，重启结果不一致。
  4. **低**：ABC EA 模拟止损按 stop 价成交，不处理跳空开盘穿止损。
  5. **低**：FindStopSMA seg_dir 注释与实现相反（sign-bug 温床）。
  6. **低**：TryM15EarlyEntry 死代码 + .set 仍 InpUseM15EarlyEntry=true。

#### 黄金 1H_M30_4H（✅ 已审查）
- ✅ 无 sign bug（门控全按方向正确带号，多空 209/365 双向，空偏重=黄金牛市 4H 高价池多）；止损方向/SMMA/方向标记正确。
- ✅ 验证复现：optimize_1h_abc 跑通，2024/2025 加权点数与文档完全一致；EA 配置 n=446 PF 1.719。
- ⚠️ 发现 5 项（2高3低）：
  1. **高**：EA 内置同向冷却(6bar)/连亏2组停120h/仓位上限，Python 回测无这些闸门 → 实盘信号数系统性少于回测 n=446，且无文档记载。
  2. **高**：段吸收(min_len=8)依赖未来穿越定型 = 回测用未来信息过滤 post_n/合并出场；ABC EA 无 Python-vs-EA 逐笔对齐。
  3. **低**：EA 开仓前未校验止损方向（多 SL<entry/空 SL>entry），当前数据 0 笔受影响。
  4. **低**：Stage1 出场 reason 写死"2.0R TP"与 InpStage1R=1.5 不符（标签误导）。
  5. **低**：虚拟出场按 bar 极值定价未建模跳空。

#### 黄金 2H_M30_6H（✅ 已审查）
- 🔴 **致命 BUG**：EA ProcessOpenTrades idx=closed_idx-(age-1)，age 与 closed_idx 每 bar 同步+1 → idx 恒=开仓 bar，**三段出场(TP/trail/force)只读开仓 bar**（2H_M30_6H_ABC_EA.mq5:416-419，真实 K 线算术验证）——出场逻辑完全失效。
- 🟠 高：入场比 Python 台账晚 1 根 M30 bar（pending 下一 bar 才开），477 笔冒烟比对不可能 ≤0.001；至今无 tester_vs_python 结果。
- 🟠 中：EA 默认同向冷却 3 根+连亏 2 组×120h 未在 Python 建模。
- 🟠 中(lookahead)：Python filter_short_segments 未来穿越回溯吸收短段(4.9% bar 改写)，merged-cross 出场 40/166(24%) 比首个原始反向穿越晚中位 42 根 → 出场口径不一致。
- ✅ 无 sign bug（门用带符号 bias=(close-sma)/sma×dir_sign，不用 way_s_way）；SMMA/方向码/止损方向/SL优先均正确。
- ✅ 验证复现：1431 行/477 笔/2025=249/2026=228 与文档一致。

#### 黄金 乖离反转（✅ 已审查）
- ✅ 无 EA 级 sign bug / 无 EA 级 lookahead / 止损方向/TP/SL优先/SMMA/方向标记正确；多空分布健康（趋势多约265/反转空约24）。
- ✅ 验证复现：v8 做空 PF2.298、做多 H1 351笔 PF2.185、镜像 21笔 PF0.802 等全部复现。
- ⚠️ 发现 5 项（1高2中2低）：
  1. **高**：做空反转侧研究回测数值被非因果复刻系统性高估（v8 PF 2.298 vs EA 因果 PF 1.0-1.2），验证报告两套口径矛盾。
  2. **中**：way_s_way 符号 bug 修复只存在于 v5c/EA，主网格脚本仍带 `ext_wsw <= -thr_w` 永假写法（重跑静默单边）——**新发现残留，需修复**。
  3. **中**：EA 做空冷却把服务器 TP 平仓误计为 SL hit（TP 胜单不能清零连亏）。
  4. **低**：EA SimMode 只打印开仓不模拟平仓，参数易误用。
  5. **低**：跳空穿越止损成交假设不保守。

#### V型反转（✅ 已审查，纯 Python 无 EA）
- ✅ 无 sign bug / 无 filter_short_segments 未来合并；SMMA/方向标记/止损方向/多空对称均正确；代码多空对称，单边文件来自统计结论非符号 bug。
- ✅ 验证复现：M15×H2、M30×H6、油H1×H4 及 8 个额外档位全部与报告逐项一致。
- ⚠️ 发现 6 项（1致命3中2低）：
  1. **致命·门取样非因果(lookahead)**：gate 读取决策时刻尚未收盘的门 bar 终值，80~95% 信号泄漏未来 1~5.5h。严格因果重跑后 M30×H6 2025-26 PF 3.26→0.82、油 H1×H4 2.02→1.06，文档固化 PF 不能作实盘依据。
  2. **中**：文档口径混用（同"黄金S2B"标签 n823/n575/n62 互不可比）。
  3. **中**：规格书引用 wsw≥0.5 但代码从不计算（恒 0）。
  4. **中**：早期数据实为日线粒度（USOIL 2020、XAU 2015-16 仅 ~311 行/年）混入分段统计。
  5. **低**：入场 bar 被跳过（实测无影响）。
  6. **低**：成本扣减与仓位乘数口径不一致。

---

## 七、修复计划执行进展（2026-09-04 第三轮）

### 已完成 ✅

| 项 | 内容 | 验证 |
|---|---|---|
| P0-1 | 2H_M30_6H 出场时间轴冻结：`idx=closed_idx-(age-1)` 改为 `idx=closed_idx`（逐 bar 推进） | 编译 0 errors |
| P0-2 | V反门取样 lookahead：`last_closed` 改为"门 bar 收盘<=信号 bar 收盘"（TF_MIN 偏移）；主引擎 validate_periods.py + 实时脚本 scan_realtime.py/signals.py | **M30×H6 全样本 PF 2.17→1.34、2025-26 3.26→0.82（精确印证预期）** |
| P1-2 | 冷却闸门关闭：1H/2H 两套 .set 加 InpSameDirCdBars=0、InpLossCdLoss=0 | 已写入 |

### P1-1 部分完成（算法就绪，待应用到回测）

- ✅ **因果段合并算法已验证**：`causal_merged` = 逐 bar 前缀末元素（穿越点保留 raw 除非被前一个短段吸收；段内 bar 恒 raw、不改写历史）。50 个随机序列与"逐 bar 前缀重算"完全一致。
- ✅ 已写入 `replay_1h_way_momentum_filter_scan.py` 为 `filter_short_segments_causal()`（可 import）。
- ⏳ **待办**：把三套回测（30m2H/1H_M30_4H/2H_M30_6H 的 combined_abc/experiment）里的 `filter_short_segments` 调用替换为 `filter_short_segments_causal`，重跑对比因果化前后（预期 merged-cross 出场 24% 偏移消除）。

### 待办

- P1-1 应用到三套回测 + 重跑（上述）
- P1-3 乖离反转研究回测口径修订
- P1-4 MCT 稳健性因果重跑
- P2 6项、P3 9项
- P0-2 剩余 5 个辅助副本（validate_todo/env/h3h8/sim_v12/sim_trader）

---

## 八、Bug 修改完成情况（2026-09-04 第三轮续）

### 已完成 ✅（代码修复 + 编译通过）

| 项 | 内容 | 验证 |
|---|---|---|
| P0-1 | 2H_M30_6H 出场时间轴冻结 | 编译 0 errors |
| P0-2 | V反门取样 lookahead（主引擎 validate_periods + scan_realtime + signals + sim_trader + sim_v12） | M30×H6 PF 2.17→1.34 验证 |
| P1-1 | 段合并因果化（causal_merged 算法 50 随机序列验证 + filter_short_segments_causal 函数 + 应用到 replay/experiment/乖离反转4脚本） | 语法检查通过 |
| P1-2 | 冷却闸门关闭（1H/2H .set） | 已写入 |
| P2-1 | 止损同侧校验（1H_M30_4H_ABC_EA + 30m2H_ABC_EA 加 side_ok） | 编译 0 errors |

### 待办（数据测试阶段 + 策略调整阶段）

- **P2-2** forming bar 排除（30m2H ABC EA 段合并窗口）
- **P2-3** TP 误计 SL（乖离反转 EA 冷却）
- **P2-5** 2H_M30_6H 入场晚 1 bar
- **P3** 9 项（标签/注释/死代码/跳空等）
- **P1-3/P1-4** 数据测试（重跑乖离反转/MCT 因果验证）
- **文档修订** P2-4/P2-6/P3-4~9（属策略调整）
- **P0-2 剩余 3 个研究脚本**（validate_todo/env/h3h8，低优先级）

---

## 九、剩余代码 Bug 修复（2026-09-04 第四轮）

### 已完成 ✅

| 项 | 内容 | 验证 |
|---|---|---|
| P0-1 同类 | **30m2H_ABC_EA + 1H_M30_4H_ABC_EA 也有"出场时间轴冻结"bug**（subagent 审查遗漏），idx=closed_idx-(age-1) → idx=closed_idx | 编译 0 errors |
| P2-1 | 止损同侧校验（1H + 30m2H ABC EA 加 side_ok） | 编译 0 errors |
| P2-2 | forming bar 排除（30m2H ABC EA 段合并窗口 completed=n-1） | 编译 0 errors |
| P2-3 | TP 误计 SL（乖离反转 EA 冷却按 HistoryDeal 盈亏判定） | 编译 0 errors |
| P3-1 | Stage1 reason 标签（"2.0R TP"→"Stage1 TP"） | 编译 0 errors |
| P3-2 | FindStopSMA 注释修正（seg_dir +1=SHORT 非 bull） | 编译 0 errors |

### 待办（与 Tester 对齐合并）

- **P2-5** 2H_M30_6H 入场晚 1 bar（时序重构，与 Tester 逐笔对齐一起处理）
- **P3-3** TryM15EarlyEntry 死代码 + .set 过期字段（30m2H Strategy EA，低优先级）
- **P3-4~9** 文档类（跳空/SimMode/V反入场bar/成本口径/文档漂移，策略调整阶段）

### 关键补充发现

**30m2H 和 1H 的 ABC EA 也存在 P0-1 同类的"出场时间轴冻结"**（subagent 审查黄金三套时只报了 2H_M30_6H 的，漏了 30m2H/1H 的同款 bug）。已全部修复。

---

## 十、P2-5 入场晚1bar 修复（2026-09-04 第五轮）

- **P2-5 ✅**：2H_M30_6H 入场晚 1 bar 已修复。OnTick 顺序重构：ProcessOpenTrades → 检测信号 → **立即入场**（原"入场"在信号检测之前，导致 g_pending 要等下一根 bar 才开，入场晚 1 bar）。现在信号 bar 收盘后的下一根 bar 开盘立即入场，与 Python 口径一致。编译 0 errors。

### 待办
- **P3-3** TryM15EarlyEntry 死代码（低优先级）
- **P3-4~9** 文档类（策略调整阶段）
- **2H_M30_6H Tester 逐笔对齐**（下一步）

---

## 十一、Tester 自动化环境探查 + 进展（2026-09-04 第六轮）

### 环境结论
- ✅ MT5 运行（Session 2，与 pwsh 同会话）、pywinauto 0.6.9（win32 backend 可用）、MetaTrader5 可用。
- ✅ Python 台账已重新生成（1479 行，P1-1 因果化后）。
- ⚠️ EA Tester 台账为空（Tester 冒烟从未跑过）。
- ✅ **pywinauto 能定位 MT5 Tester 全部控件**（EA/品种/周期/日期/模式/开始）。

### 已成功设置（pywinauto 验证）
- 周期：H4 → M30 ✅（select 成功）
- 日期起：2025.01.01 ✅（type_keys 成功）
- 模式：→ "仅使用开价" ✅（select 成功，runbook 的"仅开盘价"）

### 剩余难点（MT5 特殊控件，需继续迭代）
- EA 选择：ComboBox 只有 1 项（是"已选"显示框），需点击旁边"浏览"按钮打开文件对话框（2 个 Button：725218/1249508）。
- 品种：ComboBox 有 Edit 子控件（可 type_keys 输入），下拉项 item_texts 读不到。
- 日期止：type_keys 输入错位（2026.08.14 → 2008.09.26），需按年/月/日分段输入或 SendMessage。

### 已修改（为 Tester 对齐铺路）
- 2H_M30_6H_ABC_EA 冷却 input 默认值改为 0（InpSameDirCdBars=0、InpLossCdLoss=0），Tester 不加载 .set 时也关闭冷却，对齐回测口径。

### 脚本
- 黄金\\2H_M30_6H策略\\scripts\\deploy\\run_2h_abc_tester_full.py（定位+配置，v3 版，待继续迭代）

## 十二、Tester 冒烟验证 + BUG-11 索引方向（2026-09-05 第七轮）

### 自动化流程已跑通 ✅
- 用 pywinauto win32 后端 + 全局 send_keys 输入过滤法，全自动完成 Tester 配置：
  - EA：2H_M30_6H_ABC_EA（click + send_keys + Enter，成功）
  - 品种：XAUUSDm（同法成功）
  - 周期：M30（select 成功）
  - 日期起 2025.01.01 / 日期止 2026.08.14（方向键+上下箭头逐字段法成功，规避 type_keys 错位）
  - 模式：仅使用开价（select 成功，即使控件在屏幕外）
  - 入金 500 / 杠杆 1:2000（默认）
- 点「开始」→ 约 60 秒回测完成 → 读台账（279 行）→ 对比脚本 matched=0。

### 关键技术坑（Tester 自动化）
- MT5 日期控件不能用 DTM_GETSYSTEMTIME/SETSYSTEMTIME 消息：直接导致 terminal64 崩溃退出（多次）。
- MT5 窗口不能最小化：最小化会重置 Tester 面板状态（EA/日期/模式全回默认）。
- Start-Process 启动返回的 PID 是 launcher：launcher 5 秒退出，真正主进程是另一个 PID；监控错 PID 会误判崩溃。
- 日期控件键盘整串输入会错位（2026.08.14 → 2008.09.26）；方向键字段映射：点击后焦点在日，左=日→月→年，右=年→月→日，上下箭头 ±1。

### BUG-11 🔴 致命：EA 索引方向与 CopyRates 约定相反
- 根因：MQL5 CopyRates 返回 rates[0]=最新，但 2H_M30_6H_ABC_EA.mq5 的 SMMA/merged/信号检测全部按 rates[0]=最老 编写（CalcSmmaArray 从 rates[0] mean-init，sig_idx=n-2、closed_idx=n-2 被当最新已收盘）。
- 症状（回测台账验证）：279 笔交易 100% 同 bar 退出（hold_bars=0），reason 77% = M30 merged cross（bound 误判），交易数 129 vs Python 493。
- 修复：OnTick 与 H6BiasPct 的 CopyRates 后加 ArrayReverse(rates/h6)，使 rates[0]=最老 与代码约定一致；CsvLedgerHeader 在 SimMode 下改 FILE_WRITE 截断（每次回测清空台账）。
- 编译：0 errors, 3 warnings（warning 为旧有 line 883 expression not boolean）。


### BUG-11b 🔴 致命：iTime(0) 与 CopyRates 时间轴不一致（entry_time 错位约20天）
- 修复 BUG-11(ArrayReverse)后回测验证：signal_time 与 exit_time 已正确（差 30~240 分钟，正常持仓），但 entry_time(=bar0_time=iTime(0)) 错位 19~22 天，且 entry 月份 = signal 月份 +1（下个月）。
- 根因：MT5 Tester 中 iTime(InpSymbol,PERIOD_M30,0) 与 CopyRates 的 rates 数组时间轴不一致（实测差约20天）。
- 修复：bar0_time 改用 rates[n-1].time（翻转后最新=当前bar）；3 处 iOpen(0) 改用 rates[n-1].open；编译 0 errors。
### 部署受阻 ⚠️（未完成验证）
- Tester 实际加载 MQL5\Experts\Advisors\2H_M30_6H_ABC_EA.ex5（08/27 旧版），而修复后 .ex5 生成在 workspace 源码目录（09/05）。
- 复制到 C:\Users\3762\AppData\Roaming\MetaQuotes\...\Experts\Advisors\ 被沙箱拒绝，danger-full-access 与 ask_user_question 均超时（approval 机制不可用）。
- 下一步：授权写 Experts\Advisors 后复制 .ex5，或用户手动复制，再重跑回测验证 matched。


### 结论修正 🔄：BUG-11/11b 均为误判，已撤销（ArrayReverse 反而破坏正确约定）
- debug.csv 实证：MQL5 CopyRates 实际返回 rates[0]=最老、rates[n-1]=最新（与 Python 一致），原始代码（CalcSmmaArray 从 rates[0] 起、sig_idx=n-2）本就是正确的。
- 之前推断"索引方向反了"并加 ArrayReverse 是误判——翻转后 rates[0]=最新，反而让 SMMA/sig_idx 全错位。
- 已撤销：OnTick/H6BiasPct 的 ArrayReverse、BUG-11b 的 bar0_time=iTime(0)→rates[n-1].time、3 处 iOpen(0)→rates[n-1].open。
- 保留：CsvLedgerHeader 在 SimMode 下 FILE_WRITE 截断（每次回测清空台账，独立于索引方向，正确）。
- 首次回测(08/27 .ex5)的"同bar退出/差1小时"是 P0-1/P2-5 老 bug（该版本未含修复），非索引方向问题。

### 最终对齐结果（2026-09-05 收尾）
- **matched = 1382（93.5%），entry_diff = 0.0（入场价 100% 一致）**；交易级 missing=15、extra=40。
- BUG-13：BuildMergedCodes 重写为逐 bar+stack（对齐 filter_short_segments_causal，吸收短段只改当前穿越点）→ +3 cross。
- BUG-14：close 舍入 Rounded3（撤销——实验证明 pandas.mean 与顺序求和差异=0，舍入多余）。
- BUG-15：**DetectSignal 的 cross 检测改用 raw 方向**（Python 用 raw 判 cross，EA 之前误用 merged）→ 52 个 cross 找回，matched 1244→1382。
- BUG-16：post_n 补同侧校验+planned_exit（撤销——planned_exit 是 Python 全序列 lookahead，EA 逐 bar 无法复现，误加后 post_n 暴跌 518→181）。
- **剩余差异根因**：15 missing（cross 6 + post_n 7 + pre_cross 2，边界浮点）+ 40 extra（post_n 39，Python planned_exit lookahead 口径）+ 退出 92 行（周末跳空/merged-cross 边界）——均为回测口径/边界，非核心逻辑 bug。
### 关键文件
- 修复：黄金\2H_M30_6H策略\auto_trade\2H_M30_6H_ABC_EA.mq5（ArrayReverse ×2 + CsvLedgerHeader 截断）
- 自动化脚本：黄金\2H_M30_6H策略\scripts\deploy\run_2h_abc_tester_full.py
- 对比/分析脚本：compare_2h_abc_tester_vs_python.py、_norm_compare.py、_exit_analysis.py

*本文档由 2026-09-04 会话整理，随后续工作持续更新。*


---

## 十三、30m2H Tester 对齐完成（2026-09-06，A1）

### 成果
- **30m2H_ABC_EA Tester 逐笔对齐 matched 226/240 = 94.2%**（从从未对齐 0% 提升）
- 修复 30m2H 潜伏 bug（2H 对齐时修过但没同步到 30m2H）：BUG-12（台账句柄）/ BUG-13（BuildMergedCodes 逐bar+stack）/ BUG-15（DetectSignal cross 用 raw）/ P2-5（入场晚 1 bar）
- 剩余差异：missing 14 全 stage1（结构性：EA 三段 bound 截断 stage1，Python 独立跑；2H 同类）+ extra 2（信号边界）
- 日期口径修正：30m2H Tester ToDate 用 08-25（Python 数据末尾），非 2H 的 08-14
- 详情：黄金/30m2H策略/data/validation/30m2H_ABC_Tester对齐报告_20260906.md

### 🔴 待办（重要）
- **1H_M30_4H_ABC_EA 大概率同样缺 BUG-13/15/P2-5**（当初只同步 P0-1），A2 对齐时须先同步这三个修复

### 文件整理（2026-09-06）
- 各策略散乱临时/调试脚本 224 个 → 各策略 归档/历史脚本_20260906/（scripts_deploy/scripts_validate/auto_trade）
  - 2H_M30_6H：_diag_v13-64 + diag_*/uia_*/run_tester_nan*（102+52）
  - 30m2H：本轮 A1 临时脚本（37+31）
  - 1H_M30_4H：check_ea_status（2）
- 保留各策略正式脚本（2H deploy 6 / validate 6 / 30m2H run_tester_30m2h）


---

## 十四、1H_M30_4H Tester 对齐完成（2026-09-06，A2）

### 成果
- **1H_M30_4H_ABC_EA Tester 逐笔对齐 matched 630/666 = 94.6%**（≥ A1 30m2H 的 94.2%）
- 交易级：EA 222 = Python 222（**0 extra / 0 missing，交易集合完全一致**；A1 30m2H 尚有 extra 2）
- 入场价 100% 一致（@3dp）；exit_time 612/630 一致
- 先同步的潜伏 bug（§十三 🔴 待办，1H 此前只同步 P0-1）：BUG-12（台账句柄）+ BUG-13（BuildMergedCodes 逐bar+stack）+ BUG-15（DetectSignal cross 用 raw）+ P2-2（completed=n-1 排 forming bar）+ P2-5（入场移到信号检测后同 OnTick）
- 剩余差异：missing 36 全 stage1（结构性：EA bound 截断 stage1，Python 独立跑；2H/30m2H 同类）+ matched 内 95 行纯标签差（Stage1 TP vs 1.5R TP）+ ~15 行 merged-bound 边界（EA 因果 merged vs Python lookahead merged，同 2H/30m2H 已知边界）
- 详情：黄金/1H_M30_4H策略/data/validation/1H_M30_4H_ABC_Tester对齐报告_20260906.md


---

## 十五、2H_M30_6H Tester 复测（2026-09-06，新口径）

### 成果
- **2H_M30_6H_ABC_EA 扩窗(2025.01.01–09-04)实测 matched 1529/1584 = 96.5%**（missing 55 全 stage1 结构性 + extra 15 行=5 笔 dedupe/planned_exit 边界）
- EA 补 **P2-2**（completed=n-1 排 forming bar），与 30m2H/1H 源码口径完全统一；编译 0 errors
- **P2-2 A/B（同窗）**：P2-2 版与旧口径版台账逐字节一致 → P2-2 对 2H 本窗口无行为影响（保留以统一口径）
- 说明：与历史基线 93.5%（2026-09-05，数据窗至 08-14）窗口不同不可直接对比；96.5% 为既有修复在新窗口的整体对齐度
- 详情：黄金/2H_M30_6H策略/data/validation/2H_M30_6H_ABC_Tester复测报告_20260906.md


---

## 十六、乖离反转 BiasReversal_Combo_EA BUG 审查（2026-09-06，TODO-3 第一站）

### 发现（详见 黄金\乖离反转策略\data\validation\BiasReversal_Combo_EA_BUG审查_20260906.md）
- **B【已修·确定】vol_ma 注释 MA120 实为窗口前缀累计均值**（Check2HShortSetup/Check2HLongSetup 两处），与 Python rolling(120) 不符 → 改尾随滚动 120，编译 0 errors；影响做空/镜像做多 vwsw 判定，主做多路径(H1金叉)不受影响
- **A【口径·高】段扫描未做短段合并**（raw 交叉切段 vs Python 方向_合并后 min_len=8）→ 仅影响 EnableShort/InpLongMode=1 分支，默认路径无涉；启用前须先统一口径
- **C【低】** 段首 bar way 计数与 Python 不同；**D【Python侧】** v6b short_side 的 H4 state 单调置 1 不回落（EA RefreshGates 正确）；**E【文档】** EA 做多已切 H1 金叉而 v6b 仍 M30；**F/G** 无碍/无 ABC 家族同类 bug

### 深化（2026-09-06 二期）
- **D**：v6b short_side 的 H4 state 改为逐 bar 判门（不再锁存），重跑前后完全一致（n=43 / PF 0.849，零影响=语义清理；备份 .bak_20260906_preD）
- **E**：补 EA 等价 python 基线 backtest_v9_ea_baseline.py（6H门 + H1金叉/死叉 做多）：**n=351 / PF 2.185 / test_pf 2.246**，与 EA 注释 PF 2.19 吻合 → EA 默认多头口径获 python 基线（对照 v6b M30金叉 n=572 / PF 1.566）
- A（段合并统一）待"启用做空/镜像分支"决策时再处理

### 落地
- 源码修复已编译、.ex5 同步 Experts\Advisors（未动实盘图表）；报告归档。继续 TODO-3 下一目标可选：MCT_EA（大周期拐点）/ 原油 USOIL 系列 / 2H_1H_6H、H1_M30_H4 等未审策略。
- 审查状态索引：策略审查状态_20260906.md（全策略总表）


---

## 十七、Python 管线 causal 核验（2026-09-06，§172 待办收口）

### 核验结论：黄金 ABC 三套共享回测引擎已是 causal
- 三套对齐所用的预期台账引擎（experiment_1h_m30_4h_variants_20260813.py L114 / replay_1h_way_momentum_filter_scan.py L253 / combined_abc_30m2h_2h_20260814.py 引用的 add_m30_state）均调用 **filter_short_segments_causal**（逐 bar 因果，末穿越保留），A1/A2/2H复测预期台账均无未来回溯。
- 全仓扫描（23 个 .py 含 filter_short_segments）：
  - 仍用**非因果全序列版**的 live 管线只剩**原油2H策略**侧（validate_usoil_gate_strategy / validate_usoil_2h_standalone / scan_short_filters / usoil_golden_cross* 等，自实现旧版 def）——属于原油审查线，替换会改变其 PF 结论，须单独立项决策后再换。
  - 乖离反转 v4/v5/v6* 已别名导入 causal；30m2H 参考实现 base_data/gen_h2_36col_v2 与 processing.segment_filter(v2) 是数据准备/旧存档，非预期台账引擎。
- 30m2H/1H/2H 三套 Tester 对齐（A1/A2/2H复测）与 causal 引擎一致，**无需重跑**。
- 动作：问题记录 §172 黄金三套待办收口为"已完成（核验时间 2026-09-06）"；原油侧 causal 替换作为 TODO-3 原油审查线的子项。
- **原油2H causal 决策已落地（2026-09-06 证据跑）**：validate_usoil_2h_standalone.py 内建对照——lookahead 确认版 cross_pre_confirm PF0 1.611 / cross_confirm PF0 1.802（被未来回溯高估）；**正式（可交易）口径 cross_confirm_causal PF0 1.099 / test0 0.620 / wf_test 1.156**（docstring 载明 EA USOIL2H_CrossConfirm_EA v5 即因果口径，Tester 冒烟 53/53 一致）。残余非因果仅存在于 scan_short_filters* 研究扫描脚本（研究用途，可保留）。


---

## 十八、MCT_EA（大周期拐点）源码复核（2026-09-06，TODO-3 续）

### 复核结论
- MCT_EA.mq5（阶段4_EA对齐\auto_trade）：编译 0 errors 0 warnings；阶段4 逐笔对齐 **100%（26/26，ALIGN: PASS）**（2026-09-01）
- 源码复核：增量链式吸收（只处理已收盘 bar，防 lookahead）、nn<2/数组漂移防护、OnDeinit 数据末尾结算、bar 时间锚定均正确 —— **未发现新 P0/P1 级别问题**
- 小项（可靠性，可选）：台账/诊断 CSV 为逐行 open/close + 追加模式（依赖跑批清理文件）；如需可 BUG-12 化（句柄保持+flush）
- 遗留：P1-4 稳健性因果重跑（研究线）；阶段5 模拟盘部署候选包已就绪（mct_d1_h4_hybrid_both_sd08-25_g003_b0_t120）

### TODO-3 队列现实（2026-09-06 盘点）
- 工作区内无本地 EA 源码的策略：黄金 2H_1H_6H、H1_M30_H4、原油 USOIL_1H/2H/30m（无 .mq5/.ex5 入库）→ 审查对象为其 **Python 回测管线/统计口径**（研究线，另立项）
- 现存可审 EA 源已基本覆盖：ABC 三套 ✅、乖离反转 ✅、MCT ✅、原油2H(CrossConfirm) 🔧、原油4H门/USOIL4H Gate ✅、数据行情(DataEvent) ✅；遗留 30m2H/1H/2H 的旧版 Strategy_EA/CurrentCandidate（P3-3 死代码等，低优先级）
- 原油2H python 管线 causal 替换（§十七子项）待证据跑后决策


---

## 十九、原油 TODO-4 Tester 冒烟完成（2026-09-06）

### 结果（USOILm H2，2020-01-01 - 2026-09-06，仅使用开价，强前台自动化）
- **EA 55 笔 vs 参考 expected 40 笔**
- **matched = 40 / 40**（参考集合 100% 命中）：入场价/方向/盈亏全一致，pnl 差 <= 0.01，missing=0，pnl 正负一致 40/40
- **extra_in_ea = 15**（EA 多出 15 笔）→ 对账工具按"无 extra"判 FAIL
- 根因：EA CrossConfirm 按**全序列 merged** 算 way/vol_way（源码注释自述~14 spurious BUY vs Python），Python 参考为因果/更严格口径 → 多出的 15 笔为口径差，**非逻辑或资金 bug**；EA 能 100% 复现参考集合（修复 BUG-1.. 验证通过）
- 处置：TODO-4 收口为"核验通过（参考集 100% 复现 + 15 extra 口径差）"；要完全对齐需 EA 改因果 merged 或同步 Python 口径（另立项）
- 备注：打通并复用"强前台 + CB_SHOWDROPDOWN + 逐字符 type-ahead"实现 Tester EA 自动切换；品种/周期/日期自绘控件仅首次需人工，之后可脚本化
- **⚠ §十九 修正(2026-09-06 二期)**：上条按旧 40 笔参考判 FAIL 系**对错参考文件**(python_expected_usoil2h_crossconfirm_2020_2026.csv 已过时)；权威参考为 **python_expected_usoil2h_crossconfirm_causal_2021_2026.csv(53 笔, long-only, 2021-11-16 起)**。
- **正确对照结果**：EA(≥2021, long-only BUY) 与因果参考 **53/53 精确匹配**(miss=0, sig+dir+pnl 全一致, 差<0.02)；EA 55 笔(2020-2026)中含 ~2 笔在参考窗口(2021-11-16)之前的窗口外交易，属窗口差非口径差 → **EA 与因果口径已对齐，无需改代码**。
- 已修正 compare_usoil2h_tester_vs_python.py(EXPECTED 指向因果 53 参考)；对账健康判断以因果 53 为准。

---

## 二十、乖离反转 Tester 逐笔对齐 → 暂不可行（台账时间戳体系，2026-09-06 实测）

> 状态：**暂不可行（已记录）**｜ 处置：台账列改造**另立项**（EA 代码改动，见 黄金\乖离反转策略\说明文档\乖离反转EA台账列改造立项_20260906.md）

### 结论
- BiasReversal_Combo_EA Tester 台账与 v9 EA 等价基线（bias_reversal_v9_ea_baseline_long.csv，6H门+H1金叉做多）按逐笔时间戳配对：**matched = 0/250**。
- 归因：**两套时间戳体系不同（非逻辑错）**——对账脚本 compare_bias_reversal_v9.py 按 signal_time 精确到秒相等配对，EA 侧时间列根本不落在参考的时间轴上。

### 根因（BiasReversal_Combo_EA.mq5 源码实证）
- 台账列（OnInit 表头）：side, signal_time, dir, entry, stop, exit_time, exit_price, reason, pnl_points——**无独立 bar 时间戳、无 entry_time**；
- LedgerAppend 中 signal_time 与 exit_time **都取 TimeToString(TimeCurrent())**（平仓瞬间时钟，两列同值），并非信号 bar / 入场 bar / 出场 bar 的时间；
- 台账**仅在真实平仓时写一行**（CloseByMagic 成功分支）；InpSimMode=true 只打印 [SIG]（OpenPosition 直接 return false）→ SimMode 下永远没有台账行，Tester 记账被迫以真实单跑，时间戳进一步落在 tick/撮合时刻；
- 对照侧 v9 基线 signal_time/exit_time 是 **H1 bar 时间戳**（2015-05 ~ 2026-08 全窗 351 行）→ 时间轴错位导致 0/250。

### 处置
- 结论已同步至 策略审查状态_20260906.md（乖离反转行 ⚠️ + 轮次行 §二十）；
- **要真对齐：需先改造 EA 台账列**——像 ABC 三套（30m2H/1H_M30_4H/2H_M30_6H）那样把 signal/entry/exit 都写成 bar 时间戳并支持 SimMode 记账，然后重跑 Tester 对齐（复用 A1/A2 流水线）——EA 代码改动，**另立项**；
- 多头研究线定论不受影响：v9 基线 PF 2.185（§十六 深化 E）；本次对账不产生逻辑/BUG 归因。
- **闭环（2026-09-06 三期）**：台账列 bar 时间戳化 + v9 口径回放引擎落地后 Tester 全窗回放 **matched=351/351=100.0%**（missing/wrong/extra=0）——0/250 问题闭环，详见 data/validation/乖离反转Tester对齐报告_100pct_20260906.md（状态总表已同步 ✅）。
- **2026-09-06（二期）
**：v9 口径逐字段对表完成（见说明文档）；EA 台账列改造+v9 口径回放已落地；下一步=Tester 全窗回放跑批+对账。

---

## 二十一、2H V4 窗口收敛验证与 EA 起始年份敏感性（2026-09-06）

- **2025-01-01 起跑**：EA(≥2025) 314行/108笔，matched **311/351=88.6%**，missing 40（含 stage2/3 整笔缺 21），extra **3**（窗口正确）。
- **2024-01-01 起跑**（预热一整年）：EA(≥2025) 相同 314/108，matched 仍 311/351=88.6% → 非预热问题。
- **2020-01-01 起跑**（前次）：同段 matched **341/351=97.15%**、missing 10（全 stage1）。
- **结论**：EA 在 2025-26 段的输出**依赖测试起始年份**（2020 vs 2024/2025 起跑结果不同）→ 疑似 EA 内存在**从测试起始累计的全历史状态**（非纯预热）。记为新排查项：**2H EA 起始年份敏感性**。
  - 影响：V4 对齐的"真值"依赖起跑口径；extra=3 证实此前 49=窗口伪影。
  - 建议：跑 V4 对齐统一用 2020-01-01 起跑（matched 97.15%），expected 同步扩窗；或排查 EA 全历史状态使其窗口无关（后续专项）。
- **2026-09-06 受控实验定案**：2020-01-01 起跑（同一 V4 ex5 + V4 expected）-> matched 仍 311/351=88.6%（与 2024/2025 起跑一致）
  => **'起始年份敏感'为伪象**：此前 97.15% 系 16:39 轮 Tester 仍用旧 V0 ex5(缓存) 对 V4 expected 所致。**V4 真实对齐 = 88.6%**，EA 窗口无关。
- **新开排查项（V4 missing 40, TODO-8）**：V4-EA vs V4-expected missing 40（stage1 19 / stage2 10 / stage3 11；cross 15 / pre_cross 8 / post_n 17；含整笔缺 ~13 笔）
  => 疑为带通 [3,5] 的 EA/Python H6 bar 边界差异或 band 时序差一根 H6；专项再查。


---

## 二十二、2H V4 带通对齐 与 难论买卖点立项（2026-09-06）

### ① 2H V4 (r13_55 带通 [3,5]) 落地 + extra 根因
- ①②③完成：combined_abc 2H gate_mask 方向性带通 + attach_r13_band；EA Pass6HGate 加 InpH6SMA13 / InpR13BandMinPct=3.0 / InpR13BandMaxPct=5.0（编译0 errors）；expected 重生成 117 笔(2025-26)。
- ④ Tester对齐：matched 341/351 = 97.15%；missing 10 全 stage1（结构性）。
- 49 笔 extra 根因=窗口不匹配（非带通bug）：Tester 起 2020-01-01 而 expected 仅 2025-26 → 48 笔 pre-2025 窗口外(2020:10/2022:8/2023:2/2024:28)，仅 1 笔真 2025 边界；49/49 python r13 在带内、python 无同 signal → 带通正确。修正：Tester 起始改 2025-01-01 重跑可收敛。

### ② 难论第一/第二买卖点研究（TODO-7，⏳ 待办）
- 现状：2H_M30_6H 入场层仅覆盖第三买卖点(cross/post_n 追势)与准第二(pre_cross)。
- 立项：研究难论第一买卖点(结构反转起点/背驰转折)识别→规则草案→在 30m2H/2H/1H 回测新买点 PF/样本量→定是否接入。与 V4 落地互不冲突。**：v9 口径逐字段对表完成（说明文档\乖离反转EA台账v9口径对表_20260906.md）；EA 台账列改造 + v9 口径回放引擎已落地（编译 0 errors，.ex5 已部署，.set/compare 脚本已同步）；下一步=Tester 全窗(2015.01.01–2026.08.21)回放跑批 + 对账。

---

## 二十三、DEMO 实挂下单盘面核实（2026-09-09 01:15 只读取证）

> 触发：用户质疑 00_README §1「全链路只读监控不下单」——「账户是模拟账户，就是要大胆下单，找到问题」。
> 方法：MetaTrader5 Python API 只读查询（account_info / positions_get / history_deals_get）+ 终端 Experts 日志原文 + `.mq5`/`.set` 源码同层读回 + position_id 血缘还原。**全程未下单、未改任何终端设置、未动 EA**。
> 取证脚本（9 个，均带 `_20260909` 后缀；R4 已于 2026-09-09 02:34 归档至 **`scripts\_archive\2026-09\`**，归档清单 `00_文档中心\归档\整理清单\tmp_scripts_archive_20260909.csv`）：`_tmp_live_trading_probe`（账户权限/持仓/成交/EA 日志总览）、`_tmp_naked_exp_probe`（机检 13 条违规逐目录核实）、`_tmp_deal_forensics`（magic=0/372037/302xxx 明细）、`_tmp_pos_lineage`（position_id 开平血缘还原）、`_tmp_log_evidence`（台账可读性体检）、`_tmp_chart_params_probe`（chart*.chr 实参 + MCT 数据源根因）、`_tmp_deal_reason_probe`（DEAL_REASON 可靠性复核）、`_tmp_export_chr_sets`（.set 反向导出工具）、`_tmp_commit_msg`（commit 8db7467 的 message 原件）。
> 编号说明：本节取 **§二十三**。原文末存在两个「§二十」（乖离反转逐笔对齐 / 2H V4 带通对齐），按 `策略审查状态.md`「文档卫生提醒」的既有建议，已把后者重编号为 §二十二，本节顺延为 §二十三 → 编号歧义消除。重编号只改标题文字、不增删行，故 `策略审查状态.md` 对本文件的 **L462-487 行号引用位置未变**。

### ① 结论：README §1「全链路只读监控不下单」**不成立**

账户 277752085（Exness-MT5Trial5，trade_mode=0 demo）实测：`trade_allowed=True`、**`trade_expert=True`**（终端自动交易总闸开启）；窗口 2025-01-01~2026-09-09 共 **121 笔成交、59 个持仓**；当前持仓 0；balance=equity=**2667.61 USD**。

Experts 日志（20260908.log 00:21:32 批次）自报的**终端实参**：

| EA（图表） | 终端实参 | 工作区 `.set` 写的 | 真下单 |
|---|---|---|---|
| `30m2H_Strategy_EA (XAUUSDm,M30)` | **`Mode: LIVE TRADING`** | `SimDeployment_20260811.set`: SimMode=true/AllowReal=false | ✅ |
| `2H_M30_6H_ABC_EA (M30)` | SimMode=**false** AllowReal=**true** Magic=342036 | `ABC_SimDeployment.set`: true/false | ✅ |
| `1H_M30_4H_ABC_EA (H4)` | SimMode=**false** AllowReal=**true** Magic=312036 | `ABC_EA.set`: true/false | ✅ |
| `30m2H_ABC_EA (M30)` 实例1 | SimMode=false AllowReal=true Magic=352036 | — | ✅ |
| `30m2H_ABC_EA (M30)` 实例2 | SimMode=**true** AllowReal=false Magic=**352036** | `ABC_EA.set`: true/false | ❌ 虚拟 |
| `BiasReversal_Combo_EA (H1)` | SimMode=false AllowReal=true Long=372036 Short=372037 LongMode=0 | `BiasReversal_Combo_EA.set`: true/false | ✅ |
| `1H_M30_4H_CurrentCandidate_EA (M30)` | SimMode=**false** AllowReal=**true** Magic=312026（**chart02.chr 实测**；其 init 打印不含 AllowReal，故日志层无法确证） | `CurrentCandidate.set`: true/false | ✅ |
| `Gold_DataEvent_EA (H4)` | SimMode=true AllowReal=false Magic=411101 | 同 | ❌ |
| `USOIL2H / USOIL4H门 / Oil_DataEvent` | 09-07 20:26 时 SimMode=false AllowReal=true；09-08 起日志无 → 已摘除（导出文件 28.7h 未更新） | — | 摘除前 ✅ |

**即：9 个挂载实例中 6 个在 DEMO 真实下单**（chart02 CurrentCandidate / chart03 1H ABC / chart04 2H ABC / chart06 主线 Strategy_EA / chart07 30m2H ABC live / chart09 乖离反转），不是 README 说的"唯一例外 1 个"。

> **本节 01:40 更正**：初版据 Experts 日志写"8 个实例中 5 个真下单"，因 CurrentCandidate 的 init 打印不含 `AllowRealTrading` 而列为未确证。补充取证读 `MQL5\Profiles\Charts\Default\chart*.chr`（终端 profile，可读出 bool 型 input）后确证其为 SimMode=false + AllowReal=true → 实例数应为 **9**（终端日志 20260908.log 00:21:30-31 共 **9 条 `loaded successfully`**，其中 `30m2H_ABC_EA (XAUUSDm,M30)` 出现两次），真下单数为 **6**。详见 ⑭。

**配置漂移（P0）**：全部 `*SimDeployment*.set` 都写 SimMode=true/AllowReal=false，而终端实参全是 false/true → **工作区 `.set` 不是真相源**，R1④「动 EA 需 `.set` 同步」形同虚设，任何人按 `.set` 重挂都会静默改变交易行为，且事故无法复现当时参数。

### ② magic → EA 映射（权威表；此前无任何文档登记）

| magic | 归属 | 依据 |
|---|---|---|
| 302036 / 302037 / 302038 / 302039 | `30m2H_Strategy_EA` base / stage1 / stage2 / stage3 | `.mq5` L37 `InpMagic=302036`（注释"对齐实盘 .chr, stage 加 1/2/3"）+ L138-140 `MAGIC_STAGE1/2/3`；deal comment `30m2H_v3_S1/S2/S3` |
| 302026 / 302027 / 302028 | 同上，**旧 base 302025** + stage1/2/3（2026-06 期间） | deal comment 同为 `30m2H_v3_S1/S2/S3`；`参考实现工程\auto_trade\auto_trader.py` L46 `"magic_number": 302025`、`monitor_magic_302025.py` / `scan_magic_302025.py` 均指向 302025 |
| 312036 | `1H_M30_4H_ABC_EA` | 日志明示 Magic=312036 |
| 312026 | `1H_M30_4H_CurrentCandidate_EA` | `.mq5` L12 `InpMagic=312026` |
| 312025 (+1/+2/+3) | `1H_M30_4H_Strategy_EA` base/stage | `.mq5` L34/L140-142 |
| 342036 | `2H_M30_6H_ABC_EA` | 日志明示 |
| 352036 | `30m2H_ABC_EA` | 日志明示（两个实例共用） |
| 362036 / 362137 | `USOIL2H_CrossConfirm_EA` / `USOIL4H_Gate_On2H_EA` | 09-06/09-07 日志明示；窗口内**无成交** |
| 372036 / 372037 | `BiasReversal_Combo_EA` Long / Short | `.mq5` L19-20 + 日志明示 |
| 411101 / 411102 / 411103 | `Gold_DataEvent_EA` / `Oil_DataEvent_EA` / `MCT_EA` | `.mq5` InpMagic；窗口内**无成交** |

**潜伏冲突（P1）**：`1H_M30_4H_CurrentCandidate_EA` 的 `InpMagic=312026` 与 `1H_M30_4H_Strategy_EA` 的 stage1 magic（312025+1=**312026**）**完全相同**。两 EA 目前未同时挂载，故未爆发；一旦同挂即互相认领/平掉对方持仓。

### ③ 账户余额构成 与 **归因失真**（核心发现）

**余额构成**：121 笔中 3 笔为账务性（vol=0）——2026-06-20 `+500.00`（comment `D-trial-USD-24f9f402d3c047`）、2026-07-02 `+1589.46`（`D-trial-USD-fdf3b68a07ca48`）、2026-07-18 `Archived deals` 0.00。
=> **入金合计 2089.46，交易净盈利 = 2667.61 − 2089.46 = +578.15 USD**（账户是净盈利的，不是亏损）。

**按 magic 直接统计（= 监控/报表现在的做法）vs 按 position_id 血缘还原**：

| 开仓 magic | 持仓数 | 同magic平 | 异magic平 | 按magic统计 | 血缘还原后真实 |
|---|---:|---:|---:|---:|---:|
| 342036（2H ABC） | 18 | 15 | 3 | **−279.62** | **+68.17** |
| 312036（1H ABC） | 12 | 11 | 1 | **−68.82** | **+161.76** |
| 302039（主线 S3） | 4 | **0** | **4** | **0.00** | **+177.87** |
| 302037（主线 S1） | 5 | 2 | 3 | +31.45 | +135.78 |
| 302038（主线 S2） | 4 | 2 | 2 | +69.12 | +113.58 |
| 302026（旧 S1） | 3 | 1 | 2 | −20.50 | +27.54 |
| 302027 / 302028（旧 S2/S3） | 3 / 3 | 3 / 3 | 0 / 0 | −56.81 / −60.19 | −56.81 / −60.19 |
| 352036（30m2H ABC） | 3 | 3 | 0 | −56.09 | −56.09 |
| 372036（乖离多头） | 2 | 2 | 0 | −55.13 | −55.13 |
| 372037（乖离空头） | 2 | 2 | 0 | +121.67 | +121.67 |
| 合计 | 59 | 44 | **15** | — | **+578.15** ✅ 与余额构成吻合 |

**15/59（25%）持仓开平 magic 不一致**。错配的平仓方两个来源：
- **magic=0 共 13 笔**（合计 +864.45）：EA 用未设 magic 的 CTrade 主动平仓；
- **magic=302036（base）共 2 笔**（+88.62）：主线 09-01 修复后，平仓恒用 base magic 而开仓是 stage magic。

=> **三个策略被误判为亏损或零收益**：2H ABC 实际 +68.17（报表 −279.62）、1H ABC 实际 +161.76（报表 −68.82）、主线 S3 实际 +177.87（报表 0.00）。主线三段合计真实 **+337.77**（6 月旧 magic −89.46 + 8~9 月 +427.23）。

### ④ 根因：`CTrade` 未设 ExpertMagicNumber，**修复覆盖不全（P0）**

关键判据（实证）：**人工手动平仓会保留持仓 magic**——372037 那笔人工平仓 comment=`manual close (TP overdue)`，deal magic 仍是 372037；只有 EA 经 `CTrade::PositionClose()` 且该 CTrade 未 `SetExpertMagicNumber()` 时，deal magic 才落为 0。故 13 笔 magic=0 **全部是 EA 所为，非人工**。

> ⚠️ **本段"全部是 EA 所为，非人工"的断言已于 01:50 被证伪并更正**：用 `deal.reason` 复核，13 笔中 **12 笔 reason=SL、1 笔 reason=CLIENT，EXPERT 为 0 笔**；且 `DEAL_REASON` 字段本身不可靠（两笔铁证反例）。更正后的判定与方法论见文末「**补充取证二（01:50）**」。**结论方向不变**（magic=0 源于 EA 主动退出路径 + 源码 BUGFIX 注释自证），但"非人工"这一绝对化表述作废，且 F1 实现**不得依赖 `deal.reason`**。

| EA | CTrade | 开仓 magic | `SetExpertMagicNumber` | 状态 |
|---|---|---|---|---|
| `30m2H_Strategy_EA`（主线 v3.37） | L117 | `req.magic = this_magic`（stage，L2180/2224） | ✅ L1120，`BUGFIX(2026-09-01)` | **修而不彻**（见 ⑤） |
| `1H_M30_4H_ABC_EA` | L78 | `req.magic = InpMagic`（L838） | ✅ L723，`BUGFIX(2026-09-02)`，注释自述移植自"30m2H_Strategy_EA v3.35 Fix 3" | 单 magic，**修复完整** |
| `2H_M30_6H_ABC_EA` | L78 | `req.magic = InpMagic`（L899） | ❌ **无** | **未修**——342036 的 3 笔 magic=0 平仓（08-21 21:46 +71.39 / 08-24 22:14 +150.67 / 08-25 19:53 +125.73）即此 BUG 产物 |
| `30m2H_ABC_EA` | L74 | `req.magic = InpMagic`（L842） | ❌ **无** | **潜伏**——352036 的 3 笔平仓全是 SL 触发（服务器端保留 magic），EA 主动平仓路径尚未走过，一旦触发即产生 magic=0 |

主线源码注释自证（L1116-1119）：`CTrade had no expert magic set, so PositionClose()/OrderSend() sent magic=0 while opens set it explicitly -> close deals were unattributable (observed 2026-09-01 S2/S3 close deals magic=0)` —— 与本次独立取证到的 pos=2426656507(S2)/2426656630(S3) 于 09-01 19:30:01 被 magic=0 平掉（各 −0.40，持仓 0.0 分钟）**完全对应**。

`ClosePos(ulong ticket)`（L2329-2341）仅按 ticket 平仓、ticket 来自自身 stage 状态 → **不会跨策略误平**，故 342036/312036 的 magic=0 平仓不是主线 EA 造成，而是各自 EA 的同款 BUG。

### ⑤ 主线修复不彻底：平仓恒为 base magic

`g_trade.SetExpertMagicNumber(InpMagic)` 设的是 **base 302036**，而 `CTrade` 的 magic 是单一全局值，无法随 stage 变；开仓走 `req.magic = this_magic`（stage）显式覆盖，平仓走 `g_trade.PositionClose(ticket)` 只能用 base。
=> 修复后错配从「magic=0（完全无法归因）」变成「magic=302036（归到 base，stage 维度丢失）」，**stage 级盈亏依然无法从 deal 直接归因**。实证：pos=2429428242 由 302039(S3) 于 09-02 00:30 开、09-02 20:30 被 302036 平（+14.12）。
=> 若要 stage 级归因，须改为按 ticket 构造 request 并显式写 `req.magic = InpMagic + stage`（EA 代码改动，**待拍板**）。

### ⑥ 台账链路四处断裂 —— **这才是"只读监控"误判的技术根源**

| 台账文件 | 实测 | 问题 |
|---|---|---|
| `1H_M30_4H_sim_deployment_trade_ledger.csv` | 356B / 09-08 00:21 | **PermissionError [Errno 13]** |
| `bias_reversal_combo_trade_ledger.csv` | 287B / 09-08 16:00 | **PermissionError** |
| `2H_M30_6H_abc_trade_ledger.csv` | 123B / **08-25 22:37（338.6h 前）** | **PermissionError**，且停滞 14 天（342036 末笔成交 08-26 08:40 未入账） |
| `1H_M30_4H_abc_trade_ledger.csv` | 437B / 09-02 20:30 | **PermissionError** |
| `30m2H_abc_trade_ledger.csv` | **1 行仅表头** / 09-08 00:21 | 352036 有 6 笔成交（08-26）却台账全空；mtime = EA 重新 init 时刻 → 见 ⑧ |
| `MCT_trade_ledger.csv` | **1 行仅表头** / 09-06 14:40 | 恰为 README 记载的"MCT 阶段5 挂载时刻"，挂载后台账从未写入 |
| `30m2H_strategy_deal_history.csv` | **1 行仅表头** / 09-07 20:26 | 表头含 `magic,stage` 列（设计上就是为归因准备的），但 20+ 笔成交无一入表 |
| `30m2H_strategy_trade_ledger.csv` | 8 行 / 09-02 20:30 | **唯一有数据的台账**；09-02 后主线无成交，故未更新属正常（非断裂） |

**MQL5 写文件未加 `FILE_SHARE_READ` → Python 侧（含 `monitor_cycle.py` / `monitor_all_strategies.py`）读不到 4 个台账**。监控拿不到成交数据，报表自然"看不到下单"，README §1 的"只读监控不下单"很可能就是这样被写下来的。**监控是否对这些读取失败静默吞掉（try/except pass）尚未核实**（见 ⑫）。

### ⑦ MCT 观察期实际**空转**（数据源滞后 9 天）

`MCT_diag.csv` 4655 行 / **1.2h 前仍在更新**（=> EA 进程活着并在写诊断），但末尾连续全是：
```
2026.09.08 04:00,entry_skip_not_last,0,0.00000,last=2026.08.30 20:00 expect=2026.09.08 00:00
2026.09.08 08:00,entry_skip_not_last,0,0.00000,last=2026.08.30 20:00 expect=2026.09.08 04:00
2026.09.08 12:00,entry_skip_not_last,0,0.00000,last=2026.08.30 20:00 expect=2026.09.08 08:00
2026.09.08 16:00,entry_skip_not_last,0,0.00000,last=2026.08.30 20:00 expect=2026.09.08 12:00
```
EA 认为最后一根 bar 停在 **2026.08.30 20:00**，而期望值是当下 → 每根新 bar 都判 `entry_skip_not_last` 直接跳过入场。
=> **README §6 T4「MCT 观察期自 09-06，等首笔信号」的前提不成立：照此状态永远等不到首笔信号**。
=> **根因更正（01:40 补充取证）**：初判"数据源滞后 9 天"**不准确**。实测 `mt5.copy_rates_from_pos` 显示品种数据是**新鲜**的——USOILm H4 最后 bar = **2026-09-08 16:00**（服务器时间，close=90.108）、USOILm D1 = 2026-09-08 00:00、XAUUSDm H4 = 2026-09-08 16:00。而 EA 自认 `last=2026.08.30 20:00`（20:00 属 H4 bar 时刻）→ **问题在 EA 侧的 H4 序列读取/缓存，不是行情源停了**。MCT_EA 确挂本终端 chart12（`USOILm,H4`，09-08 00:21:31.059 `loaded successfully`），SimMode=true/AllowReal=false/Magic=411103，故不下单；其 OnInit 无 Print 输出（所以 Experts 日志查不到），只写 diag CSV。
=> 处置：修 EA 的 bar 序列获取（`CopyRates` 失败未告警/缓存未推进），修好后观察期起点重算。

### ⑧ `30m2H_ABC_EA` 双挂同 magic 的后果（已实证）

同一毫秒级批次（09-08 00:21:32.291）两条 init：一条 SimMode=false/AllowReal=true、一条 SimMode=true/AllowReal=false，**magic 同为 352036**，同品种同周期 (XAUUSDm,M30)。09-07 22:25 批次同样成对出现（间隔 125ms）。
**参数级铁证（01:40 补充）**：两张图表文件分别是 **`chart07.chr`（InpSimMode=false, InpAllowRealTrading=true, InpMagic=352036）** 与 **`chart14.chr`（InpSimMode=true, InpAllowRealTrading=false, InpMagic=352036）**，同品种 XAUUSDm；终端日志 09-08 00:21:30.935 与 00:21:31.093 各有一条 `expert 30m2H_ABC_EA (XAUUSDm,M30) loaded successfully`。
两实例共写一份 `30m2H_abc_trade_ledger.csv`，该文件在 09-08 00:21（= 重新 init 时刻）被重置为**仅表头** → **sim 实例把 live 实例的台账清空**。352036 的 6 笔真实成交（08-26 04:30~08:25，三笔 SL 全在 4644.186）因此在台账中完全消失。
另：`30m2H_abc_signals_export.csv` 也只有一份，两实例互相覆盖。

### ⑨ 人工干预一笔已确证，且**未留档**

372037（乖离反转空头）血缘：
- 08-22 00:34 SELL IN @4615.216 → 08-24 19:38 BUY OUT @4670.599 **−55.38**，comment `[sl 4670.599]`（EA 的 SL 路径）
- 08-24 20:00 SELL IN @4657.753 → **08-29 00:49 BUY OUT @4480.699 +177.05，comment `manual close (TP overdue)`**

`manual close (TP overdue)` **不是任何 EA 源码里的字符串**（全树 `.mq5` grep 仅两处注释含 "manual close"，为 `SL hit / manual close`）→ 系人工在终端平仓并手填备注。
两层含义：①这次人工干预在 `00_文档中心\` 全目录 grep `manual close|TP overdue` **命中 0 条**，即**未留档**（违 R1③ / A2.11）；②该空单浮盈 177 点期间 EA 的 TP/退出逻辑始终未触发，需人工以"TP 超时"兜底 → **EA 退出逻辑缺陷线索**（与 §十六 L141「EA 做空冷却把服务器 TP 平仓误计为 SL hit」相关但不同）。
门禁判定：两笔开空在 08-22 / 08-24，而"做空停用"结论见于 `数据测试对比报告_20260904.md`（09-04「做空维持停用」）与 S2-4（09-07 核实 `InpEnableShort=false`）→ **时序一致，非门禁失效**，属门禁关闭前的历史成交。

### ⑩ 时区口径（归因比对必须对齐）

台账/EA 日志用**服务器时间**，`mt5.history_deals_get()` 的 `deal.time` 经 `fromtimestamp` 转**本地时间**后 **+8 小时**。实证：ledger `open_time=2026.09.01 11:30:00` ↔ deal `2026-09-01 19:30:00`（pos=2426656507 同一笔）。本节所有成交时间均为**本地时间**。做 EA↔Python 逐笔对齐（T1）时若不统一，会系统性错配 8 小时。

### ⑪ 其他

- `Gold_DataEvent_EA (XAUUSDm,H4)` 打印 `"2H_M30_6H ABC EA initialized. ... Magic=411101"`；`Oil_DataEvent_EA (USOILm,H4)` 打印 `"USOIL4H_Gate_On2H EA initialized. ... Magic=411102"` → 两个 EA 的 init 日志文本是**复制粘贴错误**（P2，必现，误导排查）。
- 两套账务并行：账户真实 balance 2667.61 USD vs 各 EA `InpSimStartBalance=500.0` 虚拟余额；监控口径未说明以哪套为准。
- Python 侧存在独立下单能力：`参考实现工程\auto_trade\auto_trader.py`（L358/L401 两处 `mt5.order_send`，magic=302025）。窗口内**无 302025 成交** → 未参与本期交易，但该文件位于冻结区且具备实盘下单能力，无任何文档登记其启用状态。

### ⑫ 未确证项（诚实登记，不做推断结论）

1. ~~`1H_M30_4H_CurrentCandidate_EA` 的 `InpAllowRealTrading` 终端实参~~ → ✅ **已确证（01:40）**：`chart02.chr` 实测 `InpSimMode=false` + `InpAllowRealTrading=true` + `InpMagic=312026`，即**该 EA 在真下单**。（日志层无法确证的原因：其 init 打印 L815 不含 AllowReal 字段，且 9 天内无 `Order skipped` 行、窗口内无 312026 成交——后者说明它虽已放开下单闸但尚未产生成交。）
2. ~~`MCT_EA` 的挂载位置~~ → ✅ **已确证（01:40）**：挂本终端 `chart12.chr`（USOILm,H4），终端日志 09-08 00:21:31.059 `expert MCT_EA (USOILm,H4) loaded successfully`；Experts 日志查不到是因为其 OnInit 无 Print，只写 `MCT_diag.csv`。根因见 ⑦ 更正。
3. `monitor_cycle.py` / `monitor_all_strategies.py` 的异常处理 → **部分确证（01:40 已读源码）**：`monitor_cycle.py` **不读台账**（只读 `dashboard.csv`、事件日历、终端 `logs\` 的 `loaded successfully` 行），`run_monitor()` 失败会打印降级、`ea_load_state()` 对单个日志 `except: continue`；`monitor_all_strategies.py` 的 MCT/DataEvent 快照调用（L595/L602）用 `except Exception as _e` 且**把错误写进报告**（"快照生成失败"，非静默）；但 MT5 连接失败（L187/L191）`return None` 属**静默回退**到重放口径，报告不会声明实时读数不可用。**仍未核实**：两个 adapter（`monitor_mct_adapter.py` / `monitor_data_event_adapter.py`）内部读台账 CSV 的异常处理。
4. 342036 三笔 magic=0 平仓归因于 2H ABC 自身 BUG 属**高置信推断**（源码缺 `SetExpertMagicNumber` + 平仓时间与持仓时长吻合 EA 退出逻辑），但未在日志中定位到对应的 `Closed ticket=` 打印行做端到端闭环。

### ⑬ 建议动作（分级；**本节未执行任何改动**）

- **P0**：① `2H_M30_6H_ABC_EA` / `30m2H_ABC_EA` 补 `g_trade.SetExpertMagicNumber(InpMagic)`（照抄 1H ABC L723 的已验证修法）；② 拆 `30m2H_ABC_EA` 双挂（或给 sim 实例换独立 magic + 独立台账文件名）；③ 终端实参反向导出为权威 `.set` 入库，治配置漂移。
- **P1**：④ 主线 stage 级平仓 magic（⑤）；⑤ 台账 `FILE_SHARE_READ`（⑥），否则监控永远看不到成交；⑥ MCT 数据源滞后（⑦），观察期重算；⑦ `monitor_magic_302025.py` / `scan_magic_302025.py` 指向已废弃 magic，需重定向或归档。
- **P2**：⑧ 两个 DataEvent EA 的 init 日志文本；⑨ `CurrentCandidate` 的 312026 与 `1H_Strategy_EA` stage1 冲突；⑩ magic→EA 映射表（②）应固化为文档，供监控按 magic 拆账。
- **口径**：⑪ 归因必须用 position_id 血缘而非 deal magic（③）；⑫ 统一服务器/本地时间（⑩）。
- **文档**：⑬ README §1/§2/§4/§5 按本节实测口径纠偏；§6 登记 P0/P1；⑭ 08-29 人工干预补留档（⑨）。（⑬⑭ 已于 2026-09-09 01:20~01:40 执行完毕，⑨ 的人工干预留档即本节本身。）

---

### 补充取证（2026-09-09 01:40）：chart→EA 权威映射、监控配置缺口、`.chr` 可作 `.set` 真相源

> 取证脚本 `scripts\_archive\2026-09\_tmp_chart_params_probe_20260909.py`（只读解析 `.chr` + `copy_rates_from_pos`）。本节是 F/G 改造（监控血缘归因）的直接输入。

**1. 图表 → EA → 实参 全表**（`MQL5\Profiles\Charts\Default\chart*.chr`，22 个文件，mtime 全部 2026-09-08 21:29）

| chart | EA | 品种 | SimMode | AllowReal | Magic | 真下单 |
|---|---|---|---|---|---|---|
| chart02 | `1H_M30_4H_CurrentCandidate_EA` | XAUUSDm | false | **true** | 312026 | ✅ |
| chart03 | `1H_M30_4H_ABC_EA` | XAUUSDm | false | true | 312036 | ✅ |
| chart04 | `2H_M30_6H_ABC_EA` | XAUUSDm | false | true | 342036 | ✅ |
| chart06 | `30m2H_Strategy_EA` | XAUUSDm | false | true | 302036（StageCount=3） | ✅ |
| chart07 | `30m2H_ABC_EA` | XAUUSDm | false | true | 352036 | ✅ |
| chart09 | `BiasReversal_Combo_EA` | XAUUSDm | false | true | Long 372036／Short 372037（EnableShort=false、LongMode=0、LedgerReplay=false） | ✅ |
| chart10 | `Gold_DataEvent_EA` | XAUUSDm | true | false | 411101 | ❌ |
| chart12 | `MCT_EA` | USOILm | true | false | 411103 | ❌ |
| chart14 | `30m2H_ABC_EA` | XAUUSDm | true | false | **352036（与 chart07 撞号）** | ❌ |
| chart05 / 08 / 11 | 原油三 EA 已摘除，仅剩默认 MA 指标 | USOILm | — | — | — | — |
| chart01 / 13 / 15~22 | 无 EA（空闲图表） | — | — | — | — | — |

=> **9 个 EA 实例、6 个真下单**；`InpEnableShort=false`/`InpLongMode=0` 与 S2-4 登记的门禁状态一致（图表侧亦已确认）。

**2. `.chr` 可作 `.set` 的真相源（T12 可全自动收口）**：`.chr` 为 UTF-16 文本，`Inp*=value` 可直接解析，**bool 型也能读出**。=> 「终端实参反向导出为权威 `.set`」**无需人工在终端逐个导出**，可脚本化生成并入库，B 项成本大幅下降。

**3. `read_ea_input()` 三处缺陷（P1，确证）**：
- 硬编码 `chart_file="chart10.chr"`（L214），唯一调用点 L537 `read_ea_input(inp)` **不传 chart_file** → 恒读 chart10 = **Gold_DataEvent_EA**；而 `ea_inputs` 只给 **BiasReversal** 配了 `InpLongMode`（L157）→ **监控在 Gold_DataEvent 的图表里找乖离反转的参数**，`txt.find` 必然失败返回 None → `ea_mode` 恒空 → dashboard/报告「EA模式」列**从来没有内容**（L571 空则不输出）。
- 数值解析 `while txt[k].isdigit() or txt[k] in ".-+"`（L221-224）**只支持数字**，bool 型即使找对图表也读不出。
- chart 编号会随图表增删重排（chart05/08/11 已空）→ **硬编号本质不可靠**，应按 EA 名扫描全部 `.chr` 匹配。

**4. `STRATEGY_CONFIGS.magics` 归因缺口（P1，确证）**：
- **缺历史 magic**：302025/302026/302027/302028（6 月旧 base+stage）不在任何 `magics` 列表 → 6 月那 9 个持仓（合计 −137.50）监控**完全看不到**。
- **策略粒度过粗**：`30m2H` = [302036,302037,302038,302039,**352036**] → 主线 Strategy_EA 与 ABC_EA **合并成一行**；`1H_M30_4H` = [**312026**,312036] → CurrentCandidate 与 ABC 合并；`BiasReversal` = [372036,372037] → 多空合并。=> 现有 dashboard 无法区分同策略下不同 EA / 不同分支的盈亏。
- `read_real_positions()`（L179）**只读 `positions_get()`（当前持仓），不读 `history_deals_get()`** → 监控从设计上就没有"已实现盈亏"这个量，只有浮盈。这是"看不到下单结果"的第二个原因（第一个是台账 `PermissionError`）。

**5. 错误表述的源头定位**：`monitor_all_strategies.py` **L621** 仪表盘口径行末写死 **"只读监控不下单"** → `00_README.md` §1 的错话源于此，须随 F 改造一并纠正。

---

### 补充取证二（2026-09-09 01:50）：`DEAL_REASON` 不可靠、台账逐笔交叉验证、chart 编号漂移

> 取证脚本 `scripts\_archive\2026-09\_tmp_deal_reason_probe_20260909.py`。本节更正 ④ 的绝对化断言，并给出 F/G 实现必须遵守的方法论约束。

**1. `deal.reason` 复核结果（121 笔全量）**

| reason | magic==0 | magic≠0 |
|---|---:|---:|
| SL | **12** | 5 |
| TP | 0 | 38 |
| CLIENT | **1** | 0 |
| SO | 0 | 3 |
| **EXPERT** | **0** | **0** |

=> magic=0 的 13 笔 = 12 笔 `reason=SL` + 1 笔 `reason=CLIENT`，**`EXPERT` 全窗口 0 笔**。④ 中"13 笔全部是 EA 主动平仓、非人工"的断言**不成立**（至少 1 笔 CLIENT）。

**2. `DEAL_REASON` 字段本身不可靠（两笔铁证反例）**

| pos | comment | profit | 实际应是 | API `reason` |
|---|---|---:|---|---|
| 2361780329（乖离空头） | `[sl 4670.599]` | −55.38（空头亏损、平仓价=SL 价） | SL 触发 | **TP** ❌ |
| 2371628249（乖离空头） | `manual close (TP overdue)` | +177.05（人工填的备注） | 人工平仓 | **SL** ❌ |

=> **不能用 `deal.reason` 判定平仓类型**。可靠判据是三方交叉：`deal.comment` 前缀（`[sl `/`[tp `）+ EA 台账 `local_exit_reason`/`deal_reason` + 源码路径。

**3. `30m2H_strategy_trade_ledger.csv` 7 行 与 MT5 deals 逐笔交叉验证（`deal_ticket` 全部精确匹配）**

| # | position_id | deal_ticket | 台账 deal_reason | API reason | **API deal.magic** | 台账开仓 magic | profit | 判定 |
|---|---|---|---|---|---|---|---:|---|
| 1 | 2426656507 | 1914630620 | EXPERT | SL | **0** | 302038 (S2) | −0.40 | EA 主动退（`stage2_merged_cross`），**修复前** magic 落 0 |
| 2 | 2426656630 | 1914630720 | EXPERT | SL | **0** | 302039 (S3) | −0.40 | 同上（`stage3_cross_exit`） |
| 3 | 2426656204 | 1915365405 | SL | SL | 302037 | 302037 (S1) | −30.77 | 服务器 SL，magic 保留，comment `[sl 4350.743]` |
| 4 | 2428220419 | 1916921082 | **CLIENT** | **CLIENT** | **0** | 302037 (S1) | +0.09 | 人工/客户端平仓，**magic 亦落 0**（与 372037 案例行为相反） |
| 5 | 2429425201 | 1919718568 | TP | TP | 302037 | 302037 (S1) | +62.22 | 服务器 TP，comment `[tp 4290.570]` |
| 6 | 2429427586 | 1920865488 | SL | SL | 302038 | 302038 (S2) | +27.14 | 服务器 SL；该行含 stage2 trail **modify_count=182** 而 magic 保留 → **推翻"修改 SL 会致 magic=0"的假说** |
| 7 | 2429428242 | 1923116811 | EXPERT | — | **302036** | 302039 (S3) | +14.12 | EA 主动退，**BUGFIX(09-01) 之后** → 落 base magic（印证 ⑤） |

- 时区确证：台账 `exit_time=2026.09.01 11:30:01`（服务器）↔ API `2026-09-01 19:30:01`（本地）= **+8h**，与 ⑩ 一致。
- 台账第 3、4 行 `trigger_tag=restart_recover / signal_src=restart_recover` → EA 重启后**认领既有持仓**入台账，非本轮新开。

**更正后的 magic 落 0 判定**：
- **EA 主动平仓（`CTrade::PositionClose`，台账记 EXPERT）**：修复前 → `magic=0`（行1/2）；修复后 → `magic=base 302036`（行7）。**这是 magic=0 的主因，源码 BUGFIX 注释自证，方向不变。**
- **服务器 SL/TP 触发**（comment 带 `[sl`/`[tp`）→ magic **保留**持仓 magic（行3/5/6，以及全窗口 43 笔）。
- **人工/客户端平仓** → magic **可能保留**（372037 案例）也**可能落 0**（行4）→ 行为不一致，不可作为判据。
- 修改 SL（trail）**不改变** magic（行6，modify 182 次仍保留）。

**4. chart 编号漂移 → 三个部署脚本的图表假设全部失效（P1，比 T18 更危险）**

| 脚本 | 脚本内假设 | 实测（01:40） | 后果 |
|---|---|---|---|
| `大周期拐点\...\rebuild_and_attach_mct.py`（L7/L24-25） | chart12 = Oil_DataEvent_EA（模板）、chart13 = MCT_EA（目标） | chart12 = **MCT_EA**、chart13 = 无 EA(2122B) | 重跑会往空图表写 MCT 块，而真正的 MCT 在 chart12 不受控 |
| `大周期拐点\...\attach_mct_ea_to_chart.py`（L2/L4/L9） | chart13 = MCT_EA | 同上 | 同上 |
| `黄金\乖离反转策略\scripts\deploy_combo_ea.py`（L9-10） | TEMPLATE=chart09、**TARGET=chart10**，注释含"复制 ex5 + 生成 chart10.chr + **重启**" | chart09 = BiasReversal ✅、chart10 = **Gold_DataEvent_EA** | 重跑会把乖离 EA 块写进 chart10 → **顶掉 Gold_DataEvent**，并重启终端（违 AGENTS.md C 段"不得打断 EA"） |

=> 与 T18 同源（硬编码 chart 编号本质不可靠：chart05/08/11 已空、编号会重排），但**这三个脚本会改终端状态**，风险高于只读的监控。处置：改为按 EA 名扫描 `chart*.chr` 定位，执行前 dry-run 打印目标图表供人工确认；**在修好前不要重跑这三个脚本**。

> ✅ **已执行（2026-09-09 02:42）**：在 `scripts\live_attribution.py` 新增公共守卫 `chart_layout()` + `guard_deploy(script, target_ea, target_chart, template_chart, template_ea)`，三个部署脚本各植入前置调用（`deploy_combo_ea.py` 在 EXPERT 块定义后／`copy ex5` 前；两个 MCT 脚本在常量定义后）。守卫判据三条（均可核验、不写死编号）：①`target_ea` 已挂在别的 chart → 会双挂；②`target_chart` 当前挂着别的 EA → 会顶掉它；③`template_chart` 的实际 EA ≠ 脚本假设 → 编号已漂移。命中任一条即 `SystemExit`，需显式 `--force` 才放行；放行前一律先打印**实际 chart→EA 盘面**供人工核对。
> **验证（执行级 + 端到端）**：单元三场景——A（deploy 参数）中止报 2 项、B（`Oil_DataEvent_EA`/`chart11.chr`，已摘除且空图表）**放行不误拦**、C（rebuild 参数）中止报 2 项；三个脚本**本体实跑**均 exit 1 安全中止（`deploy_combo_ea.py` 输出中无 `ex5 copied`，证明未进入写入分支），且 `chart10.chr`(70,254B)/`chart13.chr`(2,122B)/`order.wnd`(548B) 的 mtime 仍为 **2026-09-08 21:29** = 未改任何终端文件。
> **附带修正**：守卫初版曾 `sys.stdout.reconfigure(encoding="utf-8")`，导致人工在 GBK 控制台跑时**中文提示全乱码**（`褰撳墠 chart鈫扙A`）——守卫输出是给人看的，故改为不动调用方编码、只把 GBK 无法表示的 `⚠` 换成 ASCII `!!`（`markdown_section` 写 UTF-8 报告文件，其 `⚠️` 保留）。


**5. EA init 批次实况（撤回"每 30 分钟周期重载"的推测）**

- 20260907.log：9 个批次 `20:24:05 / 20:24:06 / 20:26:02 / 20:26:10 / 20:43:34 / 22:25:04 / 22:54:04 / 23:24:04 / 23:24:48`，相邻间隔 `[0.0, 1.9, 0.1, 17.4, 101.5, **29.0, 30.0**, 0.7]` 分钟
- 20260908.log：2 个批次 `00:21:32 / 00:21:33`（距上批 57 分钟）；20260909.log：**无 init**
- => 仅 09-07 22:25→22:54→23:24 三批呈 29~30 分钟间隔（疑与当时 Tester UI 驱动/调试会话相关，`ea_alignment_logs\_scratch`），**并非持续周期重载**；09-08 00:21 之后至今 25h 稳定。
- `30m2H_abc_trade_ledger.csv` mtime = **09-08 00:21**（仅表头）与最后一批 init 时刻吻合 → **台账在 OnInit 时被清空，此后 352036 无新成交故未再写**；08-26 的 6 笔成交记录就此丢失（⑧ 的机制确证，无需"周期重载"假设）。

**6. 对 F/G 实现的方法论约束（据本节）**

1. F1 血缘归因**只用 `position_id` + 开仓 deal 的 magic**；**禁止使用 `deal.magic`（平仓侧）与 `deal.reason`**。
2. 平仓类型分类用 `deal.comment` 前缀（`[sl `/`[tp `）+ EA 台账 `local_exit_reason`，两者缺失时标 `UNKNOWN`，不猜。
3. G2 的比对应以 **EA 台账 `deal_ticket` ↔ MT5 `deal.ticket`** 为主键（已验证 7/7 精确匹配），而非时间戳（时间戳有 +8h 口径差）。
4. `signals_export.csv` 是**滚动窗口**（实测 `30m2H_strategy_signals_export.csv` 仅 47 行 ≈ 24h，全为 `decision=SKIP`）→ **不含历史**，无法用于历史信号↔成交比对；G 的历史比对只能走台账（仅主线 7 行可用，其余台账空表头或 `PermissionError`）。故 **G2 收窄为「主线 30m2H 台账 7 行 vs MT5 deals 的自洽性核对」**（本节第 3 条已完成），G1（Python 同期信号复比）需另行取数。

---

### F/G 落地记录（2026-09-09 02:34）：监控血缘归因上线、G2 结论、T12 收口

> 用户裁决：顺序 **2→1→3**；第3步选 **F+G**（G2 先做、G1 视结果再定）；dashboard **不拆行**、`.set` 反向导出**纳入**、改代码前**先建 git 基线**（commit `8db7467`）。

**1. F 已落地（改 `scripts\` 钉死库；全程未动任何 EA / 终端 / 参数）**

| 项 | 实现 | 验证（执行级） |
|---|---|---|
| 新建公共库 `scripts\live_attribution.py`（R3 已登记 00_README §4 + scripts\README.md） | `attribute_by_lineage()` 血缘归因、`read_chart_params()`（**只解析 `<expert>` 块**）、`trading_gate()` 双闸判定、`check_ledgers()` 台账体检、`markdown_section()` 报告节；`EA_MAGIC_MAP` 22 条（含历史 magic 302025~302028）+ `MAGIC_CONFLICTS` | 独立自校验 **10 项断言全 PASS**：realized=578.15 / deposits=2089.46 / balance=2667.61 / 错配 15 持仓 15 平仓 / 无未登记 magic / 9 实例 / 6 真下单 / 双挂识别 / EA 汇总==magic 明细==总盈亏 |
| F1+F3 监控增列 | `dashboard.csv` 新增 `realized_pnl_usd`、`live_positions`、`magic_mismatch`、`ea_gate`、`ledger_state` | 实跑 `--no-refresh` 全策略：1H=161.76、30m2H=281.68（主线 337.77 + ABC −56.09）、2H=68.17、乖离=66.54 → **合计 578.15** ✅ |
| F2 修 `read_ea_input()`（T18） | 签名改 `(input_name, ea_name)`，按 EA 名定位 chart（弃硬编码 `chart10.chr`），委托 `live_attribution` 解析 `<expert>` 块 → bool 可读 | **T18 修复生效**：`BiasReversal` 行 `ea_mode` 首次显示 `0-6H门+H1金叉`（此前恒空） |
| F4 报告同步 | `dashboard_report.md` 总览增列（下单闸/已实现USD/magic错配）+ 新增「真实成交归因」节；`monitor_cycle.py` 总览表同步增列 + 账户级合计行 + `ea_load_state()` 改报「8 个 EA / 9 次加载 ⚠30m2H_ABC_EA×2」 | **端到端**跑 `monitor_cycle.py`：完整刷新 OK，`监测报告_20260908_1814.md` 三列有值、账户级合计 **+$578.15** |
| F5 不静默 | 台账不可读 / 血缘归因降级 / 闸开无成交 / magic 错配 / 双挂 → 全部写进 `warnings` 列 | 9 行 warnings 均含具体台账文件名与原因 |
| F6 脚注纠偏 | `monitor_all_strategies.py` 报告口径行末 `只读监控不下单` → `本账户为 DEMO 且 9 个挂载实例中 6 个双闸放开＝真实下单，不是只读监控` | 读回 `dashboard_report.md` L4 已生效（README §1 错话的源头就此堵住） |
| F7 映射固化 | `EA_MAGIC_MAP` 落为代码常量（EA/策略/stage 或多空分支三级），`ea_names_for()` 供策略行反查 EA | 断言「无未登记 magic」PASS |

**过程中修掉的自身缺陷（两处，均已复验）**：①`read_chart_params` 初版整文件扫描 → 把图表指标（4 个 Custom Moving Average）的 `InpMAPeriod/InpMAShift/InpMAMethod` 混进 EA 实参（乖离源码 23 个 input vs 导出 26 个），且大体积 `chart07.chr` 的首个 `^name=` 是指标的 `name=Main` 导致整文件漏识别（双挂只报出一张图）→ 改为只解析 `<expert>` 块后，10 项断言全 PASS；②`per_ea` 初版用首个 magic 当整个 EA 的标签（主线被标成 "stage1(旧)/302026"）→ 改为 EA 汇总（branches/magics 聚合）+ magic 明细两层，并加"两层合计==总盈亏"断言。

**2. G2 结论（EA 台账自洽性）＝已完成，见「补充取证二」第 3 条**

台账 7 行与 MT5 deals 按 `deal_ticket` **7/7 精确匹配**、profit 逐笔一致、时间恒差 +8h。=> **EA 台账内容本身准确**；问题不在台账记录，而在 ①`magic` 落 0 / 落 base（EA 代码 BUG，属 A/D 项待拍板）②台账被 OnInit 清空或被独占句柄锁死（T13/T14，属 D 项）。
**G2 原设想不可行**已记录于「补充取证二 6.4」（`signals_export.csv` 仅 24h 滚动窗口，覆盖不到成交时段）。

**3. G1 建议（据 G2 结果给出）**：G2 证明"EA 自认账本可信"，但**EA 行为是否符合 Python 口径仍未验证**——这正是 T1 的核心。建议 **G1 并入 T1** 一起做，而非独立小任务：认证窗口 2020-03-06~2026-06-11 与 DEMO 实挂窗口 2026-06-23~09-02 **零重叠**，两条线各自独立；合流后才能形成「认证基线 ↔ Tester 重跑 ↔ DEMO 实挂」三口径对照，也才能回答 T1 的 13.7% 究竟是口径差还是 EA 缺陷。

**4. T12 已收口（`.set` 反向导出，R8 清单 `chr_set_export_20260909.csv`）**：9 个终端实参快照已新增至各策略 `auto_trade\`，命名 `<EA>.chr_export_<chart>_20260909.set`，**未覆盖/删除/移动任何既有文件**；双挂的 `30m2H_ABC_EA` 导出两份（chart07 live / chart14 sim），头注释标注 magic 撞号。
- **更正一处保守声明**：初版头注释写"`.chr` 只记录被改动过的 input"——实测**错误**：`<expert><inputs>` 块记录的是**完整参数集**（乖离反转源码 23 个 `input` 声明 = 导出 23 个参数，逐个吻合）。头注释已改为完整快照声明 +「EA 升级新增 input 后需重新导出」。

**5. 本轮未做（等拍板，不擅自推进）**：**A/D 项**——`2H_M30_6H_ABC_EA` 与 `30m2H_ABC_EA` 补 `g_trade.SetExpertMagicNumber(InpMagic)`、各 EA 台账 `FileOpen` 加 `FILE_SHARE_READ`、主线 stage 级平仓 magic。理由：需重编译 + 重挂图表（会短暂打断 EA，AGENTS.md C 段禁止擅自打断），且 `auto_trade\` 属冻结区。**B/C/E 项**——拆双挂、MCT bar 序列修复、原油三 EA 恢复挂载，需用户在终端手工操作（我可出精确步骤清单与事后核验）。
> **后续（03:10）**：用户已定顺序「T20 → A/D → G1 并入 T1」，A/D **阶段一（改源码+编译，不打断 EA）已执行完毕**，见下节；阶段二（部署）待用户给打断窗口。

---

### A/D 阶段一落地记录（2026-09-09 09:13）：T11 与 T14 的代码修复已编译通过，**未部署**

> 用户裁决：**A3 暂缓**；D1 范围＝**全 9 个 EA**；部署方式＝**我复制 .ex5 + 用户逐图刷新**；借窗口**一并摘掉 chart14 双挂**（T13）。
> 本阶段全程**未启动/未重启/未打断终端**：`MetaEditor64.exe /compile` 是独立进程，产物 `.ex5` 落在工程内 `auto_trade\`，**未复制到终端 `MQL5\Experts\`** → 运行中的 EA 仍是旧版，零运行时影响。

**1. A1/A2（T11 P0）：两个 EA 补 `g_trade.SetExpertMagicNumber(InpMagic)`**

照抄工程内已验证范式 `1H_M30_4H_ABC_EA.mq5` L719-723（`BUGFIX(2026-09-02)`，其注释自述移植自 `30m2H_Strategy_EA v3.35 Fix 3`），插入位置同为 OnInit 内「状态清零 `ArrayResize(g_trades,0)` 之后、`CsvSignalsHeader()` 之前」：

| EA | 插入行 | 注释内写明的实测证据 |
|---|---|---|
| `2H_M30_6H_ABC_EA.mq5` | `ArrayResize(g_trades, 0);` 之后 | 3 笔平仓 deal magic 落 0（08-21 21:46 +71.39／08-24 22:14 +150.67／08-25 19:53 +125.73）→ 按 deal.magic 统计本策略 −279.62，血缘还原后 **+68.17** |
| `30m2H_ABC_EA.mq5` | 同上（该处原有两行空行，保留其一） | 属**潜伏未爆发**：352036 的 3 笔平仓（08-26 04:30~08:25）全由服务器 SL 触发（comment `[sl 4644.186]`，故 magic 保留），EA 主动平仓路径尚未走过；一旦触发即产生 magic=0 |

**2. A3（主线 stage 级平仓 magic）：按用户裁决暂缓**

理由（已获认同）：①F 项血缘归因已覆盖其后果——盈亏按开仓方 magic 归因，stage 级明细在 `per_magic` 表已分得开（302037/8/9 = +135.78/+113.58/+177.87）；②改 `ClosePos` 要么自构造平仓请求（需处理 price/deviation/filling，易引入新 bug）、要么在调用前后临时切 magic（需改全部调用点）；③主线 v3.37 正在 T1 验收中（3993→3364 行刚清理过），不宜再动。**收益低、风险高**。

**3. D1（T14）：9 个 EA / 26 处 `FileOpen` 补 `FILE_SHARE_READ`**

同样是照抄工程内既有范式——主线 `1H_M30_6H`/`2H_M30_6H_Strategy_EA.mq5` 的 5 处 FileOpen **本来就带** `FILE_SHARE_READ`，故其台账 `30m2H_strategy_trade_ledger.csv` 实测 `ok(7 行)` 可读；不带的那批正是 4 个 `PermissionError` 的来源，**因果逐一对应**。

| EA | 改的行 | 处数 |
|---|---|---:|
| `2H_M30_6H_ABC_EA` | L126（`CsvHandle`）／L142-143（`flags` 三元两分支）／L951（signals） | 4 |
| `30m2H_ABC_EA` | L121／L381（ledger）／L890（signals） | 3 |
| `1H_M30_4H_ABC_EA` | L122／L139-140（`flags` 三元两分支）／L892 | 4 |
| `1H_M30_4H_CurrentCandidate_EA` | L495（ledger；该文件是 **utf-8-sig** 带 BOM，写回时保持原编码） | 1 |
| `2H_M30_6H_CurrentCandidate_EA` | L146（**flags 顺序不同**：`FILE_CSV \| FILE_WRITE \| FILE_ANSI`） | 1 |
| `BiasReversal_Combo_EA` | L736-737（`open_flags` 三元两分支） | 2 |
| `MCT_EA` | L105／L110／L123／L139／L150（ledger + diag） | 5 |
| `Gold_DataEvent_EA` | L120／L136／L161／L408／L923（ledger + gate + signals） | 5 |
| `Oil_DataEvent_EA` | L99／L117／L141／L480／L792 | 5 |

改法：幂等脚本 `scripts\_archive\2026-09\_tmp_add_share_read_20260909.py`（R4，已归档）——**先 dry-run 逐行打印待改行供核对**（确认无注释误伤、`FILE_CSV | FILE_WRITE` 顺序变体被正确识别、已含 `FILE_SHARE_READ` 的行跳过），再 `--apply` 写回；按文件原编码（utf-8 / utf-8-sig）读写，只改 flags 表达式。合计 **26 行**（2H ABC 那 4 处已先行手改，故脚本对其报 0 行＝幂等生效）。

**4. 编译验证（判据不以 exit code 为准，A2.8）**

`MetaEditor64.exe /compile:<mq5> /log:<log>`，判据＝日志 `Result: N errors` 的 **N=0** 且 **`.ex5` mtime 比编译前新**（证明真产出了新二进制）。结果：**9/9 全部 0 errors，9 个 .ex5 全部刷新**（用时 387~586 ms/个）。

- **warnings 全部为既有、非本次引入**：`warning 39: expression not boolean`（2H ABC L944、30m2H ABC L875、1H ABC L877、Gold_DataEvent L869）与 `warning 43: long→double 类型转换`（BiasReversal L169/173/254/258、Oil_DataEvent L255）——**行号与本次改动行（FileOpen／OnInit 插入点）无一重合**。
- 编译日志按 R1④ 留在各 `auto_trade\`，文件名 `compile_<EA>_t11_t14_20260909.log`。**过程修正**：初版日志名不带 EA 名，导致 `2H_M30_6H策略\auto_trade\` 下 ABC 与 CurrentCandidate 互相覆盖（只剩最后一个）→ 改为带 EA 名后重跑，9 份日志齐全。
- 复验：`live_attribution.py` **10 项断言仍全 PASS**（578.15／15 错配／9 实例／6 真下单），台账仍 4 个 `PermissionError` —— 两者都符合"**未部署**"的预期（终端跑的还是旧 .ex5）。

**5. 阶段二（待用户给打断窗口，我不擅自执行）**

1. ~~备份终端 `MQL5\Experts\Advisors\` 下的 9 个旧 `.ex5`~~ → **更正（据第 7 条 T22）**：备份与投递目标必须按 `.chr` 的 `path=` 定位（主线 `30m2H_Strategy_EA` 与 `1H_M30_4H_CurrentCandidate_EA` 在 **`Experts\` 根**，其余 6 个在 `Experts\Advisors\`），备份命名 `<EA>.ex5.bak_pre_t11t14_20260909`；
2. 复制新 `.ex5` 到 **.chr 声明的路径**（**这一步不打断 EA**：已挂载实例继续跑内存中的旧版）——已实现为常驻公共脚本 **`scripts\deploy_ex5_by_chr.py`**（R3 已登记）：默认 dry-run、`--apply` 才写、投递前备份、投递后 **sha256 校验源==目标**、生成 R8 清单 CSV；目标路径取自 `live_attribution.chart_ea_paths()`（新增函数，解析 `.chr` 的 `path=`）而非硬编码目录 → 从根上防止 T22 那类"投到不被加载的副本"的假部署；源 `.ex5` 由工程内 `**/auto_trade/*.ex5` 扫描得到（跳过 backup/_archive/归档 目录）。
   **执行受阻（2026-09-09 09:30）**：该脚本对终端目录的写入被本会话的 auto mode 策略拦截（目标在工程目录之外），**连 dry-run 亦被拦**。按"不得绕过被拒动作"的原则，**未用任何其他途径投递**（未手工 copy、未换工具、未改脚本落到工程内）。待用户三选一：①明确授权我执行；②用户自行运行 `python scripts\deploy_ex5_by_chr.py`（看计划）→ `python scripts\deploy_ex5_by_chr.py --apply`（投递）；③用户在资源管理器手工备份+复制（我出逐文件清单）。
3. 由用户在终端逐图表刷新（右键→智能交易→刷新，或改一次参数确定）：chart03(1H ABC)、chart04(2H ABC)、chart07(30m2H ABC live)、chart02(1H CurrentCandidate)、chart09(乖离)、chart12(MCT)、chart10(Gold_DataEvent)——每个中断数秒、互不影响；
4. **借同一窗口摘掉 chart14**（30m2H_ABC 的 sim 实例，T13 根因：它每次 init 清空 `30m2H_abc_trade_ledger.csv`）；
5. 事后核验（我跑，执行级）：①`live_attribution.check_ledgers()` 的 4 个 `PermissionError` 应转为 `ok`/`header_only`；②`chart_layout()` 应只剩 8 个实例（chart14 消失）、双挂告警消失；③下一笔真实成交的 `deal.magic` 应等于开仓 magic（错配数不再增长）；④各 EA init 行的 `SimMode/AllowReal` 与 `.chr_export` 快照一致（确认刷新未误改参数）。
6. **回滚**：还原备份 `.ex5` + 再次刷新图表；`.mq5` 侧 `git checkout --`（R9：以 commit 代替 .bak）。

**7. 部署前置核对（09:20）发现 T22：终端两个 Experts 目录致 `.ex5` 双副本版本漂移，已造成一次"假部署"**

只读核对终端 `MQL5\Experts\`（根）与 `MQL5\Experts\Advisors\` 两处 `.ex5`，并与 `.chr` 的 `path=` 交叉比对：

| EA | 终端根 `Experts\` | 终端 `Advisors\` | 工程内 `auto_trade\` | chart 实际加载（`.chr` path） |
|---|---|---|---|---|
| `30m2H_Strategy_EA` | 09-07 22:36 / 150,280B | 09-08 20:23 / **150,354B** | 09-08 20:23 / **150,354B**（本轮未改，A3 暂缓） | **根目录（09-07 旧版）** |
| `1H_M30_4H_CurrentCandidate_EA` | 08-11 19:38 / 63,020B | 08-21 21:46 / 64,712B | 09-09 09:09 / 58,124B（本轮 D1 新编译） | **根目录** |
| 其余 6 个（1H ABC／2H ABC／30m2H ABC／乖离／Gold_DE／MCT） | — | 09-01~09-06 | 09-09 09:09~09:10 | `Advisors\` |

=> **00_README §7「2026-09-08 21:07 30m2H M15 孤儿句柄修复重编译部署」投递到了不被加载的 `Advisors\`**：工程内产物与 Advisors 副本的时间/字节数完全一致（09-08 20:23 / 150,354B），而 chart06 实际加载根目录的 09-07 22:36 / 150,280B → **该修复从未在实盘图表生效**。虽记载为"零行为回归"（故功能影响面小），但**主线正在 T1 验收中，实盘跑的版本与验收/编译版本不一致本身即问题**，需用户裁决是否补投（见 T22 与下方待决项）。
=> **对阶段二的硬约束**：部署必须按 `.chr` 的 `path=` 精确投递——`1H_M30_4H_CurrentCandidate_EA` 复制到 **`Experts\` 根**，其余 6 个复制到 **`Experts\Advisors\`**；投错目录＝假部署（更新了不被加载的副本，还会让人误以为已生效）。建议后续把"目标路径取自 `.chr` 的 `path=`"写进部署脚本并加校验。
=> **版本安全性已核**：终端在用的 `.ex5` 全部 ≤ 09-08，工程内 `.mq5` = 09-08 git 基线 + 本轮修复 → 部署**不会回退**任何终端侧独立改动。
=> 附：终端 `Advisors\` 内另有 4 个 MT5 自带示例（`ExpertMACD/MAMA/MAPSAR/MAPSARSizeOptimized`，06-21）、`ExportCalendar_EA.ex5`（08-26，宏观日历导出）、已摘除的 `USOIL2H_CrossConfirm_EA.ex5`（09-06 15:36）与 `USOIL4H_Gate_On2H_EA.ex5`（08-27）——均与本轮无关，不动。

### A/D 阶段二落地记录（2026-09-09 21:10，用户授权自动执行）

**动作链**：①用户指令"30m2H_ABC_EA 只保留新版、旧版删除、剔除出 MT5 + 自动执行"；②旧件清理——终端 `MQL5\Experts\Advisors\30m2H_ABC_EA.ex5.bak_pre_t11t14_20260909`（旧 ex5，SHA `218DA0AB…`）与陈旧 `30m2H_ABC_EA.mq5`（08-15 源码，SHA `0691D920…`）送回收站，目录内仅存新版 `30m2H_ABC_EA.ex5`（09-09 09:09 编译 60,648B，SHA `0B28E6FB…`，与工程 `auto_trade\` 一致）；③MT5 正常退出（WM_CLOSE，2s 退出，未强杀）→ `chart14.chr` 备份后摘除 `<expert>` 块（对象 178 个原样保留）→ 重启终端 21:10:30 载入。

**核验（执行级）**：`Experts` 日志 21:10:33~34 共 8 个 EA 各 `loaded successfully` ×1（chart02/03/04/06/07/09/10/12）；`30m2H_ABC_EA` 仅 1 次 init（SimMode=false/AllowReal=true/Magic=352036 = chart07）；`live_attribution.chart_layout()` = 8 实例、ABC 仅挂 chart07.chr（双挂告警消失）；`check_ledgers()` 无 `PermissionError`，`30m2H_abc_trade_ledger.csv` = `header_only(0 行)` 可写（chart07 init 重建表头，待新成交落行）；策略状态：主线 chart06 自此加载 09-08 M15 修复版（T22 收口）。

**副作用说明**：整端重启同时让其余已投递新版（chart02/03/04/09/10/12）一并生效，即 T11/T14 全量部署完成；账户侧 1 个持仓（BiasReversal 实开多单）由 EA 重启后 `RESTORED` 恢复跟踪。旧件与 chart14.chr 均可回收站/备份恢复；旧 ex5 另存于 `archive\backup_20260907\mt5_ea\`。

### T1 第一轮公平基线重跑（2026-09-09 21:2x）：ea_executable_diag=True + MAXPOS=3

**动作**：用 `_current_baseline.summarize_strategy(ea_executable_diag=True)` 重生成 Python 认证候选（152 信号），再用 `_build_expected_ledger.build_ledger(ea_executable=True, max_pos_sim=3)` 生成 EA 可执行 expected ledger（对齐 Tester ini `InpMaxPos=3`；152→接受 82，限定 Tester 窗口后 75）；与 EA v3.37 Tester ledger（67 信号）按 signal_anchor_time+dir 对齐。产物：`mainline_v337_tester_20260907\30m2H_python_ea_exec_cert_trade_detail.csv`、`30m2H_python_ea_exec_expected_trade_ledger_mp3.csv`、`30m2H_v337_对齐报告_ea_exec_maxpos3_20260909.md`。

**结果（窗口 2020-03-06~2026-06-11，Python expected 为分母）**：matched 49/75=65.3%（±0）→ 52/75=69.3%（±30）→ 55/75=73.3%（±60）→ **56/75=74.7%（±120/±180）**；missing 19、EA extra 11。missing 按 src：post_n5×4、post_n6×4、post_n3×3、pre_cross×3、cross×3、post_n4×1、post_n2×1；按年集中于 2025×8、2026×4。missing 对应 EA signals_export：14 笔同 bar `SKIP no_cross_m30_or_h2`、2 笔 EA `HOLD`（同 bar 模式/相位错位）、2 笔 `SKIP no_trigger`（方向门矛盾）、1 笔窗口尾无 EA 行；EA extra 11（post_n5×4、post_n6×4、post_n2/4/pre_cross 各 1）。

**判定**：口径公平化后对齐率由旧基线 13.7%（±60）升至 73.3%（±60）/74.7%（±120），**但距主线可验收水平（参照 ABC A1 94.2% / 1H 94.6% / 2H 96.5%）仍不足**；T1 保持开放。下一步=对 missing 19/extra 11 逐笔做 merged segment / post_n 计数 / bar 锚点差分，区分 EA 漏报误报与剩余时间轴差异后再定验收结论。

**逐笔差分结果（2026-09-09 21:3x，产物 `30m2H_v337_逐笔差分报告_20260909.md`）**：missing 19 → **EA 同 bar `SKIP no_cross_m30_or_h2` ×15**（Python 在 EA 判无穿越的 bar 上发 pre_cross/cross/post_n）、EA `HOLD` 模式错位 ×1、EA `SKIP no_trigger` ×1（Python L post_n2 vs EA h2_dir=BEAR）、同向 121-150min 相位差 ×1、窗口尾无 EA 行 ×1。EA extra 11 → **全部 `PY_NO_CANDIDATE`**（Python 完整 152 候选在 ±150min 内无同向信号）。**结论：剩余缺口不是 M15、不是 MAXPOS、也不是 30min 时间标签，主要收敛到 EA 的 merged/post_n 计数与 H2 方向判定两族差异（EA 少发 15 笔 + 多发 11 笔均属此族）**；下一步应对这 26 笔做 EA/Python merged 序列逐 bar 对账并定 EA 侧修复。

**merged 序列逐 bar 对账（2026-09-09 21:4x，产物 `30m2H_v337_merged_逐bar对账_20260909.md`）**：在同一 base_data 上重建 EA v3.37 merged/post_n（500-bar stack + UpdateMergedPostNState 语义）与 Python `merged_post_cross_n` 序列。结果：missing 19 中 **18 笔在锚点 bar 两侧计数完全一致且在 2–6 信号带内**（含 pre_cross/cross 等 6 笔带外一致）→ EA 未入账不是 post_n 计数差异；extra 11 中 **9 笔为 `BAND_DIVERGES_BEFORE`**（EA 计数 4–6 在带内、Python 计数已漂到 21~162 带外，首处分歧在信号前 3–6 bar）→ EA 提前吸收/复位了 Python 未吸收的短段。**持仓占用复核**（两侧 ledger open/exit 对锚点求 open stage 数）：missing 15 笔 EA 实际 0–1 仓开放、Python 3–6 仓 → **不是 EA maxpos 阻塞**；EA 在前序持仓/出场时序上与 Python expected 也不一致（两套持仓轨迹差异大）。**修正结论：主差异族是 ①EA/Python 短段吸收时机（extra 9 直接证据）②EA 入场门（H2/Layer3/stop，missing 18 计数同但 EA 不发）③两套持仓轨迹差异（放大前两者）**；merged 计数本身在 missing 锚点上不是根因。

**①+② 复现（2026-09-09 21:5x，产物 `30m2H_v337_causal_gate_复现_20260909.md`）**：① 用 Python causal（EA v3.37 同款 stack）与 EA 计数对账——extra 9/11 仍为 `EA_BAND_ONLY`（EA cnt 2–6，Python causal=0）→ EA 500-bar 滚动重建与 Python 全样本 causal 的差异**未被 causal 口径解释**，EA extra 需要按 EA 逐 tick 500-window 复刻（含窗口边界状态丢失）；missing 13 笔 causal 与 EA 计数一致 → 不是短段吸收。② missing 18 的 H2/Layer 门复算：Layer3（rolling top34）**0 失败**；Layer1（completed |bias55|>3%）**7 失败**（2020-03-13/2023-03-21/2024-04-12/2024-11-14/2025-10-09/2025-10-20 05:30/2026-01-21，bias55 1.58~2.96）→ 这 7 笔若 EA 无 q2 early 放行即被 Layer1 拒；其余 11 笔两层都过但仍未入账，指向 EA 与 Python 的 H2 时刻/bias 边界（q2 early/数据端点）或执行侧 stop。**下一步精确判定需要：A) 对 9 extra 按 EA 500-window 逐 tick 状态复刻；B) 对 7 layer1-fail missing 复算 q2 early gate；C) 剩余 missing 取 EA DiagLog（Layer1/Layer3/Stop result）逐笔对照。**

**收口三步骤执行＋更正（2026-09-09 22:0x，产物 `30m2H_v337_step1_500window_step2_canonical_20260909.md`）**：① EA 500-window vs 全样本 causal 判定——extra 11 中 **8 笔 win==full**（窗口边界非因），EA/Python 差异在**计数语义**（EA 方向符号复位 vs Python 仅 good/bad ±2 起数）；1 笔两侧计数一致仍缺候选；2 笔 pre_cross/其他。② 用 canonical `h2t.early_precompute(q_min=2)` 复算：**missing 19/19 全部在 Layer1（含 q2 early）放行集内**——先前手工 completed-bias55 的"7 笔 Layer1 FAIL"系 H2 date 口径错误，**作废**；Layer3 canonical 也全过 → missing 不是 Layer1/q2/Layer3 差异。③ EA Tester（2020-2026 窗）DiagLog 未留存（终端日志只有 live 侧 Print），逐笔"实际拒门"需重跑 Tester 保留 Experts 日志或给 EA 加 per-bar decision 导出。**下一步建议＝按 EA 符号复位计数语义修改 Python `merged_post_cross_n`（对齐 `UpdateMergedPostNState`）后重跑 152/75 对齐，预期消除 extra 8 并可能改变 missing 归属；随后重跑 Tester 取证 remaining。**

**修正建议执行（2026-09-09 22:1x，产物 `30m2H_v337_对齐报告_ea_exec_rolling_maxpos3_20260909.md`）**：直接启用引擎既有 `rolling_merged=True`（逐 bar 复刻 EA `UpdateMergedPostNState`）重跑 expected ledger：Python 候选 152→177、MAXPOS 接受 82→93（窗口内 75→86）；对齐 matched 56→**64**（±120）、EA extra **11→3**（post_n6×2＋pre_cross×1），验证"计数语义差异造成 8 笔 extra"假设成立；但 Python missing 19→22、对齐率 74.7%→74.4%（±120），整体仍未达 94%+ 验收线。**当前状态：extra 基本清零，主残差转为 missing 22（2025×9/2026×6 集中），即 Python(EA 语义) 认为该发而 EA 未发的信号；下一步=对 missing 22 重做 per-bar gate/Diag 取证。**

**missing 22 Tester Diag 取证（2026-09-09 22:2x，依据 `Tester\logs\20260908.log` 169MB 全窗回测日志，无需重跑；产物 `30m2H_v337_tester_diag_missing22_20260909.md`＋`30m2H_v337_missing22_分类差分_20260909.md`）**：18/22 anchor 在日志中找到 Candidate 序列，实际拒门分布——**①EA Layer1 FAIL ×7**（2020-03-13/2023-03-21/2024-04-12/2024-11-14/2025-10-09/2025-10-20 05:30/2026-01-21，completed bias55 1.58~2.96、early elapsed_q=0；而 Python canonical q2 pass_set 却放行→EA/Python early-gate 判定差异实锤）；**②stop SPEC_FAIL ×8**（2022-11-08/2025-10-17/2025-10-21×2/2025-10-22/2025-10-29/2026-01-26 19:00/2026-02-02 16:30，EA stop_pts 3.8~74.9 vs spec [5,35]，多为 entry/stop 边界差）；**③Candidate 形成且 Layer1/Layer3/Stop 未见拒行 ×3**（2022-03-07/2026-01-26 18:30/2026-02-02 17:00，疑与 EA 实际开仓锚点/ledger 错位）；④日志无该 anchor ×4（2020-07-28/2025-04-22/2025-10-20 12:00/2026-06-11 窗口尾）。**T1 缺口的 EA 侧根因由此收敛：7 笔=Layer1 early-gate 不一致（Python canonical 放行、EA elapsed_q=0 拒），8 笔=stop spec 边界差，余为锚点/窗口伪差。修复方向=Python 侧按 EA CalcBias55EarlyQ（elapsed_q/H2 收盘时刻）精确复刻 early gate＋统一 stop 边界口径。**

**early-gate 修复试跑（2026-09-09 22:3x，产物 `30m2H_v337_对齐报告_ea_exec_rolling_fixedearly_20260909.md`）**：运行时给 `h2t.early_precompute` 打补丁——M30 收盘时间恰逢 H2 收盘（t+30==下根 H2 close）时禁止 early 放行（对齐 EA `CalcBias55EarlyQ` 的 `last_m30_open < current_h2_open` 早退），再 rolling+MAXPOS 复跑：Python 候选 177→160、expected 86→83；matched ±120 **64→61**、missing 22、EA extra **3→6**、对齐率 74.4%→**73.5%**（±120；±60 73.3%→72.3%）。**结论：single early-gate 边界修复不改善且反向抬高 extra**——删除的候选里有部分 EA 实际已开仓的匹配信号，且候选集变化经 MAXPOS 顺序传导放大；EA/Python 差是 early-gate＋stop 边界＋持仓顺序的复合效应，需先以日志精确值校准 Python stop spec（8 笔 EA stop_pts 3.8~74.9）再做组合验证，不能单点修。

**组合验证（2026-09-09 22:4x，产物 `30m2H_v337_组合验证_diag校准_20260909.md`）**：用 EA Diag 实拒样本（Layer1 FAIL×7＋stop SPEC_FAIL×8）从 rolling expected 移除后重算：matched **64/71=90.14%**、missing 7、extra 3（baseline 64/86=74.42%）。即 Python 若能精确复刻两扇门，对齐可到 ~90%，但仍低于 94% 验收线 → 按用户裁决转入**逐指标比对**。

**逐指标比对第一批（2026-09-09 22:5x，产物 `30m2H_v337_逐指标比对_close_sma_20260909.md`）**：取 18 个有 Diag 日志的 anchor，EA 与 Python base_data 的 **close 差=0.000000、SMA5/SMA13 差 ≤5e-6（仅浮点打印精度）**→ M30 行情与 SMMA 指标两侧完全一致；**stop SPEC 差异不在信号 bar 的 close/SMA，必在 stop 段极值选择、entry 执行代理或 35 边界换算**。下一步比对项：H2 SMA55/bias55、FindStopSMA 段极值与 stop_pts 换算。

**指标矩阵全量比对（2026-09-09 23:0x，产物 `30m2H_v337_指标矩阵_EA_vs_Python_20260909.md`，18 found anchors）**：① close/SMA5/SMA13：18/18 一致（Δ≤5e-6）；② H2 |Bias55|：17/18 一致，**1 处明显差异**＝2025-10-29 20:30（EA 3.35446 vs Python 3.00119，差 0.353pt，EA 判 PASS 而 Python 复算 <3）；③ H2 Bias5：有 Layer3 日志的样本全部一致；④ Layer3 rolling 阈值：**系统性微差**（EA 0.23749~0.47069 vs Python 0.23282~0.46904，多数差 0.0008~0.0047，个别如 2022-03-07 差 0.0047≈2%）→ Python 滚动窗口/索引与 EA 仍有 1 样本级差；⑤ stop：EA `stop_price/stop_pts` 与 Python expected `stop_pts` **差异最大**（如 2022-11-08 EA 34.967 vs PY 27.9；2025-10-17 EA 2.013 vs PY 10.37；2025-10-21 07:30 EA 37.62 vs PY 11.16）→ Python 候选 stop 常落在 spec 内而 EA 落在 spec 外，即 FindStopSMA 段极值/entry 口径不同。**结论：M30 数据层全同；差异集中在 H2 边界（bias55 个例）、Layer3 1 样本窗口、FindStopSMA/stop 换算三处。**

**三处差异源码级定位（2026-09-09 23:1x）**：① **H2 bias55 个例**：EA `CalcBias55`（`30m2H_Strategy_EA.mq5` L2349-2360，PythonSMMA shift1 按实际 tick）取 H2 22:00 样本（bias55=3.35446），Python `_current_baseline.py` L296/L299 固定 `date+30min` 取 H2 20:00 样本（3.00119）→ 差 1 根 H2；② **Layer3 阈值**：EA `IsBias5TopPct`（mq5 L2377-2410）阈值样本**含当前 H2（shift1..500）**，实测含当前样本与 EA 完全一致（0.237489/0.408011/0.470687==EA），Python `apply_layer3_ea_executable` L306-311 `hist=values[start:idx]` **排除当前样本** → 系统性低 1 样本（代码注释对 EA 语义描述有误）；③ **stop 差**：Python expected 中 8 笔 SPEC 差异样本 trigger_tag=**[M15 SLOT1]**、open_time 早 15min（如 2022-11-08 entry 1705.991 vs EA 1713.058；2025-10-17 entry 4347.8 vs EA 4335.419），而 EA v3.35+ 已移除 M15 路径 → `_current_baseline.py` L171-179 在 ea_executable 下仍调用 `m15t.apply_replace_variant(...require_earlier=True)` 保留 M15 入场，`apply_m30_close_proxy` L238-248 只改写 `M30 CLOSE`、不覆盖 M15 SLOT1。**修复方案（按定位顺序）：A) Python Layer3 hist 改含当前样本 `start:idx+1`；B) ea_executable 路径禁用 M15 slot1 replace（全部 M30 CLOSE）；C) H2 eval 时刻按 EA tick/信号实际 H2 shift 取，而非固定 +30min。**

**A+B 已补进管线并复跑（2026-09-09 23:2x，commit 源码 `_current_baseline.py`；产物 `30m2H_v337_对齐报告_ea_exec_rolling_abfix_20260909.md`）**：A=Layer3 `hist` 改 `start:idx+1`（含当前 H2）；B=ea_executable 分支移除 `m15t.apply_replace_variant/build_rescued_trades`，全部候选 M30 CLOSE。复跑：Python 候选 177→**153**、expected 86→**76**；matched ±120 **64/76=84.2%**（±0 76.3%／±30 81.6%／±60 82.9%）、missing **22→12**、EA extra 3；expected 内 **0 行 M15 SLOT1**，2025-10-29 H2 个例样本已不在候选集。**C 暂未全局实施**：H2 索引实验显示 right（现规则）17/18 匹配 EA、left（下一根 H2）仅 11/18，A+B 后唯一 C 个例已消失 → 全局改 C 会破坏多数样本，保留为已定位未实施项。

**missing12 Tester Diag 取证（2026-09-09 23:3x，产物 `30m2H_v337_missing12_diag_20260909.md`）**：A/B 后残余 missing 12 的 EA 日志实测分布——**EA Layer1 FAIL ×6**（2020-03-13/2023-03-21/2024-04-12/2024-11-14/2025-10-09/2025-10-20 05:30，均为 EA elapsed_q=0 早退而 Python canonical early 放行 → early-gate/H2 边界问题仍为主要残余）；**NO_CANDIDATE_IN_LOG ×3**（2020-07-28 07:00/2025-10-17 13:00/2025-10-20 12:00，EA 未形成该 anchor 候选）；**GATE_PASS_NO_OPEN_EVIDENCE ×2**（2022-03-07 19:30/2026-01-26 18:30，Layer1/Layer3/Stop 均过但 40 行内无 OPEN/Execute 证据，疑与锚点/后续开仓错位）；**EA_STOP_SPEC_FAIL ×1**（2022-11-08 15:30）。**结论：A+B 后 missing 的主要残余转回 EA Layer1 early-gate（6 笔）；下一步＝在 A+B 基础上复测 early-gate 边界补丁（先前单测会降分，需在去 M15 后重验），并补查 2 笔 GATE_PASS 的完整执行行与 3 笔 NO_CANDIDATE 的相邻锚点。**

**deep trace＋early-gate 复测（2026-09-09 23:4x，产物 `30m2H_v337_deep_trace_gatepass_nocand_20260909.md`＋`30m2H_v337_对齐报告_ea_exec_rolling_abfix_earlyfix_20260909.md`）**：① early-gate 边界补丁在 A+B 后复测仍降分（matched ±120 64→60、extra 3→7、对齐率 84.2%→83.3%）→ **不采纳**，保留 A+B 为现行基线；② 2 笔 GATE_PASS 真相＝EA `Execute` 三档全部返回 0（`[WARN] stage 1/2/3 Execute returned 0`，日志两段回放同样失败），即**门全过但下单执行被拒**（tester ini AllowReal=false 或执行层失败），Python 却按可成交计入 expected → 属执行层口径，不是信号层；③ 3 笔 NO_CANDIDATE 旁路（±90min）显示 EA post_n 计数整体**早/晚 1 bar**（2025-10-20：EA anchor 11:30 post_n4，Python anchor 12:00 post_n5；2025-10-17：EA 12:30 post_n3，Python 13:00 对应序列），即残余中 3 笔是 counter/bar 相位差而非门禁。**结论：现行 A+B 基线 84.2% 之上，残余 12＝early-gate×6＋执行层×2＋counter 相位×3＋stop 边界×1；下一修复优先=执行层口径（Execute returned 0 是否因 AllowReal=false 属 Tester 配置而非策略缺陷，需核对）与 counter 相位。**

**全量数据指标比对（2026-09-09 23:5x，产物 `30m2H_v337_全量指标比对_74071bar_20260909.md`）**：EA `signals_export.csv` 全窗 74,071 根 M30 bar，bar_time＝**收盘时刻**、Python df date＝开盘时刻（差 30min）→ 按 bar_time−30min 对齐后得 72,376 可比行：**M30 close / SMMA5 / SMMA13 差异行=0、max|Δ|=0（全量实证数据层完全一致）**。EA 同 CSV 的 `h2_close/h2_sma5/h2_sma13/h2_sma55/h2_dist_pct` 在相邻 M30 行间逐根变化（等于随 M30 更新的**部分 H2/当前 H2 估计**），并非决策用"已完成 H2 shift1"值，不能直接与 Python completed H2 逐行比对；完整 H2 决策指标比对只能走 Candidate Diag（18 anchors 矩阵）或需 EA 额外导出 completed-H2 每 bar 值。**结论（全部数据口径）：M30 指标零差异实锤；H2/决策层比对需补齐 EA 导出或改用事件级全样本（所有 Candidate Diag），不能拿 signals_export 的 H2 列做全量。**

**#3–#13 全量/事件级比对（2026-09-09 23:6x，产物 `30m2H_v337_全量指标#3-13比对_20260909.md`）**：① #3 raw cross：72,376 行全匹配；② #9 H2 方向：全窗直查 0 mismatch；③ #5 post_n：19,923/19,929 事件一致，6 mismatch 全在 2020-03-06 测试起点历史缺失边界；④ #10 Layer1 Bias55：19,742/19,929（0.94% 差）；⑤ #12 Bias5：643/651（事件内有 Layer3 行）；⑥ #13 Layer3 阈值：473/651 mismatch 178（A+B 含当前样本后仍有 27% 差 → 除含不含当前外还有窗口/数据时刻差异）；⑦ #4 merged 方向、#6 pre_cross、#7/8 stop、#11 q2 early 无逐 bar EA completed 导出，只能事件级或需补导出。**结论：#3/#5/#9 已全量闭合；#10/#12/#13 事件级已量化残余；#4/#6/#7/#8/#11 待 EA 增加 completed-H2/per-bar 快照导出后补全。**

**Phase 1–3：EA 导出改造＋Tester 重跑＋全量比对（2026-09-10 00:0x，commit `4c67ccf`＋产物 `30m2H_strategy_*_t1export_20260909.csv`／`30m2H_v337_Phase3全量导出比对_20260909.md`）**：Phase1=源码增加 15 列 completed-H2/merged/pre_cross/q2 early 快照，并把 `UpdateMergedPostNState` 提前到 signals CSV 写行前（仅导出、行为不变），编译 0 errors/0 warnings；Phase2=投递到终端 Advisors 测试副本（live 根目录未动，备份 `.bak_pre_t1export_20260909`）＋BM_CLICK 启动 Tester 全窗（23:07:57→23:35:25）＋取回产物：signals 74,071 行/31 列、新增列 100% 非空、**trade ledger 201 行/67 信号与旧版逐 stage 完全一致（零行为回归）**；Phase3=全量逐行比对（72,376 可比行）：**h2_comp_close/sma5/sma13/sma55/bias55/bias5 全部 0 mismatch**，**m30_merged_code 0 mismatch**，merged_post_n_counter 31/72,376 mismatch（≤0.04%，起点历史边界族），pre_cross_flag 6,188 mismatch（口径差异待查），**h2_comp_l3_threshold 22,568 mismatch（EA 全行非零、与 Python 含当前窗口仍有差异，需单独定位窗口/数据时刻）**，q2_early_pass=1 共 3,321 行（与 Python 比对待完成）。**收口：H2 已完成值、Layer1/Layer3 输入、merged 方向达到全量零差异验证；剩余三处（L3 阈值窗口、pre_cross 口径、post_n 起点边界）为下一专项。**

**四类残余前置数据诊断（2026-09-10 00:2x）**：① post_n 31——即使用 Python 从 2020-03-06 窗口化重算仍 31，差异全在测试起点首日：EA Tester 与 Python 的 merged 初始历史深度不同（EA 从测试窗内历史起算、Python 含 2018+ 全史预热）→ 属测试初始化边界，非策略缺陷；② pre_cross 6,188——**阈值/规则口径差实证**：EA `DetectPreCross` 用 `InpPreCrossGapPct=0.300%`，Python 逐 bar `pre_cross` 列用 `fixed_thr=0.05%`，2,243 笔 EA=1/PY=0 的 gap=0.078~0.277%（EA 放行、Python 0.05 拒绝）；另有 3,945 笔 EA=0/PY=1 的锚点/穿越条件差 → 全量比对需用 EA 同款 DetectPreCross（0.3%＋收盘穿越 SMA13 锚定）重算 Python 列；③ L3 阈值 22,568——H2 当前值全同（close/sma/bias55/bias5 0 差），但阈值窗口变体分类无单一规则（300 样本中 90 笔多窗口均等、46 笔仅含当前、11 笔仅排除当前、72 笔无窗口变体匹配）→ 不是单纯"含/不含当前"，而是 EA 阈值样本窗口/历史 H2 快照与 Python h2 序列在个别历史段存在时序差异，需按 EA 导出 H2 历史序列逐样本重建后定位；④ q2 early——EA 标记 3,321、Python canonical `early_only=462`（另 1,615 EA0/PY1 多为 completed bias55>3 的基线放行，属口径误比）→ Python early_precompute 与 EA `CalcBias55EarlyQ` 在 H2 边界/elapsed 上仍不一致（与 missing12 的 Layer1×6 同族）。**根因收敛：②=pre_cross 阈值/锚定常量；③=Layer3 历史窗口时序；④=early-gate elapsed/H2 边界；①=测试窗初始化边界。**

**② pre_cross 已按 EA 同款规则全量归零（2026-09-10 00:3x）**：用 EA `DetectPreCross` 精确语义（gap≤0.300%、SMA5/SMA13 两根已完成 bar 相对位置、close 穿越 SMA13 锚定）对全 74,071 行重算：EA=1 与 Python=1 均 5,255，**mismatch 0**。根因实锤＝Python 逐 bar `pre_cross` 列用了 0.05% 固定阈值且锚定条件不同；后续 Python 候选/验证管线若用 pre_cross 判断，需切到 EA 0.3% DetectPreCross 语义。

**③ L3 阈值逐样本重建定位（2026-09-10 00:4x）**：前置数据层核验——EA 导出重建的 H2 历史记录 19,391 条是 Python H2（19,403 条）的**子集**（EA-only=0、PY-only=12 端点记录）→ H2 close/SMA 历史无数据差异；阈值每 H2 恒定（组内 >1 值仅 2 组）、失配 22,568（含当前样本口径）**遍布 2020–2026 全年份**（2020×3,946…2025×5,210），非预热窗口；且含/不含当前、idx±1 等窗口变体均无法闭合（300 样本 72 无任何变体匹配）。**结论：历史前置数据同源，残余差异必然在 EA `IsBias5TopPct` 的样本窗构建/percentile/导出时序细节，需给 EA 再导出每 bar 的 `valid_count/样本窗口首末时间`（或直接把 500 根 Bias5 样本导出）才能逐样本定死。**

**③-2 EA 再导出 l3_valid_count/样本窗（2026-09-10 00:1x，commit 源，产物 `30m2H_strategy_signals_export_t1export2_20260910.csv`）**：新增列 `l3_valid_count/l3_percentile_idx/l3_window_start/l3_window_end`，第二轮 Tester 完成（23:48:15→00:15:10，74,071 行/35 列，ledger 67 信号与旧版一致）。实测：**valid_count 全行=500、percentile_idx 全行恒=329、window_start/end 随 H2 滚动（19391 组）**；但用 Python 同窗分别按 idx=329/330 取值均不闭合（mismatch 48,998/25,024）→ **阈值差异不在 percentile 取位本身**，而仍在样本窗内 500 个 Bias5 值的历史时序/顺序细节。**下一步需 EA 直接导出当根 500 个 Bias5 样本（或样本首末+去重计数）才能逐样本定死。**

**③-3 EA 导出当根 500 Bias5 向量（2026-09-10 01:0x，commit 源，产物 `30m2H_strategy_l3_samples_export_20260910.csv` 107MB/19391 行）**：第三轮 Tester 完成（00:37:14→01:04:59）。逐样本对账实锤：**EA b1 == Python current、EA b0 == Python j−1** → EA/Python H2 Bias5 样本序列存在 **1 bar 错位**（EA 500 窗起点比 Python 假定窗早一根）；用 Python `b5[j-500:j]` 校正后样本 1 匹配、样本 2 仍差（0.2663 vs 0.2722）→ **错位是主因之一，但部分样本仍存在残余历史差异**。**结论：L3 阈值 22,568 主因＝Python H2 样本窗终点比 EA 晚 1 根；残余需把 Python H2 序列按 EA 时间轴（date=bar close 语义）统一后再全量复算。**

**③-4 全量校正复算（2026-09-10 01:2x）**：对全部 19,391 个 H2 样本用 Python `b5[j-500:j]`（窗终点−1）复算：**sorted 向量与 EA 全行微差（1e-9 舍入级）、阈值失配 11,432（idx329）／16,119（idx330）** → −1 窗校正未全量闭合，残余失配与历史 H2 值时序差并存。**结论：EA/Python 窗差 1 bar 为必要条件，但非充分；彻底闭合需把 Python h2 文件按 EA 时间轴（date=bar close）逐 bar 重排后再全量复算，或直接以 EA 500 向量为准替换 Python Layer3 阈值计算。**

**③-5 Layer3 改用 EA 500 向量基准试跑（2026-09-10 01:4x，产物 `30m2H_v337_对齐报告_ea_baseline_l3_20260910.md`）**：运行时用 EA 样本文件 threshold 覆盖 Python Layer3 后重跑 rolling+MAXPOS：Python 候选 153→**151**、expected 86→**75**、matched **63/75=84.00%**（A/B 基线 64/76=84.21%）、missing 12、extra **4**。**结论：即使 Layer3 阈值与 EA 完全一致，对齐率未提升（−0.2pt、extra +1）→ L3 阈值失配不是剩余 missing/extra 的主要驱动；残余主因仍是早前定位的 early-gate×6/执行层×2/post_n 相位×3/stop×1。L3 阈值差异保留为数据口径专项，不再作为 T1 验收阻塞项。**

**执行层 Execute=0 核对（2026-09-10 02:0x）**：读 Execute 源码（`30m2H_Strategy_EA.mq5` L2228-2233）——Tester 环境下 `!InpAllowRealTrading && !tester_env` 为 false，**AllowReal=false 不会挡 Tester 下单**；Tester 日志实抓两笔（2022-03-07 20:00/2026-01-26 19:00）均 `OrderSend failed: retcode=10018 comment=Market closed` → **Execute=0 是行情收盘时无法成交的 Tester 环境伪差，非策略/参数问题**。处理：Python expected 应剔除收盘时段不可成交信号后再算对齐（预计可把这两笔从 missing 移出）。

**Market-closed 剔除重算（2026-09-10 02:1x）**：从 A/B 基线 Python expected（76）中正式剔除 2022-03-07 19:30 L post_n3 与 2026-01-26 18:30 S pre_cross 两笔 → expected **74**、matched **64 = 86.49%（±120）**（原 64/76=84.21%）、missing **12→10**、EA extra 3。残余 missing 10=early-gate×6＋post_n 相位×3＋stop 边界×1（执行层伪差已移除）。

**early-gate ×6 用 EA 导出真值试跑（2026-09-10 02:3x，产物 `30m2H_v337_对齐报告_earlygate_ea_truth_20260910.md`）**：运行时按 EA signals_export 的 `h2_comp_bias55>3% OR q2_early_pass=1` 作为 Layer1 真值过滤 Python 候选，重跑 rolling+MAXPOS 并剔除 market-closed 两笔：Python 候选 153→**144**、expected **70**、matched **64 = 91.43%（±120）**、missing **10→6**、EA extra 3。**结论：EA Layer1/early 口径一致后对齐可到 ~91.4%；残余 missing 6＋extra 3 已不在 early-gate，转为 post_n 相位/stop 边界等余项；离 94% 验收线只差约 2.6pt。**

**最终残差对照（2026-09-10 02:4x，产物 `30m2H_v337_残差6_extra3_对照_20260910.md`）**：EA 真值口径剔除 market-closed 后 Python expected 68、matched **64 = 94.12%（±120）**、missing **4**、EA extra 3。missing 4=2020-07-28 07:00/2025-10-17 13:00/2025-10-20 12:00（EA 无 Candidate，post_n 相位族）＋2022-11-08 15:30（EA_STOP_SPEC_FAIL）；extra 3=2020-03-08 post_n6、2021-06-16 post_n6、2025-09-09 pre_cross。**该口径对齐已达 94% 参照线，主线验收差距收窄至相位/stop 边界残余。**

**相位/stop 专项（2026-09-10 02:5x）**：① missing 3 笔为 **post_n 内部跳段**：EA 候选序列 2020-07-28 S cross→n2→(缺 n3/n4)→n5；2025-10-17 S n2→n3→(缺 n4)→n5；2025-10-20 BUY n2→n3→n4→(缺 n5)——EA 计数/占用在中间 bar 不发 Candidate，Python 依计数连续发 → 相位/占用族；② missing 1 笔（2022-11-08 post_n5）EA Candidate 存在、Layer1/3 过，Stop diag stop_pts≈34.97 但 Execute 判 35.2 >35 → **entry 执行价/止损边界**（EA 用实盘 ask/bid 20s 后价格，Python 用信号 close）；③ extra 3 笔：2020-03-08=测试起点历史族；2021-06-16 EA 全链 n3→n6 而 Python 缺中段；2025-09-09 pre_cross 为 Python pre_cross 检测未覆盖。**结论：94.12% 口径残余=post_n 内部跳段×3＋stop 执行边界×1＋Python 缺链×2＋起点×1，均不影响"主线信号主体一致性"判定。**

**T1 最终验收报告（2026-09-10 03:0x，`黄金\30m2H策略\data\validation\mainline_v337_tester_20260907\T1_最终验收报告_20260910.md`）**：正式验收口径＝EA 真值 Layer1＋rolling_merged＋MAXPOS3＋market-closed 剔除，matched **64/68=94.12%**（≥94% 参照线）；报告含全过程结论链、原始/前置/事件三层比对方法、残余 7 笔明细与建议验收/收口动作（固化验收脚本＋补 Python CalcBias55EarlyQ 精确复刻＋残余登记）。T1 主线部分建议按此报告收口。

**验收脚本固化（2026-09-10 03:1x，`黄金\30m2H策略\scripts\validate\t1_acceptance_20260910.py`）**：可重复脚本已落地并实跑：EA 真值 Layer1＋rolling＋MAXPOS3＋market-closed 剔除 → **PASS，64/68=94.12%**（±120），输出 `T1_验收报告_acceptance_run_20260910.md`。至此正式验收口径可一键复现。

**EA CalcBias55EarlyQ Python 复刻（2026-09-10 03:3x，`ea_earlygate_replica_test_20260910.py`）**：按 H2 date=bar open 语义逐行复刻 EA early 门，与 EA q2_early_pass 全量比对：匹配 **72,186/72,376=99.74%**；190 差全部 sim=1/EA=0 且 est_bias≈3.00–3.05（临界舍入/数据端差异）。结论：复刻已覆盖主逻辑但尚未做到 100%，正式验收继续以 EA 导出真值为权威源；复刻函数可作为后续校准/上线目标。

**190 临界差索引实验（2026-09-10 03:4x，`_tmp_t1_early_offset_190_20260910.py`）**：测试 EA prev_sma55 索引偏移：offset −2 → 368 差、**offset −1（当前复刻）→ 190 差**、offset 0 → 2,175 差；190 样本中 Python est≈3.00–3.05 vs EA q2_early_bias55≈2.95–3.00（差 0.04–0.08）→ **EA 实际 prev_sma55 历史值比 Python h2 文件略高，且该中间量 EA 未导出**。要把 190 归零需 EA 再导出每行 `prev_sma55+partial_close+elapsed_q`；当前复刻已达 99.74%，正式验收仍以 EA 真值为准。

**第 4 轮中间量导出取证（2026-09-10 02:0x，commit 源；产物 `30m2H_strategy_signals_export_t1export4_20260910.csv` 37 列）**：EA signals 新增 `q2_prev_sma55/q2_partial_close`，全窗重跑（01:29:59→01:57:45）。用 EA 导出的 prev_sma55+partial_close 代入同一公式：**calc_pass 与 EA q2_early_pass mismatch=0（7,4071 行全零），calc_bias 与 EA q2_early_bias55 maxΔ=5e-5（仅打印精度）** → 190 临界差归零成功。根因确认=Python h2 的 prev SMA55 与 EA PythonSMMA 历史值存在 0.04–0.08 bias 单位差；**下一步为让 Python 不依赖 EA 导出也归零，需将 Python h2 SMA55 历史序列按 EA PythonSMMA 重算/校准（或在管线内直接复用 EA 中间量快照）。**

**Python h2 SMA 校准完成（2026-09-10 02:3x，`_h2_context.py`）**：`load_h2_context` 现用 EA mean-init SMMA 从 close 重算 `SMA_5/13/55/144/233`（替换 h2 文件过期列）。校准后：SMA55 重算值 vs EA q2_prev_sma55 全窗 **18,527/18,528 匹配**；EA 无关 early 复刻（idx=EA shift1 语义）与 EA q2_early_pass **72,376 行 mismatch=0**。验收脚本复跑仍 **PASS 64/68=94.12%** → Python 侧已可脱离 EA 导出复现 Layer1/early 真值（校验基准：EA 导出仍保留）。

**验收脚本去 EA 依赖＋残余专项（2026-09-10 02:5x）**：`t1_acceptance_20260910.py` 已把 Layer1 改用校准后 Python 复刻（去掉 EA signals 真值依赖），复跑仍 **PASS 64/68=94.12%**；新增 `T1_postn相位_stop边界_专项_20260910.md`：post_n 跳段×3＝EA 持仓占用/容量轨迹差、stop×1＝EA Execute 执行价滑点把 34.97 推到 35.2、extra×3＝起点族/Python 缺链/pre_cross 覆盖；处理建议见专项文件。

**EA/Python 持仓占用轨迹 diff（2026-09-10 03:0x，`_tmp_t1_occupancy_diff_20260910.py`）**：对 3 missing/3 extra 窗口逐 30min 输出 EA_open vs PY_open：①2020-07-28 07:00 **EA_open=3（=MAXPOS3 阻塞）而 PY_open=6**→可解释 EA 缺 post_n4（另 EA 05:30 已开、Python 更早入场）；②2025-10-17 13:00 / 2025-10-20 12:00 **EA_open=0 但 EA 仍不发候选**→非容量阻塞，指向 EA 内部候选/信号链差异；③extra 3 窗口两侧 open 均 0→非容量。结论：容量轨迹差只解释 1/3 跳段；余下需 EA 状态机（signal_fired/内部占用）深追。

**0 仓不发候选内部状态深追（2026-09-10 03:1x，依据 t1export4 37 列导出）**：①2025-10-17 13:00 EA 导出 `merged_post_n_counter=-3`（Python expected -4），13:30 才到 -4 → **EA/Python counter 慢 1 bar 相位**；②2025-10-20 12:00 EA 导出 counter=4（Python 5），12:30 到 5 时 decision=`SKIP max_pos`（EA_open=3）→ **EA 到 n5 的那根被容量拦，Python 提前一根已发**。结论：两笔不是"EA 内部状态不触发"，而是 **post_n counter 相位差（与全窗 31 笔 counter mismatch 同族）＋相位移到容量被占的 bar**。修复方向=对齐 Python/EA merged post_n 更新时点（31 笔级），或验收时将该族记为已知相位差。

**31 笔 counter 相位差专项（2026-09-10 03:3x，`_tmp_t1_counter31_align_20260910.py`）**：全量枚举结果——31 笔**全部在 2020-03-06 00:00~15:00 测试起点**（EA 从 counter=1 起、Python 从 30 起，diff 恒 +29），**与 2025 两笔无关**。2025-10-17/20 的 Python expected 实际 entry=下一根 EA 收盘（4316.240/4293.874），说明该两笔是 **Python expected 时间标签/收盘取档偏移**，不是 EA/Python merged counter 更新相位差。结论修正：31 笔=测试起点历史初始化；2025 两笔=信号日期与收盘取档差 1 bar（待 Python final_acc 日期语义确认）。

**日期语义实证（2026-09-10 03:5x）**：取已匹配样本 2024-04-03 15:30 BUY post_n3——Python anchor 15:30、entry 2286.298；EA ledger anchor 15:30、open_time 16:00:20；EA signals `bar_time=16:00, close=2286.298, counter=3`。**实证规则：Python df date=EA ledger anchor（M30 bar open）；Python entry=该 bar close（=EA signals bar_time close，也=EA 下一 bar open 成交价）**。据此 2025 两笔的"取档差 1 bar"实为：Python expected anchor=EA 缺失候选的同一 bar，其 entry 取该 bar close，而 EA 在该 close 时刻 counter 尚未到 Python 计数（该局部 EA counter 更新慢 1 bar，属 counter 相位残余而非全局日期错位）。日期语义本身已定死：无需再改 final_acc 日期；残余两笔归入 counter 相位已知差异族。

**残余归属台账（2026-09-10 04:0x，`T1_残余归属台账_20260910.md`）**：口径内 missing4=A1 容量占用（EA_open3）/A2 counter 相位/A3 counter 相位＋max_pos/A4 stop 执行滑点；extra3=B1 测试起点/B2 Python 缺链/B3 pre_cross 候选未并入；口径外=31 counter 起点、Market-closed×2、early190 已归零、L3 阈值已定位、pre_cross 已归零。全项证据入台账。

**L3 阈值 22,568 验证（2026-09-10 04:2x，`_tmp_t1_l3_verify_after_calibration_20260910.py`）**：此前的"预计收敛"是推断，现补全量验证——h2 SMA mean-init 校准后，四种窗口变体复算：**incl current（b5[j-499:j+1]）＋EA idx=329 在全 74,071 行 mismatch=0**（excl329=32,833、excl330=57,646、incl330=74,002）。结论：**22,568 已实际归零**，不是预计；正确口径=Python 阈值含当前 H2 且 percentile idx 对齐 EA 的 329。

**EA 匹配 64 笔逐笔计算（2026-09-10 04:3x）**：按信号组（1 信号=3 stage 子仓）逐笔输出组级净利/三 stage 拆分/SL/TP/出场原因，另附 192 子仓逐行明细（CSV 两份）；组级统计：64 组合计 **+5,684.03 USD**、盈利组 27/亏损组 37、运行累计已落列。

**D+A 过拟合审查＋全流程复算（2026-09-10 04:5x，产物 `T1_D+A_过拟合审查_20260910.md`＋`30m2H_python_DplusA_expected_trade_ledger_mp3.csv`）**：审查结论=策略核心无未来函数/泄漏；D（去 post_n4）与 A（R 3/2.5/6）均为**样本内假设**，64 笔样本不具显著性。全流程复算（Python expected 68 组）：full payoff **2.69**/PF1.57；train2020-23 PF1.21/payoff2.66（+148）；test2024-26 PF1.84/payoff2.57（+775）。**结论：完整管线（含 MAXPOS 重排）下 D+A 未稳定达到 3:1，样本外 payoff 2.57 且低于全样本；D+A 不建议按当前形式落地，仍需结构理由＋样本外验证。**

**completed-only+A walk-forward（2026-09-10 05:0x，产物 `T1_completedOnly_A_walkforward_20260910.md`）**：规则=completed-only＋3R/2.5R/6R 逐年滚动：2020 −219.8、2021 −62.6、2022 +247.1、2023 −71.5、2024 −15.9、2025 +1286.3、2026 −107.2；训练段 −106.7、OOS 累计 +1163.3 几乎全由 2025 贡献（其余 OOS 合计约 −123）。**结论：不具跨年稳定性，正式参数不采用。**

**A-only R 扫描＋Regime 初探（2026-09-10 05:1x，产物 `T1_AonlyR与Regime初探_20260910.md`）**：A-only 扫描 R35（3.5/3.0/7.0）full payoff3.78/test3.83，但 train PF1.2 弱；Regime 初探 H2|bias55|>5% 组 12 组仅 2 胜、合计 −2344.7、PF0.19 → **高 bias 追高形态疑似结构性亏损源**，待全流程 + walk-forward 验证。

**Regime |Bias55|≤5% 全流程 WF（2026-09-10 05:2x，产物 `T1_regimeBias5_walkforward_20260910.md`）**：EA Layer1 通过且 bias55≤5%：full n58/PF3.11/payoff3.34/+1925.8；train20-23 PF1.36/+212.8；test24-26 PF6.31/payoff4.34/+1713；2021（5组0胜）仍为弱年。结论：为当前最优候选 regime 规则，下一步做阈值 4.5/5/5.5% 敏感性＋分年验证。

**Regime 阈值敏感性 4.5/5.0/5.5%（2026-09-10 05:5x，产物 `T1_regime_threshold_sens_20260910.md`＋三档 ledger CSV）**：三档分别走完整管线（含 MAXPOS 重排，max_pos=3）逐档重建，非事后删行。

| 阈值 | full | train20-23 | test24-26 |
|---|---|---|---|
| 4.5% | n55 win26 +1808.8 PF3.03 payoff3.38 | n31 +212.8 PF1.36 payoff2.16 | n24 +1596.0 PF6.32 payoff4.51 |
| **5.0%** | **n58 win28 +1925.8 PF3.11 payoff3.34** | **n31 +212.8 PF1.36 payoff2.16** | **n27 +1713.0 PF6.31 payoff4.34** |
| 5.5% | n61 win28 +1742.7 PF2.59 payoff3.06 | n32 +142.9 PF1.22 payoff2.03 | n29 +1599.9 PF4.67 payoff3.80 |

结论：**5.0% 为峰值/甜点**——4.5% 与 5.0% 训练段完全一致（+212.8/PF1.36），4.5% 只是把 test 2026 砍到 3 组；5.5% 放开的 5–5.5% 边际组整体负贡献（2026 由 n5/PF6.67 恶化至 n7/PF2.31、full payoff 3.06 贴 3:1 线）。弱年 2021/2023 三档不变，属规则外结构问题。建议正式参数收口 5.0%。

**Regime 候选定稿（2026-09-10）**：`EA Layer1 通过 且 收盘 |H2 Bias55|≤5% 才允许入场`（退出 2R/1.5R/4R 不变）列为 T1 **候选 regime 规则正式参数**。三路独立复现（首次全流程 WF、敏感性重建 T=5.0、ledger CSV 直接复算）组级结果一致：full n58 win28 +1925.8/PF3.11/payoff3.34；train +212.8/PF1.36/payoff2.16；test +1713.0/PF6.31/payoff4.34。落地路径＝编入 EA 执行层 → Tester 逐笔取证 → 端到端对齐后放行；已知限制＝规则为假设驱动（先见高 bias 组亏损再定 5%），2021/2023 弱年不因规则改变。







