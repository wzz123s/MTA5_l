# AGENTS.md — 进入本工程的任何 AI/agent 必读

> 建立：2026-09-08 ｜ 本文件是行为入口，细则见 B 段指针。适用于千问工作助理、DSH harness、Codex、QQ 机器人网关及一切进场 agent。

## A. 工作纪律（用户制定的 4 条基础黄金规则 + 8 条进阶规则）

### A1 基础黄金规则
1. **先思考再编码**：开工前输出需求清单（理解 · 关键假设 · 待确认项 · 计划与验证/回滚法），确认后才写代码；纯读取/查询类操作除外。
2. **简单优先**：用能达成目标的最少代码实现；主动删冗余，不堆"以防万一"的分支与参数。
3. **只改任务相关**：不触碰无关代码、文件与格式；不顺手重构、不顺手美化。
4. **只对最终验收标准负责**：人给目标与验收标准，实现路径 AI 自主；但遇阻、需绕路、或可能要改动目标本身时——停，给出 ①根因分析 ②备选方案与利弊 ③建议方向，由人拍板。**边界：路径自主 ≠ 遇阻自决。**

### A2 进阶规则
5. **只做判断，不写死逻辑**：规则/阈值表达为可核验的判断，不散埋常量与补丁式 if-else。
6. **严格控制 token 消耗**：不重复执行同因失败的命令；大扫描/多轮重试先估成本；产出够用即止。
7. **代码冲突先读源码、不妥协**：冲突与疑似 bug 必须读通相关源码再动手；禁止 workaround 糊住，不为"跑通"向错误设计让步。
8. **拒绝假测试**：验证走真实下游路径；exit 0 / 自印"成功"字样不算证据；结论必须标注证据档位（执行级 / 同层读回 / 端到端）。
9. **分阶段设检查点**：长任务逐阶段落检查点，任一检查点不过即停下汇报，不带病推进。
10. **遵守原有代码风格**：命名、注释语言、目录约定与所在文件/策略现状一致，不自造新范式。
11. **有问题不隐瞒**：报错、降级、跳过、未验证项，如实写进汇报与问题记录。
12. **遇不合理结构主动说明**：职责外的结构问题只登记/报告，不擅自修改（走 R 规则流程）。

附 · 问与不问：能自查的（读文件/跑查询/查配置）不问人；答案只在人脑中且猜错返工代价高才问。

## B. 工程指针（细则所在，开工前读）

1. `00_README.md` —— 项目现状、策略状态表、目录地图、开放事项
2. `00_项目规则.md` —— R1~R9 变更/新文件/归档留档规则
3. `目录整理方案_20260908.md` §六 —— 硬引用冻结区全表（违反即破坏运行链路）

## C. 禁止（无例外，除非用户逐条授权）

- 在冻结区移动/删除文件（MT5 运行链路、监控脚本、认证基准所在路径）；
- 在 data\ 内新建 `_备份日期\` 副本；
- 新建不带 `_tmp_` 前缀的一次性脚本；
- 改动 `scripts\` 与 `observation_dashboard\` 的布局；
- 删除用户既有文件走永久删除（一律回收站）；
- 对 MT5 终端做杀进程/重启等打断 EA 的操作（DEMO 账户低风险 ≠ 可随意）。

## D. 收尾义务（每轮大任务结束时）

- 更新所动策略的 README + `00_README.md` §2·§6·§7 + 需要时 `00_文档中心\问题记录.md` 追加节；
- 文件操作留 dry-run 清单（`00_文档中心\归档\整理清单\`）并回填执行状态；
- 未解决事项登记进 `00_README.md` §6，不留口头。
