# MTA5_l 项目规则 · 更新与留档

> 本文件回答一个问题：**项目有了新东西（变更/文件/脚本/文档），按什么规则记录和留档。**
> 建立：2026-09-08（目录整理 Phase 1）｜详细背景见 目录整理方案_20260908.md §五b/§五c/§五d
> 适用对象：所有进场作业的 agent（千问 / DSH harness / Codex / QQ 机器人）与用户本人。

## 一、九条留档规则（R1~R9）

| # | 触发事件 | 必须动作 | 留档位置 |
|---|---|---|---|
| R1 | 策略代码/参数/口径变更 | ①该策略 README 状态行更新；②一行变更记入 00_README §7；③重大修复（P0/P1 级）在 00_文档中心\问题记录.md 追加一节；④动 EA：编译日志留 auto_trade\，旧 ex5/mq5 进策略 归档\（命名 `原名.bak_版本_日期`）；⑤在该策略根 `版本记录.md` 登记变更行（版本号／commit／tag／产物 sha256／状态） | 策略README + 问题记录 + auto_trade归档 + 版本记录.md |
| R2 | 新策略立项 | 按 00_文档中心\公共方法论\其他策略复用流程.md 建标准骨架（含 README 四要素：口径/EA/对齐/观察期）；策略审查状态表新增一行；00_README §2 同步 | 新策略目录 + 两张表 |
| R3 | 新增**公共**脚本/库（≥2 策略使用即算） | 放 `scripts\` 根；头部注释写明"谁在用"；登记 00_README §4 + scripts\README.md 双处 | scripts\ + 两处索引 |
| R4 | 新增**一次性**脚本 | 命名 `_tmp_主题_日期.py`（下划线前缀=归档候选）；用完当周移入 `scripts\_archive\<年-月>\`，移前 grep 确认无引用 | scripts\_archive\ |
| R5 | 新增经验/坑文档 | 先过"公共文档三判据"（见下）：公共→ 00_文档中心\公共方法论\ 并更新其 README 索引；策略私有→该策略 说明文档\ | 按判据二选一 |
| R6 | validation 产物生成 | 目录命名 `主题_YYYYMMDD`；**收口时必须把结论写进策略说明文档或策略审查状态表**（禁止裸实验）；活跃目录 ≤20，90 天后按整理规则沉 `_archive\<年>\`；禁止在 data\ 内建 `_备份日期\` 副本（用 归档\ 或 git） | 策略 data\validation → 归档 |
| R7 | 文档滚动 | 策略审查状态/问题记录**只保一个现役文件名**（不带日期，文内"数据截至"行担版本）；旧快照进 00_文档中心\归档\；同日多轮在文内加时间戳小节，不新开文件 | 00_文档中心 |
| R8 | 归档/移动/删除动作 | 动手前生成 dry-run 清单（CSV：序号,动作,源,目标,原因,状态），存 `00_文档中心\归档\整理清单\`；执行后回填状态；**用户既有文件删除一律回收站，agent 自产临时文件方可直接删** | 整理清单\ |
| R9 | git 提交（仓库重建后） | 代码类动作以 commit 代替 .bak；规范 `策略名: 动作 (对齐率/BUG号)`；EA `#property version` 为机器可读发布版本，**仅在发布/认证基线通过时递增**，并以 `策略名-EA名-vX.Y` 打 tag；.gitignore 白名单外的大数据/运行时产物不进库 | git 历史 |

> **版本载体约定（2026-09-09 用户裁决）**：EA `#property version` 为机器可读发布版本，仅在发布/认证基线通过时递增；每次实质变更在该策略根 `版本记录.md` 记一行（§3 待发布 或 §2 基线），发布时回填 tag 与 `.ex5` sha256。

## 二、公共文档三判据（R5 用）

满足任一条 → 进 `00_文档中心\`，不留策略目录：
1. 文中明说"供其他/后续策略复用"，或举例跨两个以上策略；
2. 描述**工具与环境层**知识（Tester 调用、编译部署、.set 覆盖、MT5 缓存、终端目录结构），而非某策略的信号/参数口径；
3. 被多个策略文档相互引用。

反向判据：策略特有的规格书、认证参数表、验证/对齐报告、待调整记录 → 留该策略目录。

## 三、收尾义务（每轮大任务结束的固定动作）

> 执行主体：**谁改谁记**（人或 AI 会话）——下列动作由做出改动的一方当场完成，不依赖他人提醒、不留口头。

1. 刷新 `00_README.md`：§2 状态表、§6 开放事项、§7 变更日志（滚动 10 条）；
2. 若动了策略：该策略 README 状态行同步（R1）；
3. 若做了文件操作：整理清单状态回填（R8）；
4. 遇整理方案未覆盖的结构问题：登记进 00_README §6，不擅改。

## 四、禁动清单（速查，全表见整理方案 §六）

- `QQ_AGENT_INSTRUCTIONS.md`（网关硬引用）、`启动DSH-Web.cmd`（桌面 .lnk）
- `scripts\` 共享五件套 + 两个公共库（monitor_*/qq_*/strategy_research_common/replay_raw_signals_with_stops）
- `observation_dashboard\`、`ea_alignment_logs\`
- `黄金\30m2H策略\参考实现工程\`（跨策略引擎+认证基准，除带 backup 字样子目录）
- `宏观日历研究\data\calendar_export.csv`、两个监控 adapter、`黄金\1H_M30_4H策略\scripts\signals\`
- 各策略 `auto_trade\`（MT5 同步锚点）
