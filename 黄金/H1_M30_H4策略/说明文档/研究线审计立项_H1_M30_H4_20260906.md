# 研究线审计立项：H1_M30_H4（2026-09-06 起草）

> 类型：研究线立项（无本地 EA 源码 → 审查 Python 回测/统计口径）
> 状态：立项中 ｜ 首轮扫描已完成（见 §四）

## 一、目标与背景
- H1_M30_H4 策略目录（黄金\H1_M30_H4策略）只有 glue 代码（py8：common loaders / docs / bundle / prepare / signals-export / validate-export）+ 24 个 md（Layer1/2/3 严格认证、Stage1/2 测试、M15_H2 提前触发、StopSpec/仓位档位/门区间等）。
- **真正的验证引擎在共享参考工程**：黄金\30m2H策略\参考实现工程\shadow_tests\multi_tf_matrix（TIMEFRAME_MATRIX 定义各 combo；validation_20260701 产出 Layer 认证结果）。
- 目标：确认该 combo 的回测口径无未来函数、统计口径（train/test/成本）成立，输出按严重度的发现报告；能修的修+重跑对照。

## 二、审计对象清单
1. 引擎（multi_tf_matrix\scripts，8 个 py，共 ~1878 行）：
   - _multi_tf_matrix_common.py(373) — TIMEFRAME_MATRIX / dir_prev1/2 特征 / signals 汇总
   - _multi_tf_matrix_validate.py(256) / _full_certify.py(253) / _test.py(120)
   - _gate_range_scan.py(209) / _gate_strength_certify.py(280) / _stop_range_scan.py(267) / _gate_stop_summary.py(120)
2. 本策略 glue（h1_m30_h4_common.py 268 = 仅 CSV IO + 读 validation 汇总）
3. 文档主张 vs 代码事实（Layer 认证数字能否从引擎重跑复现）

## 三、审计点（分级）
- **A（lookahead）**：① 信号触发用的 bar 是否为"已收盘"（尤其 M15/H2 提前触发 = 实时判 M15/H2，与 M30 close 同源性问题——_multi_tf_matrix_test.py L66 自注"当前 30m×2H + M15/H2 提前触发增强"）；② 段/方向特征是否含未来回溯（dir_prev1/2 只用到前向已收盘，未见 filter_short_segments 类全序列合并——首扫无此模式）；③ 入场价/触发时点是否 next-bar-open。
- **B（统计口径）**：train/test 划分（test_ev 打分已在引擎内，需核对样本外区间定义是否与 ABC 家族一致）；成本（点差/滑点）是否计入 pnl；参数挑选是否在同一段数据上反复挑（多重检验）。
- **C（一致性）**：H1_M30_H4 文档口径 vs 引擎 combo 定义 vs 相邻策略（2H_1H_6H）是否同源。

## 四、首轮扫描结果（2026-09-06）+ 下钻结论（2026-09-06 二期）
- 引擎层 8 个 py 的 shift/rolling/iloc[i±k] 扫描：**未发现** centered-rolling / 未来回溯式段合并等明确 lookahead 模式；特征以 dir_prev1/prev2（前向已收盘）为主。
- **下钻（追 import 链定位核心）**：combo = 黄金\30m2H策略\参考实现工程\scripts\_current_baseline.py（"30m×2H 主线"引擎；H1_M30_H4 经 TIMEFRAME_MATRIX 由同一引擎生成）。核心已**显式因果化**：
  - `rolling_merged_postn`（逐 bar 滚动，注释"EA 实时语义, 无 look-ahead"）、`_ea_merged_dir_code`（复刻 EA 段合并）
  - v3.36 修复：**M15 SLOT1 提前触发 & Layer3 阈值"样本不含当前 H2 bar"** 两处 lookahead（注释明指旧版 M15 SLOT1 不加 shift 导致 bias5 取错时刻、阈值含当前 bar 差 1 样本 → 边界翻转）——**A 类 lookahead 在引擎核心已基本解决**
- **遗留（下一批微钻）**：combo 实际构建器 `_m15_h2_combo_test.py`（build_candidate_frames/combine_full_sample/dedupe_anchor）+ `apply_replace_variant/build_rescued_trades`(m15t) 的触发/并集细节；B 类统计口径（SPLIT_DATE=2023-01-01、成本/点差、top_pct 参数挑选）；validation_20260701 认证数字重跑复现性。

## 五、执行计划（下钻，估算）
| 步 | 内容 | 输出 |
|---|---|---|
| 1 | 追 import 链定位机会/回测核心（common → 调用方） | 核心模块清单 |
| 2 | 逐核心函数核对已收盘/下根bar开盘/无未来合并 | A 类发现清单 |
| 3 | 重跑 H1_M30_H4 combo 关键认证（Layer3/StopSpec）比对文档数字 | 复现性报告 |
| 4 | 成本/样本外口径核对 | B 类发现 |
| 5 | 汇总报告（A/B/C 分级 + 修复建议），修复项单独再跑 | 立项验收 |

## 六、暂缓项
- 2H_1H_6H / USOIL_1H·2H·30m 为纯文档型（py=0），文档一致性核对可并入步骤 3（若其数字引用了本引擎则覆盖）。
