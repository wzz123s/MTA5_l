# 2H_M30_6H 对齐执行记录

> 执行日期：2026-08-10

## 已完成

1. 生成 Python expected ledger：
   - 路径：`data/validation/ea_alignment_current_candidate/python_expected_trade_ledger.csv`
   - 变体：`2H_M30_6H__6h_bias5_13_55_signed_pos`
   - StopSpec 过滤 `[2, 10]pt` 后共 `179` 笔，PF `2.5134`，测试 PF `3.7530`，0.01 lot 口径 `$1546.47`。
   - 导出脚本：`scripts/export_2h_current_candidate_ea_alignment.py`

2. 修复并编译 EA v1.04（`auto_trade/2H_M30_6H_CurrentCandidate_EA.mq5`）：
   - 信号改用已收盘 M30 bar（shift 1/shift 2）判定 SMA5/SMA13 穿越。
   - 6H bias 门改用已收盘 H6 bar（shift 1）。
   - 止损距离单位修正为 price/pt 直接比较（2-10pt），不再乘 `_Point*10`。
   - 出场改为逐 bar 检查止损，反向穿越在下一根 M30 open 退出。
   - 编译结果：`0 errors, 0 warnings`。

3. 修正 H6 周期配置：
   - 发现原 `.set` / Tester profile 中 `InpH6Period=16388` 实际是 H4，导致首轮回测 0 交易。
   - 已全部修正为 `16389`（PERIOD_H6），包括：
     - `auto_trade/2H_M30_6H_CurrentCandidate_EA.set`
     - EXNESS 终端 `MQL5\Presets` 与 `MQL5\Profiles\Tester` 下 2H 相关配置。

4. Tester 参数已预设：
   - Expert：`Experts\Advisors\2H_M30_6H_CurrentCandidate_EA.ex5`
   - Symbol：`XAUUSDm`，Period：`M30`，Model：Every tick
   - 日期：`2024.01.01` 至 `2026.07.25`
   - 入金：`500 USD`，杠杆：`1:2000`

## 待完成（需要 MT5 界面操作）

1. 打开 EXNESS MT5（`F:\Program Files\MetaTrader 5 EXNESS\terminal64.exe`）。
2. 打开 Strategy Tester（`Ctrl+R`），确认「设置」页参数与上方一致。
3. 点击「开始」运行回测。
4. 运行 `auto_trade/align_2h_after_tester.ps1`（自动复制 EA ledger 并执行逐笔对齐）。

## 已知问题

- 自动化尝试点击「开始」不稳定：曾成功启动一次回测（v1.04 + H4 错误配置），后续 UIA 点击未触发新回测，需手动点击一次。
- 首轮回测 0 交易已定位为 H6 周期配置错误，修正后需重跑验证。
