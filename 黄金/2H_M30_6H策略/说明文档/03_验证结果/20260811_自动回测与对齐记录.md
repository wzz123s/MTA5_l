# 2H_M30_6H 自动回测与对齐记录

> 执行日期：2026-08-11

## 执行方式

通过 UIA 自动化控制 EXNESS MT5（`F:\Program Files\MetaTrader 5 EXNESS`），自动点击 Strategy Tester「开始」完成回测，未依赖人工点击。

## 修复过程

1. `v1.04 -> v1.05`：H6 指标句柄在 Tester 中报 `4805`，改为从 M30 K 线聚合计算 H6 SMMA。
2. `v1.06 -> v1.07`：复刻 Python 研究口径：
   - 入场价 = 信号 K 线 `(H+L+C)/3`。
   - 止损 = 上一段 M30 SMMA13 区间极值（BUY 取 min，SELL 取 max）。
   - 出场 = 下一次反向穿越 K 线收盘价。
3. `v1.07 -> v1.08`：M30 信号由 `iMA(MODE_SMA)` 改为从原始 K 线计算 SMMA，与 Python 研究一致。
4. `v1.08 -> v1.09`：H6 聚合改为 Python 的 `label="right", closed="right"` 口径（H6 close 取 6h 边界 K 线收盘）。

## 最终对齐结果（v1.19）

| 指标 | 数值 |
|---:|---:|
| Python expected | 179 |
| EA actual | 179 |
| matched | 179 |
| missing | 0 |
| extra | 0 |
| 入口/止损/出场/PnL 最大差 | 0.0 |
| matched / expected | 100% |

对齐文件：

- `data/validation/ea_alignment_current_candidate/python_vs_ea_alignment_summary.csv`
- `data/validation/ea_alignment_current_candidate/python_vs_ea_alignment_detail.csv`
- `data/validation/ea_alignment_current_candidate/ea_alignment_current_candidate_report.md`

## 修复链（v1.09 -> v1.19）

1. `v1.10`：H6 门改为按 UTC 边界聚合，避免服务器时区偏移。
2. `v1.14`：放宽 `n < 720` 的旧历史守卫，让窗口初期信号也能进入 H6 门判断。
3. `v1.18`：H6 桶按 pandas `label=right/closed=right` 口径，周末等空桶直接丢弃，不再用前一根 close 填充。
4. `v1.19`：ledger 输出改为 3 位小数，与 Python expected 完全对齐。

## 结论

- 2H_M30_6H EA 已能自动跑 Tester 并导出逐笔 ledger。
- Python vs EA 逐笔对齐：`179/179 matched`，missing=0，extra=0，entry/stop/exit/pnl 最大差异均为 `0.0`。
- 达到与 1H_M30_4H 相同的 `expected=actual=matched` 对齐标准。

## 下一步

- 完成部署前参数评审，进入模拟盘候选队列（默认 SimMode=true，禁止真实交易）。
