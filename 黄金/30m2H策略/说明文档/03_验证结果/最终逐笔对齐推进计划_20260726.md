# 30m2H 最终逐笔对齐推进计划

> 创建时间：2026-07-26  
> 目标标准：按 `1H_M30_4H` 的标准推进到最终历史逐笔 matched。

## 1. 新的认定标准

从本文件创建开始，`30m2H` 不能再用“EA 已加载”“AutoTrading 已打开”“ready to live trade”来替代最终完成。

最终完成必须满足：

```text
Python expected ledger
EA actual ledger
expected_rows = actual_rows
matched_rows = expected_rows
missing_in_actual = 0
extra_in_actual = 0
entry_diff = 0
stop_diff = 0
exit_diff = 0
pnl_diff = 0 或在预先写明的 MT5 最小跳动容差内
```

如果只完成信号级对齐、加载包、模拟监控、首 tick 观察，都只能算部署前过程证据，不能算最终历史逐笔 matched。

## 2. 当前已知状态

| 模块 | 当前状态 |
| --- | --- |
| Python 主线 | 已有当前快照：102 笔，PF 11.54，PnL `$2200` |
| EA 工程 | 已有 `30m2H_Strategy_EA.mq5/.ex5` 和多轮编译日志 |
| 风控演练 | 已完成非生产 rehearsal，交易 15 笔，净利润 `$43.67` |
| 手工加载证据 | 已有 EA loaded / AutoTrading allowed / LIVE TRADING mode evidence |
| 首信号/首交易 | 截至 2026-07-24 20:13:10，accepted signal = 0，trade ledger rows = 0 |
| 最终历史逐笔 matched | 未完成 |

## 3. 与 1H_M30_4H 的差距

`1H_M30_4H` 已经具备：

- 独立 MT5 raw manifest。
- Python expected trade ledger。
- 专用 current candidate EA。
- MT5 Strategy Tester actual trade ledger。
- `88 / 88 matched`。
- 部署前参数评审。

`30m2H` 当前缺口：

1. 顶层 `黄金/30m2H策略/data/raw` 还没有和新策略同规格的 `raw_source_manifest.json`。
2. Python 当前主线结果在 `current_strategy_summary.md` 中明确，但还没有整理成和 1H 一样的 `python_expected_trade_ledger.csv`。
3. 现有三版本报告显示 Python-only、Python-MT5、MT5-only EA 存在交易数和收益差异。
4. 当前没有一份最终版 `python_vs_ea_alignment_summary.csv` 显示 expected=actual=matched。
5. 当前 live evidence 只能说明 EA 已加载并等待信号，不能证明历史逐笔一致。

## 4. 推进步骤

### Step 0：解决 Python 基线漂移

- [ ] 对比顶层快照和参考实现工程复跑快照。
- [ ] 明确最终采用哪一个 Python 基线：
  - 顶层快照：102 笔，PF 11.54，PnL `$2200`，Layer 3 threshold `0.652164`。
  - 参考实现工程复跑：121 笔，PF 2.96，PnL `$913`，Layer 3 threshold `0.374868`。
- [ ] 形成 `baseline_freeze` 记录。
- [ ] 未冻结前，不进入最终 EA 对齐。

复跑记录见：

- `黄金/30m2H策略/说明文档/03_验证结果/Python主线快照复跑记录_20260726.md`

### Step 1：冻结当前主线

- [ ] 冻结 Python 主线参数：
  - Layer 1: `|H2 Bias_55| > 3.0%`
  - Layer 2: `pre_cross + cross + post_n(2-6)`
  - Layer 3: `Bias_5 top 34%`
  - M15: `replace_any + rescue`
  - H2: `Layer1 q2 early-gate`
  - StopSpec: `[5,35] pt`
  - Stage: `2.0R / 1.5R trail / 4.0R force`
  - Lots: `0.01 / 0.02 / 0.03`
- [ ] 固化唯一参数文件 `.set`。
- [ ] 固化唯一 EA `.ex5` 和编译日志 hash。

### Step 2：补独立 MT5 raw manifest

- [ ] 用 MT5 API 重新抓取 M15/M30/H2 历史数据。
- [ ] 生成 `黄金/30m2H策略/data/raw/raw_source_manifest.json`。
- [ ] 记录 terminal/account/symbol/leverage snapshot。
- [ ] 不覆盖旧 raw 文件；新数据包放入 `data/raw/mt5_history/30m2h_mt5_gen_<range>`。

### Step 3：生成 Python expected ledger

- [ ] 从当前主线 Python 脚本输出逐笔交易。
- [ ] 每笔交易必须包含：
  - `trade_key`
  - `signal_anchor_time`
  - `entry_time`
  - `dir`
  - `stage`
  - `entry`
  - `stop`
  - `exit_time`
  - `exit`
  - `exit_reason`
  - `pnl_points`
  - `lot`
- [ ] 输出到 `data/validation/final_ea_alignment_20260726/python_expected_trade_ledger.csv`。

### Step 4：生成 EA actual ledger

- [ ] 用冻结 `.set` 跑 MT5 Strategy Tester。
- [ ] EA 必须导出：
  - `signal_anchor_time`
  - `entry_time`
  - `dir`
  - `stage`
  - `entry`
  - `stop`
  - `exit_time`
  - `exit`
  - `exit_reason`
  - `pnl_points`
  - `lots`
  - `ticket`
  - `position_id`
- [ ] 输出到 `data/validation/final_ea_alignment_20260726/30m2H_strategy_trade_ledger.csv`。

### Step 5：逐笔对齐

- [ ] 建立 30m2H 专用 `compare_python_vs_ea.py`。
- [ ] 先按 `trade_key = signal_anchor_time + dir + stage + sequence` 对齐。
- [ ] 再比对 entry、stop、exit、exit_reason、pnl_points、lot。
- [ ] 输出：
  - `python_vs_ea_alignment_summary.csv`
  - `python_vs_ea_alignment_detail.csv`
  - `alignment_report.md`

### Step 6：部署前评审

- [ ] 对齐通过后，生成部署前参数评审。
- [ ] EA 默认必须保留：
  - `InpSimMode=true`
  - `InpAllowRealTrading=false`
  - `InpMaxOpenPositions=1`
  - spread / risk / margin guards
- [ ] 模拟盘首信号和首交易仍要单独记录。

## 5. 当前阻塞

当前项目里能找到信号级诊断脚本：

- `黄金/30m2H策略/参考实现工程/scripts/_ea_python_signal_diff.py`
- `黄金/30m2H策略/参考实现工程/scripts/_ea_python_gap_diagnose.py`

但它们不是最终 trade ledger 逐笔 PnL 对齐器。要达到 `1H_M30_4H` 的标准，必须补齐 30m2H 专用的 trade ledger 对齐入口。

追加阻塞：2026-07-26 复跑参考实现工程当前 Python 快照时，结果与顶层快照不一致。必须先冻结唯一 Python 基线，再做 EA 对齐。

## 6. 当前结论

30m2H 的目标已经明确改为：必须达到最终历史逐笔 matched，且部署要求不低于 `1H_M30_4H`。

在该目标完成前，30m2H 的状态应标记为：

```text
python_mainline_ready__ea_loaded_evidence_exists__final_trade_ledger_alignment_pending
```
