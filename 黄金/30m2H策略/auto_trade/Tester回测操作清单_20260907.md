# 30m2H 主线 EA（v3.37）Tester 回测操作清单（2026-09-07）

> 目的：验证修复后主线 EA 的信号数与 Python 参考实现的对齐率（早期 62% → 目标 90%+）
> 对比基准：Python 认证口径 102 笔（2020-03-06 ~ 2026-06-11，带 Layer3 top34%，仓位 0.5/1.0/1.5，PF 11.54）

---

## ⚠️ 第 0 步：先处理「同名 .set 自动加载」坑（必做，否则参数被覆盖）

MT5 Tester 会**自动加载与 EA 同名的 .set**，覆盖 ex5 的 input 默认值。当前存在：

```
C:/Users/3762/AppData/Roaming/MetaQuotes/Terminal/DAD3B8CC3EAC09C0C9725021DF0C7A65/MQL5/Profiles/Tester/30m2H_Strategy_EA.set
```

这个 .set 是 07-10 自动保存的**旧配置**，含已过时/已删除的参数，必须处理：

| 旧 .set 里的问题 | 影响 |
|---|---|
| `InpUseM15EarlyEntry=true` | ⚠️ v3.37 已删除该参数（v3.35 起移除 M15，等价论证 144/144 零差）|
| `InpUseM15RescueTag=true` | ⚠️ 同上已删除 |
| `InpMagic=302025` | ⚠️ v3.37 默认已改 302036 |
| 缺 `InpExportTradeLedger` 等新参数 | 退回 ex5 默认值 |

**两种处理方式（二选一）：**

- **方式 A（推荐，参数可控）**：用下面「参数核对表」重写这个同名 .set 为 v3.37 认证口径。
- **方式 B（简单，用 ex5 默认）**：删除这个同名 .set，Tester 退回 ex5 的 input 默认值（v3.37 默认值基本就是认证口径，但 InpExportTradeLedger 等需确认）。

---

## 第 1 步：Tester 设置

| 项 | 值 |
|---|---|
| EA | `30m2H_Strategy_EA`（Experts 根，v3.37 已同步）|
| 品种 | `XAUUSDm` |
| 周期 | `M30` |
| 时间范围 | **2020-03-06 ~ 2026-06-11**（对齐 Python 认证）；或 2025-01-01 ~ 2026-08-25（纯测试段）|
| 模型 | 「仅开盘价」或「1 分钟 OHLC」（对齐 ABC 用「仅开价」）|
| 资金 | $10,000（认证口径 MaxDD -0.55% ≈ -$55）|
| 杠杆 | 1:500（Exness 模拟盘）|
| 优化 | 关闭 |

---

## 第 2 步：参数核对表（认证口径）

| 参数 | 认证值 | 说明 |
|---|---|---|
| `InpMagic` | **302036** | v3.37 默认（旧 .set 是 302025）|
| `InpRiskPct` | 3.0 | 动态手数风险比 |
| `InpStopLo` / `InpStopHi` | 5000 / 35000 | spec [5, 35] pt |
| `InpMaxPos` | 3 | 最大持仓 3 段 |
| `InpBias55Threshold` | **3.0** | Layer1 硬门 |
| `InpPostNMin` / `InpPostNMax` | 2 / 6 | post_n 范围 |
| `InpUseLayer1` | true | |
| `InpUseH2EarlyGateQ2` | true | H2 q2 早门 |
| `InpH2EarlyGateQ` | 2 | |
| `InpUseLayer3` | **true** | 认证口径带 Layer3 |
| `InpBias5TopPct` | **34.0** | Layer3 top34% |
| `InpBias5Lookback` | 500 | |
| `InpStage1Lots/2Lots/3Lots` | **0.01 / 0.02 / 0.03** | 仓位 0.5/1.0/1.5 |
| `InpStage1R` | 2.0 | Stage1 TP |
| `InpStage2TrailR` / `InpStage2ForceR` | 1.5 / 4.0 | Stage2 |
| `InpStage3On` | true | Stage3 merged cross 退出 |
| `InpFastMA` / `InpSlowMA` | 5 / 13 | M30 SMA |
| `InpH2SMA5/13/55` | 5 / 13 / 55 | H2 SMA（Bias_55 锚）|
| `InpSimMode` | false | 真实下单（Tester 虚拟资金）|
| `InpExportCSV` | **true** | 关键：导出 signals_export.csv 供对齐 |
| `InpExportTradeLedger` | true | 导出成交台账 |

> ⚠️ v3.37 已删除的参数（不要写在 .set 里）：`InpUseM15EarlyEntry`、`InpEnableM15Slot2`、`InpUseM15RescueTag`、`InpExportM15EntryDiag`。
> 说明：认证口径虽写「M15 replace_any + rescue」，但 v3.35 起已移除 M15 执行路径，注释论证与 M30-close 等价（144/144 零差），故 v3.37 无 M15 参数是预期。

---

## 第 3 步：跑完后对齐验证（告诉我，我来做）

回测完成后，我会自动：
1. 读 `MQL5/Files/30m2H_strategy_signals_export.csv`（EA 信号）与 `30m2H_strategy_trade_ledger.csv`（成交台账）
2. 对比 Python 认证的 102 笔信号（`参考实现工程/data/results/position_sizing_strict_certify_20260627/position_sizing_trade_detail.csv`）
3. 算对齐率（匹配信号数 / 总信号数），看是否从早期 62% 提升

---

## 注意事项

1. **Tester 与实盘抢资源**：跑 Tester 前建议先暂停实盘图表 EA（当前 9 个 EA 在跑），避免争抢终端。
2. **信号数预期**：修复后主线 EA 信号应接近 Python 的 102 笔（带 Layer3），早期 74 笔（无 Layer3 近似）或 118 笔的分歧应大幅缩小。
3. **时间范围影响**：若用全历史 2020-2026，信号数应约 102 笔（认证口径）；若只跑测试段 2025-2026，信号数更少（约 20-40 笔）。
4. **同名 .set 坑**：每次改参数后，Tester 会把当前参数存回同名 .set，下次自动加载——所以第 0 步的处理要持续注意。

---

## 附：完整 v3.37 认证口径 .set（可直接替换同名 .set）

```
; 30m2H_Strategy_EA v3.37 认证口径（2026-09-07）
InpMagic=302036
InpSymbol=XAUUSDm
InpRiskPct=3.0
InpStopLo=5000.0
InpStopHi=35000.0
InpMaxPos=3
InpBias55Threshold=3.0
InpPreCrossGapPct=0.3
InpPostNMin=2
InpPostNMax=6
InpUseLayer1=true
InpUseH2EarlyGateQ2=true
InpH2EarlyGateQ=2
InpUseLayer3=true
InpBias5TopPct=34.0
InpBias5Lookback=500
InpStage1Lots=0.01
InpStage2Lots=0.02
InpStage3Lots=0.03
InpStage1R=2.0
InpStage2TrailR=1.5
InpStage2ForceR=4.0
InpStage3On=true
InpFastMA=5
InpSlowMA=13
InpH2SMA5=5
InpH2SMA13=13
InpH2SMA55=55
InpH2SMA144=144
InpH2SMA233=233
InpSimMode=false
InpExportCSV=true
InpExportTradeLedger=true
InpExportStagePriceDiag=false
```
