# 30m2H_Strategy_EA 五份源码差异报告

> 生成：2026-09-07 ｜ 目的：人工确认权威版本前的内容差异比对（未编译/未同步）

## 一、五份源概况

| 标识 | 版本/位置 | 行数 | md5(前8) |
|---|---|---|---|
| A | auto_trade 当前 09-06 14:28 | 4003 | 11461532 |
| B | auto_trade .bak_v335_prebugfix 09-01 | 3999 | 5f89ae45 |
| C | auto_trade .bak_v331 08-12 | 3920 | 7a30a7e0 |
| D | B695BC Exness终端 Experts 09-01 | 4001 | 1fb8b53b |
| E | DAD3B8 主运行终端 Advisors 08-12 | 3854 | 782237b8 |

## 二、与 A（auto_trade 09-06）相似度

| 对比 | 相似度 | 差异行(+/-) |
|---|---|---|
| B (auto_trade .bak_v335_prebugfix 09-01) | 0.9983 | 14 |
| C (auto_trade .bak_v331 08-12) | 0.9539 | 365 |
| D (B695BC Exness终端 Experts 09-01) | 0.9993 | 6 |
| E (DAD3B8 主运行终端 Advisors 08-12) | 0.9737 | 207 |

## 三、函数构成差异（相对 A）

### vs B (auto_trade .bak_v335_prebugfix 09-01)

- 函数集合一致（差异在函数体内）

### vs C (auto_trade .bak_v331 08-12)

- A 独有: RecoverOpenPositions

### vs D (B695BC Exness终端 Experts 09-01)

- 函数集合一致（差异在函数体内）

### vs E (DAD3B8 主运行终端 Advisors 08-12)

- A 独有: RecoverOpenPositions

## 四、input 参数差异（相对 A，仅列取值不同者）

### vs B（0 处）


### vs C（2 处）

- `InpMaxPos`: A=1 -> C=3
- `InpUseM15EarlyEntry`: A=false -> C=true

### vs D（0 处）


### vs E（1 处）

- `InpMaxPos`: A=1 -> E=3

## 五、A vs D（终端部署版 09-01）关键差异片段（前4块）

### A 行 1491-1492 vs D 行 1491-1492 (replace)
```diff
- //| seg_dir = +1 means SHORT (find HIGHEST SMA13 as stop)             |
- //| seg_dir = -1 means LONG  (find LOWEST  SMA13 as stop)             |
+ //| seg_dir = +1 means SMA5 above SMA13 (bull segment)                |
+ //| seg_dir = -1 means SMA5 below SMA13 (bear segment)               |
```

### A 行 3044-3045 vs D 行 3044-3043 (delete)
```diff
- // P3-3 (2026-09-06): 死代码 — 全文件无调用方；InpUseM15EarlyEntry 默认 false 且仅此处读取。
- // v3.32 起统一走 M30-close 路径，本函数保留仅供历史对照，重构时删除（连同 M15EntryDiagWrite）。
```

## 五之二、A vs B（v335_prebugfix 基线，14 行差异全量）

### A 行 1491-1492 vs B 行 1491-1492 (replace)
```diff
- //| seg_dir = +1 means SHORT (find HIGHEST SMA13 as stop)             |
- //| seg_dir = -1 means LONG  (find LOWEST  SMA13 as stop)             |
+ //| seg_dir = +1 means SMA5 above SMA13 (bull segment)                |
+ //| seg_dir = -1 means SMA5 below SMA13 (bear segment)               |
```

### A 行 3044-3045 vs B 行 3044-3043 (delete)
```diff
- // P3-3 (2026-09-06): 死代码 — 全文件无调用方；InpUseM15EarlyEntry 默认 false 且仅此处读取。
- // v3.32 起统一走 M30-close 路径，本函数保留仅供历史对照，重构时删除（连同 M15EntryDiagWrite）。
```

### A 行 3907-3911 vs B 行 3905-3907 (replace)
```diff
-    // recomputed real merged direction and drop the signal on conflict.
-    // NOTE: only when the FINAL signal is post_n. pre_cross/cross override post_n by
-    // design and may legitimately oppose the merged direction (e.g. pre_cross LONG
-    // fires while SMA5 is still below SMA13), so they must NOT be cross-checked here.
-    if(signal_dir != 0 && !is_pre_cross_mode && !is_cross_mode)
+    // recomputed real merged direction and drop the signal on conflict. pre_cross/cross
+    // directions come from live SMA comparisons and are not affected by the counter.
+    if(signal_dir != 0 && (is_post_n_mode || is_post_n_mode_neg))
```
## 六、结论建议

1. 相似度排序可揭示演进链（A~D 若高相似则属同代、B/E/C 属更早或分支）。
2. 所有版本 `#property version` 均为 3.31：源码版本号未随迭代维护，确认权威版后建议统一维护真实版本号。
3. 确认权威源前，请勿用任一版本覆盖终端运行中 ex5（DAD3B8 Experts 根当前为 09-01 编译版）。
