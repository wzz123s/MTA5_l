# 阶段5 执行报告：模拟盘部署（MCT 大周期拐点）

> 完成时间：2026-09-01　状态：✅ 部署基础设施就绪（EA 已部署 + 监控已接入 + 仪表盘已挂载）；实盘 EA 挂载为交互式 GUI 动作（见第四节）
> 候选包：mct_d1_h4_hybrid_both_sd08-25_g003_b0_t120（原油 USOILm，D1 13×55 → H4 待定穿越 → hybrid 退出）
> 前置验证：因果回测 n=26/EV_R+0.40/PF_R1.86 ✅ → 稳健性全过 ✅ → EA 逐笔对齐 26/26=100% ALIGN: PASS ✅

---

## 一、部署清单

| 项目 | 状态 | 位置 |
|---|---|---|
| MCT_EA.ex5 编译产物 | ✅ 0 errors 0 warnings | 终端 Advisors（MQL5\\Experts\\Advisors\\MCT_EA.ex5） |
| SimMode 配置 | ✅ InpSimMode=true / InpAllowRealTrading=false / 0.01 手 / 虚拟余额 $500 | EA 默认 inputs + chart14 profile |
| 图表 profile（USOILm H4 + MCT_EA） | ✅ 已生成 | MQL5\\Profiles\\Charts\\Default\\chart14.chr |
| 监控适配器 | ✅ monitor_mct_adapter.py（实时 ledger 优先 → 因果回测兜底） | 阶段4_EA对齐/scripts/ |
| 仪表盘接入 | ✅ dashboard.csv/report 新增 MCT 行 | observation_dashboard/ |
| 每轮快照产物 | ✅ monitor_state.json / trades_snapshot / equity_0.5%/1% / open_positions / monthly_last6 | observation_dashboard/MCT/ |

## 二、监控快照（首轮，回测基线兜底）

| 指标 | 值 |
|---|---|
| 信号组（D1 穿越聚合） | 15（= 26 笔 EA 交易的信号级聚合） |
| 累计加权点数 | +13,830（≈ 因果账本 +13.83 价格单位 × 1000 点） |
| 0.5%/1% 风险净值（$500 起） | $638 / $638 |
| 警戒 | 无 |
| 口径 | 回测基线兜底（EA 挂载后自动切换为实时 ledger） |

## 三、监控集成说明

- **数据源优先级**：① 终端 MQL5\\Files\\MCT_trade_ledger.csv（EA SimMode 虚拟盘实时写入）→ ② 阶段4 因果化 expected ledger（26 笔历史）
- **点数口径**：USOILm 1 价格单位 = 1000 点；1% 风险复利按 $500 起
- **运行**：`python scripts/monitor_all_strategies.py`（Windows 任务计划每 6 小时，与既有 8 策略同链路）
- **新增/回补判定**：按 (signal_time, dir) 与上轮 trade_keys 差集
- **警戒规则**：最近12月加权点数<0 / 连续3月后2月为负 / 空单占比>85%（与既有策略一致）

## 四、实盘 EA 挂载（需交互式 GUI 一步）

本轮自动化尝试结论：chart profile 新增文件不会被终端自动加载（终端恢复上次会话的图表列表）；
Navigator 树控件在本环境不可枚举（root=0）。**挂载需在交互桌面会话执行一次**：

```powershell
# 1. 确认终端运行并登录（Exness-MT5Trial5 / 277752085）
# 2. 打开 USOILm H4 图表（或复用现有 USOILm 图，切到 H4）
# 3. 打开导航器（Ctrl+N）→ 展开「智能交易系统」→ 找到 MCT_EA
# 4. 双击 MCT_EA（或拖到图表）→ 设置弹窗确认 InpSimMode=true → 确定
# 5. 验证：终端 MQL5\Files\MCT_trade_ledger.csv 出现表头 → 随行情写入
# 6. 下轮 monitor 自动切换为实时 ledger 口径
```

## 五、护栏（挂载后生效）

- InpSimMode=true：虚拟盘，不产生真实订单（绝对安全护栏）
- InpAllowRealTrading=false：实盘交易守卫（阶段6 需显式确认才改）
- 固定 0.01 手 + 虚拟余额 $500：观察净值按 0.5%/1% 风险外推
- 同方向 1 持仓 + 结构止损 0.8%~2.5% + 120bar 超时：策略内建风控

## 六、监测目标（阶段5 验收）

- **≥1 个完整机会周期（3~6 个月）无异常**：EA 稳定运行、账本连续写入、无重复/丢失交易
- 与既有 8 策略同链路挂 observation_dashboard，每 6 小时自动快照
- 信号退化（累计加权点数转负）即触发警戒，按计划纪律停用

## 七、下一步（阶段6 实盘前置）

1. 交互会话完成 MCT_EA 挂载（第四节步骤）→ 监控进入实时 ledger 口径
2. 观察 ≥1 个机会周期（预计 3~6 个月，年 5 笔机会）
3. 稳定后：人工按《大周期拐点操作速查卡》执行 + 系统信号核对 → 实盘评审（V6：真实点差/滑点/隔夜复核）→ 操作者显式确认后 SimMode=false
